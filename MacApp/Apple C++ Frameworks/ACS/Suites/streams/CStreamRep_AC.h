// CStreamRep_AC.h// Copyright © 1985-2001 by Apple Computer, Inc. All rights reserved./*	Change History		02/14/01	Don't include Streams_AC. 		01/02/01	Update copyright.		03/24/99	Update copyright notice.*/#ifndef __CStreamRep_AC__#define __CStreamRep_AC__// ACS#ifndef __CRefCountingPtr_AC__#include "CRefCountingPtr_AC.h"#endif//----------------------------------------------------------------------------------------// CLASS CStreamRep_AC//----------------------------------------------------------------------------------------class EXPORT_AC CStreamRep_AC : public MRefCountable_AC// A reference-counted stream representation, which provides the protocol for// various implementations in ACS streams.{protected:	CStreamRep_AC();public:	virtual ~CStreamRep_AC();	// positioning	virtual long GetWritePosition() const;	virtual void SetWritePosition(long newPosition);				virtual void SetReadPosition(long pos);		virtual long GetReadPosition() const;	// streaming	virtual void WriteBytes(const void* inPtr, long amt) = 0;	virtual void ReadBytes(void* inPtr, long amt) = 0;	virtual void Flush();		// For flushing buffered streams. This is sent to the pipes, and does nothing		// on nonbuffered streams.};// typedefstypedef CRefCountingPtr_AC<CStreamRep_AC> CStreamRepPtr_AC;#endif // __CStreamRep_AC__
// CCountingStream_AC.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved.// Originally written by Larry S. Rosenstein. Used by permission/*	Change History		02/28/01	Don't include Streams_AC. 		01/02/01	Update copyright.		08/23/00	Added embedded documentation.		03/24/99	Update copyright notice.*/#ifndef __CCountingStream_AC__#define __CCountingStream_AC__#ifndef __CRandomAccessStream_AC__#include "CRandomAccessStream_AC.h"#endif//----------------------------------------------------------------------------------------// CCountingStream_AC: This implements a stream that counts up the total number of bytes. It// is only usable for writing. You will get a debugging break if you try to read from this// kind of stream. //----------------------------------------------------------------------------------------class EXPORT_AC CCountingStream_AC : public CRandomAccessStream_AC// CCountingStream_AC implements an object that accumulates the// number of bytes stored in a stream. This class provides accessor// methods that allow you to get or set the current position in// the stream and get or set the current size of the stream.//    For example, MacApp uses a counting stream to determine the// size of the user selection associated with a designator object,// as follows:// CCountingStream_AC* aStream; 			// declare the counting stream// aStream = new CCountingStream_AC; 		// create the counting stream// aStream->ICountingStream(); 				// initialize the counting stream// aStream->WriteStreamObject(theSelection, kNoStandardObject); 		// update the stream// rsrcForkBytes += aStream->GetSize();		// add the size of the stream to the fork size// aStream->Free();							// stream no longer needed - be free{	public:	//------------------------------------------------------------------------------------	// Creation/Destruction 	//------------------------------------------------------------------------------------	CCountingStream_AC();		// Constructor	virtual ~CCountingStream_AC();		// Destructor};	#endif // __CCountingStream_AC__
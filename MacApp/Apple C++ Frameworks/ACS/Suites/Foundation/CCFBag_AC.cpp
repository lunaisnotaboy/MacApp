//----------------------------------------------------------------------------------------// CCFBag_AC.cpp// Copyright © 1999-2001 by Apple Computer, Inc., all rights reserved.//----------------------------------------------------------------------------------------// Contains:	C++ wrappers for CoreFoundation bag collection/*	Change History:		05/03/01	Consolidate inlines. 		02/27/01	Redesign so CF references point to CF objects. 		01/02/01	Update copyright.		10/31/00	Move to ACS. 		07/31/00	ThrowIfBadAlloc_AC. 		07/29/00	Build standalone without ACS. 		07/10/00	Allocators. 		06/29/00	Error handling. 		06/06/00	Initial version. */#ifndef __CCFBag_AC__#include "CCFBag_AC.h"#endif// ACS#ifndef __CException_AC__#include "CException_AC.h"#endif// Toolbox#ifndef __MACERRORS__#include <MacErrors.h>#endif//========================================================================================// __CFBag//========================================================================================/*static*/ CFBagRef __CFBag::Create(CFAllocatorRef allocator, const void** values, 									CFIndex numValues, const CFBagCallBacks* callBacks){	CFBagRef result = CFBagCreate(allocator, values, numValues, callBacks);	ThrowIfBadAlloc_AC(result);	return result;}/*static*/ CFBagRef __CFBag::CreateCopy(CFAllocatorRef allocator, CFBagRef theBag){	CFBagRef result = CFBagCreateCopy(allocator, theBag);	ThrowIfBadAlloc_AC(result);	return result;}/*static*/ CFMutableBagRef __CFBag::CreateMutable(CFAllocatorRef allocator, 												  CFIndex capacity, 												  const CFBagCallBacks* callBacks){	CFMutableBagRef result = CFBagCreateMutable(allocator, capacity, callBacks);	ThrowIfBadAlloc_AC(result);	return result;}/*static*/ CFMutableBagRef __CFBag::CreateMutableCopy(CFAllocatorRef allocator, 													  CFIndex capacity, 													  CFBagRef theBag){	CFMutableBagRef result = CFBagCreateMutableCopy(allocator, capacity, theBag);	ThrowIfBadAlloc_AC(result);	return result;}//----------------------------------------------------------------------------------------// End of CCFBag_AC.cpp
// CJavaSession_AC.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved. ////	Our sincere thanks to Carsten Brinkshulte who graciously contributed his example //	Java code in the hopes that the MacApp team would refine it and make it an //	official part of MacApp./*	Change History		03/01/01	Cosmetic. 		01/02/01	Update copyright.		08/29/00	Need to include CPascalString directly, because it is no longer					included by the geometry classes. 		01/12/00	JavaProcMsgLenType is SInt32.		01/05/00	Bring back Carbon conditional definition of JavaProcMsgLenType. 		12/24/99	Added JavaProcMsgLenType typedef.		12/07/99	Added Exit, AuthentificateURL and LowMemory to CJavaSession_AC.					Condition the callbacks on UNIVERSAL_INTERFACES_VERSION, not qCarbon.					Added JavaExitProc, JavaAuthentificateURLProc and JavaLowMemoryProc callbacks.		10/18/99	Create a typedef (JavaProcMsgLenType) for Java write and error proc message len types,					for Carbon, Universal interfaces 3.2 and 3.3, instead of creating					multiple function declarations.		07/22/99	[C] Different URLToFSSpec, ConsoleErrorProc and ConsoleWriteProc.		03/24/99	Update copyright notice.		11/05/98	For CarbonSeed110398, removed special definitions of ConsoleWriteProc and ConsoleErrorProc		10/26/98	For Carbon, changed definitions of ConsoleWriteProc and ConsoleErrorProc*/#ifndef __CJavaSession_AC__#define __CJavaSession_AC__// ACS#ifndef __CLongGeometry_AC__#include "CLongGeometry_AC.h"#endif#ifndef __CPascalString_AC__#include "CPascalString_AC.h"#endif// Java#ifndef __JMANAGER__#include <JManager.h>#endif// Externalclass MJavaView_AC;class CFSSpec_AC;class CJavaAppletContext_AC;//----------------------------------------------------------------------------------------// CJavaSession_AC//----------------------------------------------------------------------------------------class EXPORT_AC CJavaSession_AC{public:	CJavaSession_AC();		// Constructs this object.		virtual ~CJavaSession_AC();		// Closes the Java session and destructs.			virtual void JavaOpen();		// Opens the Java session.			void JavaIdle();		// Call from your application's Idle function so that Java applets get idle time.		void JavaMenuSelect(short theMenuNumber, short theItemNumber);		// Call from your application's MenuSelect function so that Java applets 		// can handle menu commands.			virtual short NewMenuIDRequest(JMAWTContextRef ref, bool isHier);		// Called from CJavaAppletContainer_AC, to get a unique menu ID.		// Return statically incremented default menu IDs.			virtual void RequestFrame(JMAWTContextRef context,										JMFrameRef newFrame,										JMFrameKind kind,										const CLongPoint_AC& initialSize,										bool resizeable,										JMFrameCallbacks* callbacks) = 0;		// Called from CJavaAppletContext_AC, to obtain a new applet frame.		// You must override this to assign a view, or open a window containing a view.	//------------------------------------------------------------------------------------	// Managing the active view and the view under the mouse.	//------------------------------------------------------------------------------------	static MJavaView_AC* GetActiveJavaView();	static void SetActiveJavaView(MJavaView_AC* activeJavaView);	static CJavaAppletContext_AC* GetActiveJavaContext();	static MJavaView_AC* GetMouseOverJavaView();	static void SetMouseOverJavaView(MJavaView_AC* mouseOverJavaView);	//------------------------------------------------------------------------------------	// Console standard output, error, and input	//------------------------------------------------------------------------------------			virtual void StdOut(const char* message, UInt32 messageLen);	virtual void StdErr(const char* message, UInt32 messageLen);	virtual SInt32 StdIn(char* buffer, SInt32 maxBufferLength);	//------------------------------------------------------------------------------------	// Convenience methods	//------------------------------------------------------------------------------------	static void AddToClassPath(const CFSSpec_AC& spec);	static Handle FSSpecToURLHandle(const CFSSpec_AC& spec);	static CStr255_AC FSSpecToURL(const CFSSpec_AC& spec);		//	#if qCarbon	static void URLToFSSpec(JMTextRef url, CFSSpec_AC& spec);	#else	static void URLToFSSpec(const char* url, CFSSpec_AC& spec);	#endif		static void FilterEndOfLineChars(char* text, UInt32 length);		//------------------------------------------------------------------------------------	// CALLBACKS	//------------------------------------------------------------------------------------		virtual bool Exit(SInt32 status);		virtual bool AuthentificateURL(const char *url, const char *realm, char userName[255], char password[255]);		virtual void LowMemory();	//------------------------------------------------------------------------------------	// Useful references to the JM session and its global instance.	//------------------------------------------------------------------------------------		static JMSessionRef GetSessionRef() { return fgJavaSession; }			static CJavaSession_AC* Instance() { return fgInstance; }	private:	OSStatus fStatus;	static CJavaSession_AC* fgInstance;	// the global instance; _not_ an auto pointer, to allow mixins.	static JMSessionRef fgJavaSession;	static MJavaView_AC* fgActiveJavaView;	static MJavaView_AC* fgMouseOverJavaView;	static JMSessionCallbacks fgSessionCallbacks;};//----------------------------------------------------------------------------------------// CALLBACKS//----------------------------------------------------------------------------------------#if (UNIVERSAL_INTERFACES_VERSION <= 0x0320)typedef UInt32 JavaProcMsgLenType;#else typedef SInt32 JavaProcMsgLenType;#endifextern "C" void ConsoleWriteProc(JMSessionRef session, const void* message, JavaProcMsgLenType messageLen);extern "C" void ConsoleErrorProc(JMSessionRef session, const void* message, JavaProcMsgLenType messageLen);extern "C" SInt32 ConsoleReadProc(JMSessionRef session, void* buffer, SInt32 maxBufferLength);extern "C" Boolean JavaExitProc(JMSessionRef session, SInt32 status);extern "C" Boolean JavaAuthentificateURLProc(JMSessionRef session, const char *url, const char *realm, char userName[255], char password[255]);extern "C" void JavaLowMemoryProc(JMSessionRef session);#endif // __CJavaSession_AC__
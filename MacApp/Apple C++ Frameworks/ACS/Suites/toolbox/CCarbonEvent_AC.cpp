//----------------------------------------------------------------------------------------// CCarbonEvent_AC.cpp// Copyright © 2000-2001 by Apple Computer, Inc. All rights reserved. //----------------------------------------------------------------------------------------/*	Change History		04/07/01	[PB] Assignment to 'UInt32' from 'double'. 		01/02/01	Update copyright.		12/11/00	Work around toolbox not supporting converting key events							to event records. 		12/04/00	Move into ACS. 		11/30/00	Add member templates for getting and setting parameters. 							Define wrapper classes for individual event types. 		11/29/00	Initial version. */#ifndef __CCarbonEvent_AC__#include "CCarbonEvent_AC.h"#endif#if qCarbonEvents// ACS#ifndef __CException_AC__#include "CException_AC.h"#endif// Toolbox//========================================================================================// CCarbonEvent_AC//========================================================================================//----------------------------------------------------------------------------------------// Low-level event fetching//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// ReceiveNext//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::ReceiveNext(UInt32 inNumTypes, const EventTypeSpec* inList,									  EventTimeout inTimeout, bool inPullEvent){	return ReceiveNextEvent(inNumTypes, inList, inTimeout, inPullEvent, &fEvent);}//----------------------------------------------------------------------------------------// Core event lifetime APIs//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// Create//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::Create(CFAllocatorRef inAllocator, UInt32 inClassID, UInt32 kind,								 EventTime when, EventAttributes flags){	return CreateEvent(inAllocator, inClassID, kind, when, flags, &fEvent);}OSStatus CCarbonEvent_AC::Create(UInt32 inClassID, UInt32 kind,								 EventTime when, EventAttributes flags){	return CreateEvent(NULL, inClassID, kind, when, flags, &fEvent);}//----------------------------------------------------------------------------------------// Copy//----------------------------------------------------------------------------------------EventRef CCarbonEvent_AC::Copy() const{	return CopyEvent(fEvent);}//----------------------------------------------------------------------------------------// Retain//----------------------------------------------------------------------------------------EventRef CCarbonEvent_AC::Retain(){	return RetainEvent(fEvent);}//----------------------------------------------------------------------------------------// GetRetainCount//----------------------------------------------------------------------------------------UInt32 CCarbonEvent_AC::GetRetainCount() const{	return GetEventRetainCount(fEvent);}//----------------------------------------------------------------------------------------// Release//----------------------------------------------------------------------------------------void CCarbonEvent_AC::Release(){	ReleaseEvent(fEvent);}//----------------------------------------------------------------------------------------// SetParameter//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SetParameter(EventParamName inName, EventParamType inType, 									   UInt32 inSize, const void* inDataPtr){	return SetEventParameter(fEvent, inName, inType, inSize, inDataPtr);}//----------------------------------------------------------------------------------------// GetParameter//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::GetParameter(EventParamName inName, EventParamType inDesiredType,									   EventParamType* outActualType,		// can be NULL									   UInt32 inBufferSize,									   UInt32* outActualSize,				// can be NULL									   void* ioBuffer) const{	return GetEventParameter(fEvent, inName, inDesiredType, outActualType, inBufferSize, 							 outActualSize, ioBuffer);}//----------------------------------------------------------------------------------------// Getters for 'base-class' event info//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// GetClass//----------------------------------------------------------------------------------------UInt32 CCarbonEvent_AC::GetClass() const{	return GetEventClass(fEvent);}//----------------------------------------------------------------------------------------// AssertClass//----------------------------------------------------------------------------------------void CCarbonEvent_AC::AssertClass(UInt32 inClassID) const{	if (fEvent && (GetClass() != inClassID))		ThrowError_AC(eventClassIncorrectErr);}//----------------------------------------------------------------------------------------// GetKind//----------------------------------------------------------------------------------------UInt32 CCarbonEvent_AC::GetKind() const{	return GetEventKind(fEvent);}//----------------------------------------------------------------------------------------// AssertKind//----------------------------------------------------------------------------------------void CCarbonEvent_AC::AssertKind(UInt32 kind) const{	if (fEvent && (GetKind() != kind))		ThrowError_AC(eventKindIncorrectErr);}//----------------------------------------------------------------------------------------// GetTime//----------------------------------------------------------------------------------------EventTime CCarbonEvent_AC::GetTime() const{	return GetEventTime(fEvent);}//----------------------------------------------------------------------------------------// Setters for 'base-class' event info//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// SetTime//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SetTime(EventTime inTime){	return SetEventTime(fEvent, inTime);}//----------------------------------------------------------------------------------------// Event Queue routines (posting, finding, flushing)//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// PostToQueue//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::PostToQueue(EventQueueRef inQueue, EventPriority inPriority){	return PostEventToQueue(inQueue, fEvent, inPriority);}//----------------------------------------------------------------------------------------// InQueue//----------------------------------------------------------------------------------------bool CCarbonEvent_AC::InQueue(EventQueueRef inQueue) const{	return IsEventInQueue(inQueue, fEvent);}//----------------------------------------------------------------------------------------// Helpful utilities//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// IsUserCancel//----------------------------------------------------------------------------------------bool CCarbonEvent_AC::IsUserCancel() const{	return IsUserCancelEventRef(fEvent);}//----------------------------------------------------------------------------------------// ConvertToEventRecord//----------------------------------------------------------------------------------------bool CCarbonEvent_AC::ConvertToEventRecord(EventRecord& outEvent) const{	return ConvertEventRefToEventRecord(fEvent, &outEvent);}//----------------------------------------------------------------------------------------// InMask//----------------------------------------------------------------------------------------bool CCarbonEvent_AC::InMask(EventMask inMask) const{	return IsEventInMask(fEvent, inMask);}//----------------------------------------------------------------------------------------// Explicit Propogation//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// CallNextHandler//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::CallNextHandler(EventHandlerCallRef inCallRef){	return CallNextEventHandler(inCallRef, fEvent);}//----------------------------------------------------------------------------------------// Sending Events//----------------------------------------------------------------------------------------//----------------------------------------------------------------------------------------// SendToEventTarget//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SendToEventTarget(EventTargetRef inTarget) const{	return SendEventToEventTarget(fEvent, inTarget);}//----------------------------------------------------------------------------------------// SendToApplication//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SendToApplication() const{	return SendEventToApplication(fEvent);}//----------------------------------------------------------------------------------------// SendToWindow//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SendToWindow(WindowRef inWindow) const{	return SendEventToWindow(fEvent, inWindow);}//----------------------------------------------------------------------------------------// SendToControl//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SendToControl(ControlRef inControl) const{	return SendEventToControl(fEvent, inControl);}//----------------------------------------------------------------------------------------// SendToMenu//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SendToMenu(MenuRef inMenu){	return SendEventToMenu(fEvent, inMenu);}//----------------------------------------------------------------------------------------// SendToUserFocus//----------------------------------------------------------------------------------------OSStatus CCarbonEvent_AC::SendToUserFocus() const{	return SendEventToUserFocus(fEvent);}//========================================================================================// CKeyboardEvent_AC//========================================================================================//----------------------------------------------------------------------------------------// ConvertToEventRecord//----------------------------------------------------------------------------------------bool CKeyboardEvent_AC::ConvertToEventRecord(EventRecord& outEvent) const// ConvertToEventRecord converts a keyboard Carbon Event to an event record. // The toolbox doesn't support converting key events to event records, but// we can do it ourselves. //// outEvent:	The output event record.// return:		Whether the event was successfully converted. //// Call?		As needed. {	bool converted = CCarbonEvent_AC::ConvertToEventRecord(outEvent);	if (!converted)	{		UInt32 eventKind = GetKind();		switch (eventKind)		{			case kEventRawKeyDown:				outEvent.what = keyDown;				break;			case kEventRawKeyRepeat:				outEvent.what = autoKey;				break;			case kEventRawKeyUp:				outEvent.what = keyUp;				break;			case kEventRawKeyModifiersChanged:			default:				outEvent.what = nullEvent;				break;		}		UInt32 keyCode = GetKeyCode();		unsigned char charCode = GetKeyMacCharCodes();		outEvent.message = (keyCode << 8) + charCode;		outEvent.when = static_cast<UInt32>(GetTime() * 60);		outEvent.where.v = outEvent.where.h = 0;		outEvent.modifiers = GetKeyModifiers();			converted = true;	}	return converted;}#endif // qCarbonEvents//----------------------------------------------------------------------------------------// End of CCarbonEvent_AC.cpp
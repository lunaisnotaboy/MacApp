// DateTimeUtils_AC.h// Copyright © 1998-2001 by Apple Computer, Inc. All rights reserved. /*	Change History		03/01/01	Cosmetic. 		01/02/01	Update copyright.		12/08/99	Changed default ctor for CLongDateRec_AC so that it can now be					constructed from CDateTime_AC (for the sake of compilers without					long long).		06/16/99	Add more constructors and assignment operators. Make more functions					inline. Add CLongDateRec_AC as a synonym for CLongDate_AC. 		03/24/99	Update copyright notice.		10/06/98	Clean up includes. */#ifndef __DateTimeUtils_AC__#define __DateTimeUtils_AC__// ACS#ifndef __CPascalString_AC__#include "CPascalString_AC.h"#endif// Toolbox#ifndef __DATETIMEUTILS__#include <DateTimeUtils.h>#endif//----------------------------------------------------------------------------------------// CDateTime_AC//----------------------------------------------------------------------------------------class CDateTime_AC : public DateTimeRec// CDateTime_AC wraps the toolbox DateTimeRec structure. It is not Y2040 safe; the long// date/time classes are better. Yes, your code really will be around 40 years from now. {public:	CDateTime_AC(unsigned long secs);	CDateTime_AC(const DateTimeRec& d)	{ *static_cast<DateTimeRec*>(this) = d; }	void			SetSeconds(unsigned long secs);	unsigned long	GetSeconds() const;	void			GetCurrentTime();	void			SetCurrentTime();	// Get and set the current clock time.	void			Clear();	// Clear the date field.};//----------------------------------------------------------------------------------------// CLongDateRec_AC//----------------------------------------------------------------------------------------class CLongDateRec_AC// CLongDateRec_AC formerly derived from the union LongDateRec. That was not allowed by // the language standard. Now the class has a LongDateRec data member. {public:		CLongDateRec_AC(const CDateTime_AC& shortDate = CDateTime_AC(0));	CLongDateRec_AC(const LongDateRec& otherDate) : fLDR(otherDate)	{ }	CLongDateRec_AC(const DateTimeRec& dateTime)	{ SetDateTime(dateTime); }	CLongDateRec_AC(const LongDateTime& longSecs)	{ SetSeconds(longSecs); }	CLongDateRec_AC(const CStr255_AC& dateString)	{ Clear(); SetDate(dateString); }	CLongDateRec_AC(Ptr textPtr, long& textLen)	{ Clear(); SetDate(textPtr, textLen); }	// Construct long date from seconds, date or time string, or date or time text buffer.		CLongDateRec_AC&	operator=(const LongDateRec& otherDate)	{ fLDR = otherDate; return *this; }	CLongDateRec_AC&	operator=(const DateTimeRec& dateTime)	{ SetDateTime(dateTime); return *this; }	CLongDateRec_AC&	operator=(const LongDateTime& longSecs)	{ SetSeconds(longSecs); return *this; }	CLongDateRec_AC&	operator=(const CStr255_AC& dateString)	{ SetDate(dateString); return *this; }	void				SetDateTime(const DateTimeRec& dateTime);	void				SetSeconds(const LongDateTime& longSecs);	StringToDateStatus	SetDate(const CStr255_AC& dateString);	StringToDateStatus	SetDate(Ptr textPtr, long& textLen);	// Set long date from seconds, date string, or date text buffer.	StringToDateStatus	SetTime(const CStr255_AC& timeString);	StringToDateStatus	SetTime(Ptr textPtr, long& textLen);	// Set long date from seconds, time string, or time text buffer.		void				Clear();	// Clear the date field.		LongDateTime		GetSeconds() const;		LongDateTime		Validate(short& result, long flags = dateStdMask) const;	// Convert to LongDateTime		short				operator[](LongDateField fieldIndex) const							{ return fLDR.list[fieldIndex]; }	// Allow access to date components using the various field enums defined in DateTimeUtils.h.	short&				operator[](LongDateField fieldIndex)							{ return fLDR.list[fieldIndex]; }	// Allow mutation of date components using the various field enums defined in DateTimeUtils.h.	operator			LongDateRec&() { return fLDR; };	operator const		LongDateRec&() const { return fLDR; };		operator			LongDateRec*() { return &fLDR; };	operator const		LongDateRec*() const { return &fLDR; };	LongDateRec&		GetLongDateRec() { return fLDR; };	static DateCachePtr	GetCache();protected:			static DateCacheRecord	sCache;		LongDateRec				fLDR;};//----------------------------------------------------------------------------------------// CLongDate_AC//----------------------------------------------------------------------------------------typedef CLongDateRec_AC CLongDate_AC;// Synonym. //----------------------------------------------------------------------------------------// CLongTime_AC//----------------------------------------------------------------------------------------class CLongTime_AC : public CLongDateRec_AC// This class differs from CLongDate in that a time string or time text buffer// can be used to construct the date time, instead of a date string or date text buffer.{public:	CLongTime_AC() : CLongDateRec_AC()	{ }	CLongTime_AC(const LongDateRec& otherDate) : CLongDateRec_AC(otherDate)	{ }	CLongTime_AC(const DateTimeRec& dateTime) : CLongDateRec_AC(dateTime)	{ }	CLongTime_AC(const LongDateTime& longSecs) : CLongDateRec_AC(longSecs)	{ }	CLongTime_AC(const CStr255_AC& timeString)	{ SetTime(timeString); }	CLongTime_AC(Ptr textPtr, long& textLen)	{ SetTime(textPtr, textLen); }	CLongTime_AC&		operator=(const LongDateRec& otherDate)	{ fLDR = otherDate; return *this; }	CLongTime_AC&		operator=(const DateTimeRec& dateTime)	{ SetDateTime(dateTime); return *this; }	CLongTime_AC&		operator=(const LongDateTime& longSecs)	{ SetSeconds(longSecs); return *this; }	CLongTime_AC&		operator=(const CStr255_AC& timeString)	{ SetTime(timeString); return *this; }};//----------------------------------------------------------------------------------------// CLongDateTime_AC//----------------------------------------------------------------------------------------class CLongDateTime_AC{public:	CLongDateTime_AC(const LongDateTime& ldt) : fDateTime(ldt)	{ }	CLongDateTime_AC(const CLongDateRec_AC& ldc);	CLongDateTime_AC(const DateTimeRec& dateTime);		CStr255_AC		GetDate(DateForm form = shortDate) const;	CStr255_AC		GetTime(bool wantSeconds = true) const;		void			Clear();	// Clear the date field.	ToggleResults	Toggle(LongDateField 			field,							 DateDelta 				delta,							 short 					ch,							 const TogglePB *		params);	LongDateTime	fDateTime;		// Static member functions		static Handle	SetItl2Handle(Handle itsItl2Handle);		static Handle	fgItl2Handle;	// Handle to the international rsrc to be used									// in string conversions. Normally NULL, MacApp depends on									// The system value for proper operation of it's internal									// Strings.  However, you may replace and restore the									// value of this field during your own operations.};#endif // __DateTimeUtils_AC__
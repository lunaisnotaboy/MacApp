// CAECompare_AC.h// Copyright © 1999-2001 by Apple Computer, Inc. All rights reserved./*	Change History		02/14/01	Don't include Toolbox_AC. 		01/02/01	Update copyright.		03/24/99	Update copyright notice.		01/11/99	Initial version. */#ifndef __CAECompare_AC__#define __CAECompare_AC__//// qAppleEventAware indicates if the build includes AppleEvent support.//#if qAppleEventAware// ACS#ifndef __CFSSpec_AC__#include "CFSSpec_AC.h"#endif#ifndef __CRect_AC__#include "CRect_AC.h"#endif#ifndef __CRGBColor_AC__#include "CRGBColor_AC.h"#endif// Toolbox#ifndef __AEREGISTRY__#include <AERegistry.h>#endif#ifndef __APPLEEVENTS__#include <AppleEvents.h>#endif//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class CAEDesc_AC;//========================================================================================// CAECompare_AC//========================================================================================class CAECompare_AC//   The CAECompare_AC class compares AppleEvent descriptors. It is designed to be easy // to extend to support new comparison algorithms and descriptor types. Override the// Compare method to define your own comparisons, and install an instance of your// comparer at the beginning of the list so it will take precedence. Comparisons that// are not handled by your method can be delegated to the next comparer in the list. // Alternatively, you can remove the default comparer from the list and install your// own in its place. In that case, you can call the inherited Compare method for// comparisons that your method doesn't handle. {protected:	static CAECompare_AC*	fList;	CAECompare_AC*			fNext;public:	CAECompare_AC(bool atBeginning = true);		// Constructor adds the comparer to the list. 		// The default is to add the comparer at the beginning		// so it takes precedence. 	virtual ~CAECompare_AC();		// Destructor removes the comparer from the list. 	void Insert(bool atBeginning);		// Add the comparer at either the beginning 		// or the end of the list. 	void Remove();		// Removes the comparer from the list. 	static bool CompareDesc(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);		// Call this function to compare two AEDescs, starting with the first		// (most recently installed) comparer. protected:	virtual bool Compare(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow) const;		// Override to define your own comparisons. public:	static bool CompareAEList(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);	static bool CompareChar(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);	static bool CompareRect(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);	static bool CompareRGBColor(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);	static bool CompareDateTime(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);	static bool CompareLong(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);	static bool CompareWildCard(const CAEDesc_AC& desc, const CAEDesc_AC& otherDesc, DescType compareHow);};//#endif // qAppleEventAware	#endif // __CAECompare_AC__
// TemplateList_AC.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/02/01	Update copyright.		07/24/00	CW Pro 6: casting to a reference. 		04/21/00	Base the template lists on void* lists. 					Returning a reference is appropriate only in non-sorted lists. 		03/16/00	Use EXPORT_CLASS_AC for forward exports.		12/07/99	Made dtor out-of-line to serve as trigger function for MrCpp and SCpp					to force vtable.		03/24/99	Update copyright notice.		10/29/98	Include CTidyHeap_AC.h.		10/06/98	Clean up includes. */#ifndef __TemplateList_AC__#define __TemplateList_AC__// ACS#ifndef __CList_AC__#include "CList_AC.h"#endif#ifndef __CListIterator_AC__#include "CListIterator_AC.h"#endif#ifndef __CTidyHeap_AC__#include "CTidyHeap_AC.h"#endif//========================================================================================// TemplateSortedList_AC//========================================================================================template<class T>class EXPORT_AC TemplateSortedList_AC : public CSortedVoidList_AC// The TemplateSortedList_AC class implements a sorted list of pointers to objects. // There is no actual requirement that the list elements be pointers, but the template// works only for elements with pointer semantics. If the elements point to deletable// objects, you can use the FreeAll method to delete the referenced objects. The list// accessors return copies of the object pointers. //// To define a sorted list for an object class, use: ////    EXPORT_CLASS_AC(TemplateSortedList_AC<CMyClass*>)//    typedef TemplateSortedList_AC<CMyClass*> CMyClassSortedList;//// or if you want to customize the sorted list: ////    class CMyClassSortedList : public TemplateSortedList_AC<CMyClass*>//    { ... custom fields and methods ... };{public:	//------------------------------------------------------------------------------------	// procedure typedefs for parameters	//------------------------------------------------------------------------------------	typedef CompareResult_AC (*TestProc)(const T* anObject, void* yourDataPtr);	typedef CompareResult_AC (*CompareProc)(const T* object1, const T* object2,											void* yourDataPtr);	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	TemplateSortedList_AC(ArrayIndex_AC initialSize = 0) :		CSortedVoidList_AC(initialSize) { }		// Constructor	virtual ~TemplateSortedList_AC();		// Destructor	virtual void* Clone() const;		// Virtual constructor	void FreeAll();		// Frees each element in the list. Leaves fSize at 0.	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void Insert(const T* item);		// Inserts the given item into the list in sorted order, using the Compare method        // to determine the item's location relative to other items in the list.	T* operator[](ArrayIndex_AC index) const;		// Return the index'th element of the list.		// the index is "zero" based.	T* At(ArrayIndex_AC index) const;		// Return the index'th element of the list.	void Delete(const T* item);		// Finds the item and removes it from the list. 	T* First() const;		// Return the first element of the list. 	T* Last() const;		// Return the last element of the list. 	void Push(const T* item);		// LIFO stack push.	T* Pop();		// LIFO stack pop	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	ArrayIndex_AC GetIdentityItemNo(const T* item) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	ArrayIndex_AC GetEqualItemNo(const T* item) const;		// Find the first equivalent item in the list. If item does not occur, return 0.	virtual CompareResult_AC Compare(const T* object1, const T* object2) const;		// By default just compare the ordinal value of the items. 	virtual CompareResult_AC CompareVoids(const void* item1, const void* item2) const; // override		// Forward into the Compare function.	T* DoSearch(TestProc testObject, void* yourDataPtr, ArrayIndex_AC& index) const;		// Search the list using the supplied testObject function. 	ArrayIndex_AC Search(TestProc testObject, void* yourDataPtr);		// Search the list using the supplied testObject function. 	void SortBy(CompareProc compareObjects, void* yourDataPtr);		// Sorts the list using the supplied compareObjects function.	//====================================================================================	// class Iterator	//====================================================================================	class EXPORT_AC Iterator : public TemplateIterator_AC<T*>	// The TemplateSortedList_AC<T>::Iterator class provides type-safe access to	// the pointers in a list. Because the list contains pointers to objects, the	// iterator returns copies of the pointers, or NULL if the iteration has run out. 	//	// Using an iterator is simple: 	//	//    void Example(CMyClassSortedList* list)	//    {	//        for (CMyClassSortedList::Iterator iter(list); iter; ++iter)	//        {	//            // iter can be used wherever a CMyClass* would work	//            if (iter->MyMethod()) // etc.	{	public:		Iterator(const TemplateSortedList_AC<T>* inList,				 ArrayIndex_AC inLowBound,				 ArrayIndex_AC inHighBound,				 bool inForward = kIterateForward_AC) :			TemplateIterator_AC<T*>(inList, inLowBound, inHighBound, inForward)			{ }		Iterator(const TemplateSortedList_AC<T>* inList,				 bool inForward = kIterateForward_AC) :			TemplateIterator_AC<T*>(inList, inForward)			{ }	};};//========================================================================================// TemplateAutoSortedList_AC//========================================================================================template<class T>class EXPORT_AC TemplateAutoSortedList_AC : public TemplateSortedList_AC<T>// The TemplateAutoSortedList_AC class implements a TemplateSortedList_AC that// "owns" its elements and thus calls FreeAll at destructor time. //{public:	TemplateAutoSortedList_AC(ArrayIndex_AC initialSize = 0) :		TemplateSortedList_AC<T>(initialSize) { }		// Constructor	virtual ~TemplateAutoSortedList_AC() { FreeAll(); }		// Destructor};		//========================================================================================// TemplateList_AC: A dynamic list of T*'s. It IS permissible for descendants to add more// named fields//========================================================================================template<class T>class EXPORT_AC TemplateList_AC : public CVoidList_AC// The TemplateList_AC class implements a simple list of pointers. It is implemented// as a type-safe version of CVoidList_AC. {public:	//------------------------------------------------------------------------------------	// procedure typedefs for parameters	//------------------------------------------------------------------------------------	typedef CompareResult_AC (*TestProc)(const T* anObject, void* yourDataPtr);	typedef CompareResult_AC (*CompareProc)(const T* object1, const T* object2,											void* yourDataPtr);	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	TemplateList_AC(ArrayIndex_AC initialSize = 0) : CVoidList_AC(initialSize) { }		// constructor	virtual ~TemplateList_AC()	{ }		// Destructor			virtual void* Clone() const;	virtual void FreeAll();		// Frees each element in the list. Leaves fSize at 0.	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void Insert(const T* item);		// Insert the item in order. 	T* operator[](ArrayIndex_AC index) const;	T*& operator[](ArrayIndex_AC index);		// Return the index'th element of the list. The reference version allows assignment. 		// the index is "zero" based.	T* At(ArrayIndex_AC index) const;	T*& At(ArrayIndex_AC index);		// Return the index'th element of the list. The reference version allows assignment. 	void AtPut(ArrayIndex_AC index, T* newItem);		// Replace the index'th element of the list. 	void InsertBefore(ArrayIndex_AC index, T* item);		// Insert a reference to item at the indicated index. 	void InsertFirst(T* item);		// Insert a reference to item at the front of the list. 	void InsertLast(T* item);		// Insert a reference to item at the back of the list. 	void Delete(const T* item);		// Finds the item and removes it from the list. 	T* First() const;		// Return the first element of the list. 	T* Last() const;		// Return the last element of the list. 	void Push(const T* item);		// LIFO stack push.	T* Pop();		// LIFO stack pop	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	ArrayIndex_AC GetIdentityItemNo(const T* item) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	ArrayIndex_AC GetEqualItemNo(const T* item) const;		// Find the first equivalent item in the list. If item does not occur, return 0.	virtual CompareResult_AC Compare(const T* object1, const T* object2) const;		// By default just compare the ordinal value of the items. 	virtual CompareResult_AC CompareVoids(const void* item1, const void* item2) const; // override		// Forward into the Compare function.	T* DoSearch(TestProc testObject, void* yourDataPtr, ArrayIndex_AC& index) const;		// Search the list using the supplied testObject function. 	ArrayIndex_AC Search(TestProc testObject, void* yourDataPtr);		// Search the list using the supplied testObject function. 	void SortBy(CompareProc compareObjects, void* yourDataPtr);		// Sorts the list using the supplied compareObjects function.	//====================================================================================	// class Iterator	//====================================================================================	class EXPORT_AC Iterator : public TemplateIterator_AC<T*>	// The TemplateList_AC<T>::Iterator class provides type-safe access to	// the pointers in a list. Because the list contains pointers to objects, the	// iterator returns copies of the pointers, or NULL if the iteration has run out. 	{	public:		Iterator(const TemplateList_AC<T>* inList,				 ArrayIndex_AC inLowBound,				 ArrayIndex_AC inHighBound,				 bool inForward = kIterateForward_AC) :			TemplateIterator_AC<T*>(inList, inLowBound, inHighBound, inForward)			{ }		Iterator(const TemplateList_AC<T>* inList,				 bool inForward = kIterateForward_AC) :			TemplateIterator_AC<T*>(inList, inForward)			{ }	};};//========================================================================================// TemplateAutoList_AC//========================================================================================template<class T>class EXPORT_AC TemplateAutoList_AC : public TemplateList_AC<T>// The TemplateAutoList_AC class implements a TemplateList_AC that// "owns" its elements and thus calls FreeAll at destructor time. //{public:	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	TemplateAutoList_AC(ArrayIndex_AC initialSize = 0) :		TemplateList_AC<T>(initialSize) { }		// Constructor	virtual ~TemplateAutoList_AC() { FreeAll(); }		// Destructor};	//========================================================================================// CLASS TemplateSortedList_AC<T>//========================================================================================//----------------------------------------------------------------------------------------// Destructor//----------------------------------------------------------------------------------------template<class T>TemplateSortedList_AC<T>::~TemplateSortedList_AC(){}//----------------------------------------------------------------------------------------// Clone//----------------------------------------------------------------------------------------template<class T>void* TemplateSortedList_AC<T>::Clone() const{	return TH_new TemplateSortedList_AC<T>(*this);}//----------------------------------------------------------------------------------------// FreeAll//----------------------------------------------------------------------------------------template<class T>void TemplateSortedList_AC<T>::FreeAll(){	CWhileShrinking(this);	while (fSize > 0)		delete Pop();}//----------------------------------------------------------------------------------------// Insert//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedList_AC<T>::Insert(const T* item){	CSortedVoidList_AC::Insert(item);}//----------------------------------------------------------------------------------------// operator[]//----------------------------------------------------------------------------------------template<class T>inline T* TemplateSortedList_AC<T>::operator[](ArrayIndex_AC index) const{	return static_cast<T*>(CSortedVoidList_AC::operator[](index));}//----------------------------------------------------------------------------------------// At//----------------------------------------------------------------------------------------template<class T>inline T* TemplateSortedList_AC<T>::At(ArrayIndex_AC index) const{	return static_cast<T*>(CSortedVoidList_AC::At(index));}//----------------------------------------------------------------------------------------// Delete//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedList_AC<T>::Delete(const T* item){	CSortedVoidList_AC::Delete(item);}//----------------------------------------------------------------------------------------// First//----------------------------------------------------------------------------------------template<class T>inline T* TemplateSortedList_AC<T>::First() const{	return static_cast<T*>(CSortedVoidList_AC::First());}//----------------------------------------------------------------------------------------// Last//----------------------------------------------------------------------------------------template<class T>inline T* TemplateSortedList_AC<T>::Last() const{	return static_cast<T*>(CSortedVoidList_AC::Last());}//----------------------------------------------------------------------------------------// Push//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedList_AC<T>::Push(const T* item){	CSortedVoidList_AC::Push(item);}//----------------------------------------------------------------------------------------// Pop//----------------------------------------------------------------------------------------template<class T>inline T* TemplateSortedList_AC<T>::Pop(){	return static_cast<T*>(CSortedVoidList_AC::Pop());}//----------------------------------------------------------------------------------------// GetIdentityItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateSortedList_AC<T>::GetIdentityItemNo(const T* item) const{	return CSortedVoidList_AC::GetIdentityItemNo(item);}//----------------------------------------------------------------------------------------// GetEqualItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateSortedList_AC<T>::GetEqualItemNo(const T* item) const{	return CSortedVoidList_AC::GetEqualItemNo(item);}//----------------------------------------------------------------------------------------// Compare//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateSortedList_AC<T>::Compare(const T* object1, const T* object2) const{	// By default just compare the ordinal value of the items. Subclasses that want to use	// the items as pointers or other types should override if the comparison is based on	// the pointed at value	return CompareAddresses(object1, object2);}//----------------------------------------------------------------------------------------// CompareVoids//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateSortedList_AC<T>::CompareVoids(const void* item1, const void* item2) const // override {	return Compare(static_cast<const T*>(item1), static_cast<const T*>(item2));}//----------------------------------------------------------------------------------------// DoSearch//----------------------------------------------------------------------------------------template<class T>inline T* TemplateSortedList_AC<T>::DoSearch(TestProc testObject, void* yourDataPtr, ArrayIndex_AC& index) const{	return static_cast<T*>(CSortedVoidList_AC::DoSearch(reinterpret_cast<CompareVoidType>(testObject), yourDataPtr, index));}//----------------------------------------------------------------------------------------// Search//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateSortedList_AC<T>::Search(TestProc testObject, void* yourDataPtr){	return CSortedVoidList_AC::Search(reinterpret_cast<CompareVoidType>(testObject), yourDataPtr);}//----------------------------------------------------------------------------------------// SortBy//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedList_AC<T>::SortBy(CompareProc compareObjects, void* yourDataPtr){	return CSortedVoidList_AC::SortBy(compareObjects, yourDataPtr);}//========================================================================================// CLASS TemplateList_AC<T>//========================================================================================//----------------------------------------------------------------------------------------// Clone//----------------------------------------------------------------------------------------template<class T>inline void* TemplateList_AC<T>::Clone() const{	return TH_new TemplateList_AC(*this);}//----------------------------------------------------------------------------------------// FreeAll//----------------------------------------------------------------------------------------template<class T>void TemplateList_AC<T>::FreeAll(){	CWhileShrinking(this);	while (fSize > 0)		delete Pop();}//----------------------------------------------------------------------------------------// Insert//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::Insert(const T* item){	CVoidList_AC::Insert(item);}//----------------------------------------------------------------------------------------// operator[]//----------------------------------------------------------------------------------------template<class T>inline T* TemplateList_AC<T>::operator[](ArrayIndex_AC index) const{	return static_cast<T*>(CVoidList_AC::operator[](index));}template<class T>inline T*& TemplateList_AC<T>::operator[](ArrayIndex_AC index){	return reinterpret_cast<T*&>(CVoidList_AC::operator[](index));}//----------------------------------------------------------------------------------------// At//----------------------------------------------------------------------------------------template<class T>inline T* TemplateList_AC<T>::At(ArrayIndex_AC index) const{	return static_cast<T*>(CVoidList_AC::At(index));}template<class T>inline T*& TemplateList_AC<T>::At(ArrayIndex_AC index){	return reinterpret_cast<T*&>(CVoidList_AC::At(index));}//----------------------------------------------------------------------------------------// AtPut//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::AtPut(ArrayIndex_AC index, T* newItem){	CVoidList_AC::AtPut(index, newItem);}//----------------------------------------------------------------------------------------// InsertBefore//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::InsertBefore(ArrayIndex_AC index, T* item){	CVoidList_AC::InsertBefore(index, item);}//----------------------------------------------------------------------------------------// InsertFirst//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::InsertFirst(T* item){	CVoidList_AC::InsertFirst(item);}//----------------------------------------------------------------------------------------// InsertLast//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::InsertLast(T* item){	CVoidList_AC::InsertLast(item);}//----------------------------------------------------------------------------------------// Delete//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::Delete(const T* item){	CVoidList_AC::Delete(item);}//----------------------------------------------------------------------------------------// First//----------------------------------------------------------------------------------------template<class T>inline T* TemplateList_AC<T>::First() const{	return static_cast<T*>(CVoidList_AC::First());}//----------------------------------------------------------------------------------------// Last//----------------------------------------------------------------------------------------template<class T>inline T* TemplateList_AC<T>::Last() const{	return static_cast<T*>(CVoidList_AC::Last());}//----------------------------------------------------------------------------------------// Push//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::Push(const T* item){	CVoidList_AC::Push(item);}//----------------------------------------------------------------------------------------// Pop//----------------------------------------------------------------------------------------template<class T>inline T* TemplateList_AC<T>::Pop(){	return static_cast<T*>(CVoidList_AC::Pop());}//----------------------------------------------------------------------------------------// GetIdentityItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateList_AC<T>::GetIdentityItemNo(const T* item) const{	return CVoidList_AC::GetIdentityItemNo(item);}//----------------------------------------------------------------------------------------// GetEqualItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateList_AC<T>::GetEqualItemNo(const T* item) const{	return CVoidList_AC::GetEqualItemNo(item);}//----------------------------------------------------------------------------------------// Compare//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateList_AC<T>::Compare(const T* object1, const T* object2) const{	// By default just compare the ordinal value of the items. Subclasses that want to use	// the items as pointers or other types should override if the comparison is based on	// the pointed at value	return CompareAddresses(object1, object2);}//----------------------------------------------------------------------------------------// CompareVoids//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateList_AC<T>::CompareVoids(const void* item1, const void* item2) const // override {	return Compare(static_cast<const T*>(item1), static_cast<const T*>(item2));}//----------------------------------------------------------------------------------------// DoSearch//----------------------------------------------------------------------------------------template<class T>inline T* TemplateList_AC<T>::DoSearch(TestProc testObject, void* yourDataPtr, ArrayIndex_AC& index) const{	return static_cast<T*>(CVoidList_AC::DoSearch(testObject, yourDataPtr, index));}//----------------------------------------------------------------------------------------// Search//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateList_AC<T>::Search(TestProc testObject, void* yourDataPtr){	return CVoidList_AC::Search(testObject, yourDataPtr);}//----------------------------------------------------------------------------------------// SortBy//----------------------------------------------------------------------------------------template<class T>inline void TemplateList_AC<T>::SortBy(CompareProc compareObjects, void* yourDataPtr){	return CVoidList_AC::SortBy(compareObjects, yourDataPtr);}#endif // __TemplateList_AC__
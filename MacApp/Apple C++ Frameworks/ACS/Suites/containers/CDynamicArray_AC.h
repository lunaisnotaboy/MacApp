// CDynamicArray_AC.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		03/01/01	Cosmetic. 		01/02/01	Update copyright.		08/23/00	Added embedded documentation.		04/21/00	Export RandomArrayIndex_AC so it can be used in CSortedVoidList_AC. 		12/07/99	Corrected filename in banner.  Removed compiler workaround.		03/24/99	Update copyright notice.		02/22/98	friend needs class.		10/06/98	Clean up includes. */#ifndef __CDynamicArray_AC__#define __CDynamicArray_AC__// ACS#ifndef __MStreamable_AC__#include "MStreamable_AC.h"#endif// ANSI#ifndef __STDDEF__#include <stddef.h>#endif//----------------------------------------------------------------------------------------// Typedefs//----------------------------------------------------------------------------------------enum {	kIterateBackward_AC = false,	kIterateForward_AC = true};enum {	kEmptyIndex_AC = 0								// Value to use when no valid index is                                                // available. Indexes are always positive};typedef long ArrayIndex_AC;						// At least always positive ( in this                                                // universe ) !!! kEmptyIndex_AC..MaxLong                                                // would be nice//----------------------------------------------------------------------------------------// Forward and external class declarations//----------------------------------------------------------------------------------------class CArrayIterator_AC;#if qTheDebuggerclass ClassDesc;#endifenum{	kAllocationIncrement_AC = 8					// Initial Allocation increment. Eight};                                              // elements of slop shouldn't break                                                // anybody and provides a _nice_ cushion                                                // from the memory manager.//----------------------------------------------------------------------------------------// Constants for CDynamicArray_AC::CompareElements and CSortedxxx::Compare//----------------------------------------------------------------------------------------enum {	kItem1LessThanItem2_AC = -1,	kItem1EqualItem2_AC = 0,	kItem1GreaterThanItem2_AC = 1};//----------------------------------------------------------------------------------------// Constants for CSortedList_AC::Search.//----------------------------------------------------------------------------------------enum {	kItemGreaterThanCriteria_AC = -1,	kItemEqualCriteria_AC = 0,	kItemLessThanCriteria_AC = 1};//----------------------------------------------------------------------------------------// procedure typedefs for parameters//----------------------------------------------------------------------------------------typedef CompareResult_AC (*CompareIndexType_AC)(ArrayIndex_AC anItem, void* yourDataPtr);typedef CompareResult_AC (*CompareElementsType_AC)(void* element1,												   void* element2,												   void* yourDataPtr);class CStream_AC;//========================================================================================// CDynamicArray_AC//========================================================================================class EXPORT_AC CDynamicArray_AC : public MStreamable_AC// The CDynamicArray_AC class implements dynamic arrays. You can use instances of // CDynamicArray_AC directly. For example: you can create lists of nonobject data types, or// create subclasses to construct other kinds of dynamic lists.// MacApp provides several subclasses to maintain lists, sorted and unsorted, of commonly// used objects.{friend class CArrayIterator_AC;public:	typedef CArrayIterator_AC Iterator;	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	CDynamicArray_AC(ArrayIndex_AC initialSize = 0,					 short elementSize = sizeof(void*));		// Initialize a new array with initialSize elements	CDynamicArray_AC(const CDynamicArray_AC& inDynamicArray);		// Copy Constructor	virtual ~CDynamicArray_AC();		// Destructor			virtual void* Clone() const;	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFrom(CStream_AC* aStream);		virtual void WriteTo(CStream_AC* aStream) const;	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void* operator[](ArrayIndex_AC index) const;		// Returns a pointer to the index-th element in the array.		// the index is "zero" based.			void* ComputeAddress(ArrayIndex_AC index) const;		// Returns a pointer to of the index-th element in the array.		// the index is "one" based.	ArrayIndex_AC GetSize() const { return fSize; }		// Returns the ACTUAL number of elements in the array.	ArrayIndex_AC GetElementSize() const { return fElementSize; }		// Returns the size of an element.	void SetArraySize(ArrayIndex_AC theSize, bool canShrink = true);		// Sets the array allocation to handle up to theSize elements		// canShrink controls whether the fData can be shrunk (if possible) when sizing.	virtual void InsertElementInOrder(const void* elementPtr);		// Insert an element in order by arrival sequence (insert last). 	void InsertElementsBefore(ArrayIndex_AC index,							  const void* elementPtr,							  ArrayIndex_AC count);		// Insert Elements before the indicated Element. The index of the new element will        // be index. If index = 1 this inserts at the start of the array. If index = fSize        // + 1 this inserts at the end of the array. Signals Failure if unable to change        // the size of the array. !!! NOTE MULTIPLE ( >1 ) element moves for non-power of        // 2 element sizes are NOT yet supported!	void ReplaceElement(const void* elementPtr,						const void* newElement);		// Replaces an item with a new item. The two items could be the same, which is		// useful for updating the position of an item that has changed in a sorted list. 	void ReplaceElementsAt(ArrayIndex_AC index,						   const void* elementPtr,						   ArrayIndex_AC count);		// Replaces the Elements at the indicated index. !!! NOTE MULTIPLE ( >1 ) element        // moves for non-power of 2 element sizes are NOT yet supported!	void DeleteElement(const void* elementPtr);		// Find the item and delete it. 	void AtDelete(ArrayIndex_AC index) { DeleteElementsAt(index, 1); }		// Deletes via an index.	void DeleteElementsAt(ArrayIndex_AC index,						  ArrayIndex_AC count);		// Deletes the Element at the indicated index. Compresses the array !!! NOTE        // MULTIPLE ( >1 ) element moves for non-power of 2 element sizes are NOT yet        // supported!	void GetElement(ArrayIndex_AC index,					void* elementPtr) const;		// copies one element to the location specified by ptr. 	void GetElementsAt(ArrayIndex_AC index,					   void* elementPtr,					   ArrayIndex_AC count) const;		// copies count elements to the location specified by ptr. !!! NOTE MULTIPLE ( >1        // ) element moves for non-power of 2 element sizes are NOT yet supported!	void DeleteAll();		// Delete every element from the list Leave fSize at 0.			void PushElement(const void* elementPtr) { InsertElementsBefore(fSize + 1, elementPtr, 1); }		// LIFO stack push.	void PopElement(void* elementPtr);		// LIFO stack pop	//------------------------------------------------------------------------------------	// Misc. functions	//------------------------------------------------------------------------------------	bool IsEmpty() const { return fSize == kEmptyIndex_AC; }		// Test if this array is empty or not.	void Merge(const CDynamicArray_AC* aDynamicArray);		// merges aDynamicArray with itself. leaves aDynamicArray unchanged. !!! NOTE        // MULTIPLE ( >1 ) element moves for non-power of 2 element sizes are NOT yet        // supported!	ArrayIndex_AC GetAllocationIncrement() const { return fAllocationIncrement; }	void SetAllocationIncrement(ArrayIndex_AC inNewIncrement)  { fAllocationIncrement = inNewIncrement; }	//------------------------------------------------------------------------------------	// Public Sorting access	//------------------------------------------------------------------------------------	virtual bool DoSearchElement(CompareIndexType_AC testItem,								 void* yourDataPtr,								 ArrayIndex_AC& index) const; 		// Performs a linear search for an element for which testItem returns true.         // "index" returns the position of a found item, or where it should be         // inserted if not found	virtual ArrayIndex_AC GetIdentityElementNo(const void* elementPtr) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	virtual ArrayIndex_AC GetEqualElementNo(const void* elementPtr) const;		// Find the first equivalent item in the list. If item does not occur, return 0.	bool IdentityElements(const void* element1, const void* element2) const;		// Returns whether the two elements are identical. 	virtual CompareResult_AC CompareElements(const void* element1, const void* element2) const;		// Returns the sort order between elements of the array. Default considers		// all elements equivalent.	virtual void Sort();		// Sorts the list usingthe Compare and SortBy methods.	virtual void SortElementsBy(CompareElementsType_AC CompareItems, void* yourDataPtr);		// Sorts the list using the supplied CompareItems function.	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------protected:	void* GetSwapBuffer();		// Returns a buffer to use in element exchanges	void ReleaseSwapBuffer(void* swapBuffer);		// Releases a buffer returned by GetSwapBuffer	void SwapElements(void* element1, void* element2, void* swapBuffer);		// Swaps two elements using the supplied swapbuffer	void QuickSort(ArrayIndex_AC beginIndex,								  ArrayIndex_AC endIndex,								  CompareElementsType_AC CompareItems,								  void* yourDataPtr,								  void* swapBuffer);		// Uses QuickSort Algorithm, adapted from:		//	Cormen, Leiserson, and Rivest, _Introductions_To_Algorithms_, pp 153-167.	ArrayIndex_AC QSPartition(ArrayIndex_AC beginIndex,										ArrayIndex_AC endIndex,										CompareElementsType_AC CompareItems,										void* yourDataPtr,										void* swapBuffer);		// Called by RandomPartition. Partitions the array A[beginIndex..endIndex] in place	ArrayIndex_AC QSRandomPartition(ArrayIndex_AC beginIndex,										ArrayIndex_AC endIndex,										CompareElementsType_AC CompareItems,										void* yourDataPtr,										void* swapBuffer);		// Called by QuickSort. Partitions the array A[beginIndex..endIndex] in place	//------------------------------------------------------------------------------------	// Iteration	//------------------------------------------------------------------------------------  public:	CArrayIterator_AC* InstallIterator(CArrayIterator_AC* iterator);		// Adds an iterator to the list. Returns the next iterator. 	void RemoveIterator(CArrayIterator_AC* iterator);		// Removes an iterator from the list.   protected:	void Deleted(ArrayIndex_AC theIndex, ArrayIndex_AC theCount) const;		// Bends the index in the Iteration node to account for the deleted elements	void Inserted(ArrayIndex_AC theIndex, ArrayIndex_AC theCount) const;		// Bends the index to account for the inserted elements 	void AbortIteration() const;		// Called when iterations are in progress and the list is deleted	//------------------------------------------------------------------------------------	// Range Checking	//------------------------------------------------------------------------------------#if qRangeCheck	CError_AC RangeCheck(ArrayIndex_AC index, const char* debugString = NULL) const;		// Range check an index for accessing; valid range is (1..fSize). 	CError_AC RangeCheckInsert(ArrayIndex_AC index, const char* debugString = NULL) const;		// Range check an index for inserting; valid range is (1..fSize+1). 	void ThrowIfRangeCheck(ArrayIndex_AC index, const char* debugString = NULL) const;		// Range check an index for accessing; valid range is (1..fSize). 		// Throw an error if the index is out of range. 	void ThrowIfRangeCheckInsert(ArrayIndex_AC index, const char* debugString = NULL) const;		// Range check an index for inserting; valid range is (1..fSize+1). 		// Throw an error if the index is out of range. #else	CError_AC RangeCheck(ArrayIndex_AC /*index*/, const char* /*debugString*/ = NULL) const { return noErr; }	CError_AC RangeCheckInsert(ArrayIndex_AC /*index*/, const char* /*debugString*/ = NULL) const { return noErr; }	void ThrowIfRangeCheck(ArrayIndex_AC /*index*/, const char* /*debugString*/ = NULL) const {}	void ThrowIfRangeCheckInsert(ArrayIndex_AC /*index*/, const char* /*debugString*/ = NULL) const {}#endifpublic:	//====================================================================================	// CWhileShrinking	//====================================================================================	class EXPORT_AC CWhileShrinking	// CWhileShrinking temporarily sets an array's allocation increment to be the same 	// as its size. This improves performance when deleting lots of elements because it	// prevents the array space from being reallocated. At the end, the CWhileShrinking	// destructor restores the allocation increment and trims the array size. 	{	public:		CWhileShrinking(CDynamicArray_AC* array);		~CWhileShrinking();	protected:		CDynamicArray_AC*	fArray;		ArrayIndex_AC		fSaveIncrement;	};	//====================================================================================	// CWhileInserting	//====================================================================================	class EXPORT_AC CWhileInserting	// CWhileInserting temporarily sets an array's allocation increment to a different	// value -- probably larger. This improves performance when inserting lots of elements	// because it reduces the number of times the array space may be reallocated. At the end,	// the CWhileInserting destructor restores the allocation increment and trims the	// array size. 	{	public:		CWhileInserting(CDynamicArray_AC* array, ArrayIndex_AC increment);		~CWhileInserting();	protected:		CDynamicArray_AC*	fArray;		ArrayIndex_AC		fSaveIncrement;	};	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------#if qBackwardCompatiblepublic:#elseprotected:#endif	Ptr fArraySpace;							// space for actually storing the elements												// of this CDynamicArray_AC.			CArrayIterator_AC* fIteratorPtr;			// pointer to the Iterators in use	ArrayIndex_AC fSize;						// number of elements ACTUALLY in the                                                // array, from 0 to the limit of memory	ArrayIndex_AC fAllocationIncrement;			// Number of elements by which to increase                                                // or decrease the allocated size of the                                                // array when it needs to grow or shrink.                                                // Thus reducing memory manager                                                // aggravation.	ArrayIndex_AC fAllocatedSize;				// Number of elements for which storage is                                                // ALLOCATED	unsigned short fElementSize;				// Size in bytes of an element. MUST be a                                                // power of 2 ie. 1, 2, 4, 8, 16, etc.	unsigned short fElementSizeShift;			// the power of 2 for the element size.                                                // Will be used to avoid DIV and MUL};//========================================================================================// CSortedDynamicArray_AC: A CDynamicArray_AC with ordering.//========================================================================================class EXPORT_AC CSortedDynamicArray_AC : public CDynamicArray_AC// The CSortedDynamicArray_AC class implements a dynamic array whose elements can be// ordered according to criteria you specify. The sort criteria are defined by your // override of CSortedDynamicArray_AC's Compare method.// This class also defines methods for inserting an element in sorted order and// for searching for an element that meets the criteria you specify.{public:	CSortedDynamicArray_AC(ArrayIndex_AC initialSize = 0, short elementSize = sizeof(void*)) : CDynamicArray_AC(initialSize, elementSize) { }		// Initialize a new array with initialSize elements	virtual ~CSortedDynamicArray_AC();		// Destructor			virtual void* Clone() const;	virtual void InsertElementInOrder(const void* elementPtr);		// Insert an element in its correct position according to the ordering defined by        // the Compare method	virtual bool DoSearchElement(CompareIndexType_AC testItem,								 void* yourDataPtr,								 ArrayIndex_AC& index) const; // override		// Performs a binary search for an element for which testItem returns true.         // "index" returns the position of a found item, or where it should be         // inserted if not found	virtual ArrayIndex_AC GetIdentityElementNo(const void* elementPtr) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0. Overridden to do a binary search using GetEqualItemNo. 	virtual ArrayIndex_AC GetEqualElementNo(const void* elementPtr) const;		// Find the first equivalent item in the list. If item does not occur, return 0.protected:	//====================================================================================	// class Searcher	//====================================================================================	class Searcher	{	public:		Searcher(const void*& elementPtr,				 const CSortedDynamicArray_AC* theDynamicArray) :			fElementPtr(elementPtr),			fDynamicArray(theDynamicArray)		{ }		bool DoSearchElement(ArrayIndex_AC& index) { return fDynamicArray->DoSearchElement(TestItem, this, index); }	protected:		static CompareResult_AC TestItem(ArrayIndex_AC anItem,										 void* yourDataPtr);		const void*& fElementPtr;		const CSortedDynamicArray_AC* fDynamicArray;	};};//========================================================================================// GLOBAL Procedures//========================================================================================EXPORT_AC ArrayIndex_AC RandomArrayIndex_AC(ArrayIndex_AC beginIndex,											ArrayIndex_AC endIndex);#endif // __CDynamicArray_AC__/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/09/96	Removed UStreamable to a separate file from UObject		06/06/96	Added CDynamicArray_AC::Get/SetAllocationIncrement		04/30/96	Make all constructors use initialization lists if they weren't already							doing do. Switch several more empty constructors to inlines.		 4/15/96	Removed CDynamicArray_AC::Free/fFreeRequested, TDynamicArrays now tell their iterators		 					to abort if they are freed during iteration.		----- R10 -----		 3/29/96	made element parameter to InsertElementsBefor/ReplaceElementsAt const		 3/27/96	Made CDynamicArray_AC::ComputeAddress non-inline		 3/27/96	Made CDynamicArray_AC primitives non-virtual and inline where possible.		 3/01/96	Re-arranged data members of CList_AC, natural alignment results in 		 					reduced storage size.		12/19/95	1222953 (Optionally) Used new/delete for TDynamicarray store. Removed Lock method.		11/28/95	Use <> for including stddef. 		----- 3.3 -----		----- 3.3b1 -----		  7/5/95	Cosmetic. 		 3/13/95	Change Free methods to destructors. 		02/08/95	replace usage of staticLink with yourDataPtr. 		07/29/92	Add the Lock method that was in TObject to CDynamicArray_AC*/
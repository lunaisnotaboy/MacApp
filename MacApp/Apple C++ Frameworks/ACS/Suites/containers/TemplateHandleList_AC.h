// TemplateHandleList_AC.h// Copyright й 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/02/01	Update copyright.		08/09/00	Moved include of CTidyHeap_AC.h into header to support					TH_new in inlines.		07/24/00	CW Pro 6: casting to a reference. 		04/21/00	Base the template lists on Handle lists. 		12/07/99	Include CHandle_AC.h for VerboseIsHandle_AC.  Qualify templates					in TemplateScalarIterator_AC and TemplateHandleList_AC.		03/24/99	Update copyright notice.		10/06/98	Clean up includes. */#ifndef __TemplateHandleList_AC__#define __TemplateHandleList_AC__// ACS#ifndef __CHandleList_AC__#include "CHandleList_AC.h"#endif#ifndef __CListIterator_AC__#include "CListIterator_AC.h"#endif#ifndef __CHandle_AC__#include "CHandle_AC.h"#endif#ifndef __CTidyHeap_AC__#include "CTidyHeap_AC.h"#endif//========================================================================================// TemplateSortedHandleList_AC: A dynamic list of Handles. It IS permissible for descendants to add more// named fields//========================================================================================template<class T>class EXPORT_AC TemplateSortedHandleList_AC : public CSortedHandleListBase_AC// The TemplateSortedHandleList_AC class implements a dynamic list of handles// sorted by ordinal value. This class includes methods for using// your criteria to insert, delete, and access elements. In addition,// iterator functions are available for performing operations on// all or a subset of the elements in the list.//// There is no actual requirement that the list elements be handles, but the template// works only for elements with handle semantics. If the elements point to deletable// handles, you can use the FreeAll method to delete them. The list accessors return// copies of the handles. //// You will need to define a Dispose function that uses the appropriate call to// dispose of your handle type. {public:	//------------------------------------------------------------------------------------	// procedure typedefs for parameters	//------------------------------------------------------------------------------------	typedef CompareResult_AC (*TestProc)(const T anItem, void* yourDataPtr);	typedef CompareResult_AC (*CompareProc)(const T item1, const T item2, void* yourDataPtr);	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	TemplateSortedHandleList_AC(ArrayIndex_AC initialSize = 0) :		CSortedHandleListBase_AC(initialSize) { }		// Constructor	virtual ~TemplateSortedHandleList_AC() { };		// Destructor			virtual void* Clone() const;	void FreeAll();		// Frees each element in the list. Leaves fSize at 0.	void FreeList();		// Frees each handle in the list, then frees the list.		//еее This method will be removed. Please call FreeAll and then if necessary free the object	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void Insert(const T item);		// Inserts the given item into the list in sorted order, using the Compare method        // to determine the item's location relative to other items in the list.	T operator[](ArrayIndex_AC index) const;		// Return the index'th element of the list.		// the index is "zero" based.	T At(ArrayIndex_AC index) const;		// Return the index'th element of the list.	void Delete(const T item);		// Finds the handle and removes it from the list. 	T First() const;		// Return the first element of the list. 	T Last() const;		// Return the last element of the list. 	void Push(const T item);		// LIFO stack push.	T Pop();		// LIFO stack pop	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	ArrayIndex_AC GetIdentityItemNo(const T item) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	ArrayIndex_AC GetEqualItemNo(const T item) const;		// Find the first reference to item in the list. If item does not occur, return 0.	virtual CompareResult_AC Compare(const T item1, const T item2) const;		// By default just compare the ordinal value of the items. Subclasses that want to        // use the items as pointers or other types should override if the comparison is        // based on the pointed at value	virtual CompareResult_AC CompareHandles(const Handle item1, const Handle item2) const; // override		// Forward into the Compare function.	T DoSearch(TestProc testHandle, void* yourDataPtr, ArrayIndex_AC& index) const;		// Search the list using the supplied testHandle function. 	ArrayIndex_AC Search(TestProc testHandle, void* yourDataPtr);		// Search the list using the supplied testHandle function. 	void SortBy(CompareProc compareHandles, void* yourDataPtr);		// Sorts the list using the supplied compareHandles function.	//====================================================================================	// class Iterator	//====================================================================================	class EXPORT_AC Iterator : public TemplateScalarIterator_AC<T>	{	public:		Iterator(const TemplateSortedHandleList_AC<T>* inList,				 ArrayIndex_AC inLowBound,				 ArrayIndex_AC inHighBound,				 bool inForward = kIterateForward_AC)			:	TemplateScalarIterator_AC<T>(inList, inLowBound, inHighBound, inForward)			{ }		Iterator(const TemplateSortedHandleList_AC<T>* inList,				 bool inForward = kIterateForward_AC)			:	TemplateScalarIterator_AC<T>(inList, inForward)			{ }	};};//========================================================================================// TemplateHandleList_AC: A dynamic list of Handles. It IS permissible for descendants to add more// named fields//========================================================================================template<class T>class EXPORT_AC TemplateHandleList_AC : public CHandleListBase_AC{public:	//------------------------------------------------------------------------------------	// procedure typedefs for parameters	//------------------------------------------------------------------------------------	typedef CompareResult_AC (*TestProc)(const T anItem, void* yourDataPtr);	typedef CompareResult_AC (*CompareProc)(const T item1, const T item2, void* yourDataPtr);	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	TemplateHandleList_AC(ArrayIndex_AC initialSize = 0) :		CHandleListBase_AC(initialSize) { }		// constructor	virtual ~TemplateHandleList_AC() { };		// Destructor			virtual void* Clone() const;	void FreeAll();		// Frees each element in the list. Leaves fSize at 0.	void FreeList();		// Frees each handle in the list, then frees the list.		//еее This method will be removed. Please call FreeAll and then if necessary free the object	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void Insert(const T item);		// Inserts the given item into the list in sorted order, using the Compare method        // to determine the item's location relative to other items in the list.	T operator[](ArrayIndex_AC index) const;	T& operator[](ArrayIndex_AC index);		// Return the index'th element of the list. The reference version allows assignment. 		// the index is "zero" based.	T At(ArrayIndex_AC index) const;	T& At(ArrayIndex_AC index);		// Return the index'th element of the list. The reference version allows assignment. 	void AtPut(ArrayIndex_AC index, T newItem);		// Replace the index'th element of the list. 	void InsertBefore(ArrayIndex_AC index, T item);		// Insert a reference to item at the indicated index. 	void InsertFirst(T item);		// Insert a reference to item at the front of the list. 	void InsertLast(T item);		// Insert a reference to item at the back of the list. 	void Delete(const T item);		// Finds the item and removes it from the list. 	T First() const;		// Return the first element of the list. 	T Last() const;		// Return the last element of the list. 	void Push(const T item);		// LIFO stack push.	T Pop();		// LIFO stack pop	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	ArrayIndex_AC GetIdentityItemNo(const T item) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	ArrayIndex_AC GetEqualItemNo(const T item) const;		// Find the first equivalent item in the list. If item does not occur, return 0.	virtual CompareResult_AC Compare(const T item1, const T item2) const;		// By default just compare the ordinal value of the items. 	virtual CompareResult_AC CompareHandles(const Handle item1, const Handle item2) const; // override		// Forward into the Compare function.	T DoSearch(TestProc testHandle, void* yourDataPtr, ArrayIndex_AC& index) const;		// Search the list using the supplied testHandle function. 	ArrayIndex_AC Search(TestProc testHandle, void* yourDataPtr);		// Search the list using the supplied testHandle function. 	void SortBy(CompareProc compareHandles, void* yourDataPtr);		// Sorts the list using the supplied compareHandles function.	//====================================================================================	// class Iterator	//====================================================================================	class EXPORT_AC Iterator : public TemplateScalarIterator_AC<T>	{	public:		Iterator(const TemplateHandleList_AC<T>* inList,				 ArrayIndex_AC inLowBound,				 ArrayIndex_AC inHighBound,				 bool inForward = kIterateForward_AC)			:	TemplateScalarIterator_AC<T>(inList, inLowBound, inHighBound, inForward)			{ }		Iterator(const TemplateHandleList_AC<T>* inList,				 bool inForward = kIterateForward_AC)			:	TemplateScalarIterator_AC<T>(inList, inForward)			{ }	};};//========================================================================================// Inline Implementation//========================================================================================//========================================================================================// CLASS TemplateSortedHandleList_AC//========================================================================================//----------------------------------------------------------------------------------------// Clone//----------------------------------------------------------------------------------------template<class T>void* TemplateSortedHandleList_AC<T>::Clone() const{	return TH_new TemplateSortedHandleList_AC<T>(*this);}//----------------------------------------------------------------------------------------// FreeAll//----------------------------------------------------------------------------------------template<class T>void TemplateSortedHandleList_AC<T>::FreeAll(){	CWhileShrinking(this);	while (fSize > 0)		Dispose(Pop());}//----------------------------------------------------------------------------------------// FreeList//----------------------------------------------------------------------------------------template<class T>void TemplateSortedHandleList_AC<T>::FreeList(){#if qCoreDebug	::ProgramBreak_AC("TemplateSortedHandleList_AC::FreeList is deprecated. Please call FreeAll and then if necessary free the object");#endif	FreeAll();	delete this;}//----------------------------------------------------------------------------------------// Insert//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedHandleList_AC<T>::Insert(const T item){	CSortedHandleListBase_AC::Insert(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// operator[]//----------------------------------------------------------------------------------------template<class T>inline T TemplateSortedHandleList_AC<T>::operator[](ArrayIndex_AC index) const{	return reinterpret_cast<T>(CSortedHandleListBase_AC::operator[](index));}//----------------------------------------------------------------------------------------// At//----------------------------------------------------------------------------------------template<class T>inline T TemplateSortedHandleList_AC<T>::At(ArrayIndex_AC index) const{	return reinterpret_cast<T>(CSortedHandleListBase_AC::At(index));}//----------------------------------------------------------------------------------------// Delete//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedHandleList_AC<T>::Delete(const T item){	CSortedHandleListBase_AC::Delete(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// First//----------------------------------------------------------------------------------------template<class T>inline T TemplateSortedHandleList_AC<T>::First() const{	return reinterpret_cast<T>(CSortedHandleListBase_AC::First());}//----------------------------------------------------------------------------------------// Last//----------------------------------------------------------------------------------------template<class T>inline T TemplateSortedHandleList_AC<T>::Last() const{	return reinterpret_cast<T>(CSortedHandleListBase_AC::Last());}//----------------------------------------------------------------------------------------// Push//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedHandleList_AC<T>::Push(const T item){	CSortedHandleListBase_AC::Push(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// Pop//----------------------------------------------------------------------------------------template<class T>inline T TemplateSortedHandleList_AC<T>::Pop(){	return reinterpret_cast<T>(CSortedHandleListBase_AC::Pop());}//----------------------------------------------------------------------------------------// GetIdentityItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateSortedHandleList_AC<T>::GetIdentityItemNo(const T item) const{	return CSortedHandleListBase_AC::GetIdentityItemNo(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// GetEqualItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateSortedHandleList_AC<T>::GetEqualItemNo(const T item) const{	return CSortedHandleListBase_AC::GetEqualItemNo(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// Compare//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateSortedHandleList_AC<T>::Compare(const T item1, const T item2) const{	// By default just compare the ordinal value of the items. Subclasses that want to use	// the items as pointers or other types should override if the comparison is based on	// the pointed at value	return CompareAddresses(item1, item2);}//----------------------------------------------------------------------------------------// CompareHandles//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateSortedHandleList_AC<T>::CompareHandles(const Handle item1, const Handle item2) const // override{	return Compare(reinterpret_cast<T>(item1), reinterpret_cast<T>(item2));}//----------------------------------------------------------------------------------------// DoSearch//----------------------------------------------------------------------------------------template<class T>inline T TemplateSortedHandleList_AC<T>::DoSearch(TestProc testHandle, void* yourDataPtr, ArrayIndex_AC& index) const{	return reinterpret_cast<T>(CSortedHandleListBase_AC::DoSearch(reinterpret_cast<CompareHandleType>(testHandle), yourDataPtr, index));}//----------------------------------------------------------------------------------------// Search//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateSortedHandleList_AC<T>::Search(TestProc testHandle, void* yourDataPtr){	return CSortedHandleListBase_AC::Search(reinterpret_cast<CompareHandleType>(testHandle), yourDataPtr);}//----------------------------------------------------------------------------------------// SortBy//----------------------------------------------------------------------------------------template<class T>inline void TemplateSortedHandleList_AC<T>::SortBy(CompareProc compareHandles, void* yourDataPtr){	return CSortedHandleListBase_AC::SortBy(compareHandles, yourDataPtr);}//========================================================================================// CLASS TemplateHandleList_AC//========================================================================================//----------------------------------------------------------------------------------------// Clone//----------------------------------------------------------------------------------------template<class T>void* TemplateHandleList_AC<T>::Clone() const{	return TH_new TemplateHandleList_AC<T>(*this);}//----------------------------------------------------------------------------------------// FreeAll//----------------------------------------------------------------------------------------template<class T>void TemplateHandleList_AC<T>::FreeAll(){	CWhileShrinking(this);	while (fSize > 0)		delete Pop();}//----------------------------------------------------------------------------------------// FreeList//----------------------------------------------------------------------------------------template<class T>void TemplateHandleList_AC<T>::FreeList(){#if qCoreDebug	::ProgramBreak_AC("TemplateSortedHandleList_AC::FreeList is deprecated. Please call FreeAll and then if necessary free the object");#endif	FreeAll();	delete this;}//----------------------------------------------------------------------------------------// Insert//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::Insert(const T item){	CHandleListBase_AC::Insert(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// operator[]//----------------------------------------------------------------------------------------template<class T>inline T TemplateHandleList_AC<T>::operator[](ArrayIndex_AC index) const{	return static_cast<T>(CHandleListBase_AC::operator[](index));}template<class T>inline T& TemplateHandleList_AC<T>::operator[](ArrayIndex_AC index){	return reinterpret_cast<T&>(CHandleListBase_AC::operator[](index));}//----------------------------------------------------------------------------------------// At//----------------------------------------------------------------------------------------template<class T>inline T TemplateHandleList_AC<T>::At(ArrayIndex_AC index) const{	return static_cast<T>(CHandleListBase_AC::At(index));}template<class T>inline T& TemplateHandleList_AC<T>::At(ArrayIndex_AC index){	return reinterpret_cast<T&>(CHandleListBase_AC::At(index));}//----------------------------------------------------------------------------------------// AtPut//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::AtPut(ArrayIndex_AC index, T newItem){	CHandleListBase_AC::AtPut(index, newItem);}//----------------------------------------------------------------------------------------// InsertBefore//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::InsertBefore(ArrayIndex_AC index, T item){	CHandleListBase_AC::InsertBefore(index, reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// InsertFirst//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::InsertFirst(T item){	CHandleListBase_AC::InsertFirst(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// InsertLast//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::InsertLast(T item){	CHandleListBase_AC::InsertLast(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// Delete//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::Delete(const T item){	CHandleListBase_AC::Delete(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// First//----------------------------------------------------------------------------------------template<class T>inline T TemplateHandleList_AC<T>::First() const{	return reinterpret_cast<T>(CHandleListBase_AC::First());}//----------------------------------------------------------------------------------------// Last//----------------------------------------------------------------------------------------template<class T>inline T TemplateHandleList_AC<T>::Last() const{	return reinterpret_cast<T>(CHandleListBase_AC::Last());}//----------------------------------------------------------------------------------------// Push//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::Push(const T item){	CHandleListBase_AC::Push(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// Pop//----------------------------------------------------------------------------------------template<class T>inline T TemplateHandleList_AC<T>::Pop(){	return reinterpret_cast<T>(CHandleListBase_AC::Pop());}//----------------------------------------------------------------------------------------// GetIdentityItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateHandleList_AC<T>::GetIdentityItemNo(const T item) const{	return CHandleListBase_AC::GetIdentityItemNo(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// GetEqualItemNo//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateHandleList_AC<T>::GetEqualItemNo(const T item) const{	return CHandleListBase_AC::GetEqualItemNo(reinterpret_cast<Handle>(item));}//----------------------------------------------------------------------------------------// Compare//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateHandleList_AC<T>::Compare(const T item1, const T item2) const{	// By default just compare the ordinal value of the items. Subclasses that want to use	// the items as pointers or other types should override if the comparison is based on	// the pointed at value	return CompareAddresses(item1, item2);}//----------------------------------------------------------------------------------------// CompareHandles//----------------------------------------------------------------------------------------template<class T>CompareResult_AC TemplateHandleList_AC<T>::CompareHandles(const Handle item1, const Handle item2) const // override{	return Compare(reinterpret_cast<T>(item1), reinterpret_cast<T>(item2));}//----------------------------------------------------------------------------------------// DoSearch//----------------------------------------------------------------------------------------template<class T>inline T TemplateHandleList_AC<T>::DoSearch(TestProc testHandle, void* yourDataPtr, ArrayIndex_AC& index) const{	return static_cast<T>(CHandleListBase_AC::DoSearch(testHandle, yourDataPtr, index));}//----------------------------------------------------------------------------------------// Search//----------------------------------------------------------------------------------------template<class T>inline ArrayIndex_AC TemplateHandleList_AC<T>::Search(TestProc testHandle, void* yourDataPtr){	return CHandleListBase_AC::Search(testHandle, yourDataPtr);}//----------------------------------------------------------------------------------------// SortBy//----------------------------------------------------------------------------------------template<class T>inline void TemplateHandleList_AC<T>::SortBy(CompareProc compareHandles, void* yourDataPtr){	return CHandleListBase_AC::SortBy(compareHandles, yourDataPtr);}#endif // __TemplateHandleList_AC__
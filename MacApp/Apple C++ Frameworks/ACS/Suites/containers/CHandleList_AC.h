// CHandleList_AC.h// Copyright й 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/02/01	Update copyright.		08/23/00	Added embedded documentation - CHandleList_AC.		10/18/00	Don't try to return void from SortBy. 		09/21/00	[W] Added export of TemplateScalarIterator_AC<Handle>.		05/15/00	CW Pro 6: must cast to same type as result. 		04/21/00	Base the Handle lists on void* lists. 		03/24/99	Update copyright notice.		10/06/98	Clean up includes. */#ifndef __CHandleList_AC__#define __CHandleList_AC__// ACS#ifndef __CList_AC__#include "CList_AC.h"#endif#ifndef __CListIterator_AC__#include "CListIterator_AC.h"#endif//========================================================================================// Dispose//========================================================================================// You will need to define a Dispose function that uses the appropriate call to// dispose of your handle type. inline void Dispose(Handle h)	{ ::DisposeHandle(h); } //еее  should use DisposeIfHandle_AC//----------------------------------------------------------------------------------------// procedure typedefs for parameters//----------------------------------------------------------------------------------------typedef CompareResult_AC (*CompareHandleType)(const Handle anItem, void* yourDataPtr);typedef CompareResult_AC (*CompareHandlesType)(const Handle item1, const Handle item2,											   void* yourDataPtr);//========================================================================================// CSortedHandleListBase_AC//========================================================================================class EXPORT_AC CSortedHandleListBase_AC : public CSortedVoidList_AC{public:	CSortedHandleListBase_AC(ArrayIndex_AC initialSize = 0) : CSortedVoidList_AC(initialSize) { }		// Constructor	virtual ~CSortedHandleListBase_AC();		// Destructor			virtual void* Clone() const;	void FreeAll();		// Frees each element in the list. Leaves fSize at 0.	void FreeList();		// Frees each handle in the list, then frees the list.		//еее This method will be removed. Please call FreeAll and then if necessary free the object	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void Insert(const Handle item);		// Inserts the given item into the list in sorted order, using the Compare method        // to determine the item's location relative to other items in the list.	Handle operator[](ArrayIndex_AC index) const;		// Return the index'th element of the list.		// the index is "zero" based.	Handle At(ArrayIndex_AC index) const;		// Return the index'th element of the list.	void Delete(const Handle item);		// Finds the handle and removes it from the list. 	Handle First() const;		// Return the first element of the list. 	Handle Last() const;		// Return the last element of the list. 	void Push(const Handle item);		// LIFO stack push.	Handle Pop();		// LIFO stack pop	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	ArrayIndex_AC GetIdentityItemNo(const Handle item) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	ArrayIndex_AC GetEqualItemNo(const Handle item) const;		// Find the first reference to item in the list. If item does not occur, return 0.	virtual CompareResult_AC CompareHandles(const Handle item1, const Handle item2) const;		// By default just compare the ordinal value of the items. Subclasses that want to        // use the items as pointers or other types should override if the comparison is        // based on the pointed at value	virtual CompareResult_AC CompareVoids(const void* item1, const void* item2) const; // override		// Forward into the Compare function.	Handle DoSearch(CompareHandleType testHandle, void* yourDataPtr, ArrayIndex_AC& index) const;		// Search the list using the supplied testHandle function. 	ArrayIndex_AC Search(CompareHandleType testHandle, void* yourDataPtr);		// Search the list using the supplied testHandle function. 	void SortBy(CompareHandlesType compareHandles, void* yourDataPtr);		// Sorts the list using the supplied compareHandles function.	//====================================================================================	// class Iterator	//====================================================================================	class EXPORT_AC Iterator : public TemplateScalarIterator_AC<Handle>	{	public:		Iterator(const CSortedHandleListBase_AC* inList,				 ArrayIndex_AC inLowBound,				 ArrayIndex_AC inHighBound,				 bool inForward = kIterateForward_AC) :			TemplateScalarIterator_AC<Handle>(inList, inLowBound, inHighBound, inForward)			{ }		Iterator(const CSortedHandleListBase_AC* inList,				 bool inForward = kIterateForward_AC) :			TemplateScalarIterator_AC<Handle>(inList, inForward)			{ }	};};//========================================================================================// CHandleListBase_AC//========================================================================================class EXPORT_AC CHandleListBase_AC : public CVoidList_AC{public:	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	CHandleListBase_AC(ArrayIndex_AC initialSize = 0) : CVoidList_AC(initialSize) { }		// constructor	virtual ~CHandleListBase_AC();		// Destructor			virtual void* Clone() const;	virtual void FreeAll();		// Frees each element in the list. Leaves fSize at 0.	//------------------------------------------------------------------------------------	// Array manipulation primitives	//------------------------------------------------------------------------------------	void Insert(const Handle item);		// Insert the item in order. 	Handle operator[](ArrayIndex_AC index) const;	Handle& operator[](ArrayIndex_AC index);		// Return the index'th element of the list. The reference version allows assignment. 		// the index is "zero" based.	Handle At(ArrayIndex_AC index) const;	Handle& At(ArrayIndex_AC index);		// Return the index'th element of the list. The reference version allows assignment. 	void AtPut(ArrayIndex_AC index, Handle newItem);		// Replace the index'th element of the list. 	void InsertBefore(ArrayIndex_AC index, Handle item);		// Insert a reference to item at the indicated index. 	void InsertFirst(Handle item);		// Insert a reference to item at the front of the list. 	void InsertLast(Handle item);		// Insert a reference to item at the back of the list. 	void Delete(const Handle item);		// Finds the item and removes it from the list. 	Handle First() const;		// Return the first element of the list. 	Handle Last() const;		// Return the last element of the list. 	void Push(const Handle item);		// LIFO stack push.	Handle Pop();		// LIFO stack pop	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	ArrayIndex_AC GetIdentityItemNo(const Handle item) const;		// Find the first reference to the IDENTITY item in the list. If item does not        // occur, return 0.	ArrayIndex_AC GetEqualItemNo(const Handle item) const;		// Find the first equivalent item in the list. If item does not occur, return 0.	virtual CompareResult_AC CompareHandles(const Handle item1, const Handle item2) const;		// By default just compare the ordinal value of the items. 	virtual CompareResult_AC CompareVoids(const void* item1, const void* item2) const; // override		// Forward into the Compare function.	Handle DoSearch(CompareHandleType testHandle, void* yourDataPtr, ArrayIndex_AC& index) const;		// Search the list using the supplied testHandle function. 	ArrayIndex_AC Search(CompareHandleType testHandle, void* yourDataPtr);		// Search the list using the supplied testHandle function. 	void SortBy(CompareHandlesType compareHandles, void* yourDataPtr);		// Sorts the list using the supplied compareHandles function.	//====================================================================================	// class Iterator	//====================================================================================	class EXPORT_AC Iterator : public TemplateScalarIterator_AC<Handle>	{	public:		Iterator(const CHandleListBase_AC* inList,				 ArrayIndex_AC inLowBound,				 ArrayIndex_AC inHighBound,				 bool inForward = kIterateForward_AC) :			TemplateScalarIterator_AC<Handle>(inList, inLowBound, inHighBound, inForward)			{ }		Iterator(const CHandleListBase_AC* inList,				 bool inForward = kIterateForward_AC) :			TemplateScalarIterator_AC<Handle>(inList, inForward)			{ }	};};//========================================================================================// CSortedHandleList_AC: A dynamic list of Handles. It IS permissible for descendants to add more// named fields//========================================================================================class EXPORT_AC CSortedHandleList_AC : public CSortedHandleListBase_AC// The CSortedHandleList_AC class implements a dynamic list of handles// sorted by ordinal value. This class includes methods for using// your criteria to insert, delete, and access elements. In addition,// iterator functions are available for performing operations on// all or a subset of the elements in the list.{public:	CSortedHandleList_AC(ArrayIndex_AC initialSize = 0) : CSortedHandleListBase_AC(initialSize) { }		// Constructor	virtual ~CSortedHandleList_AC();		// Destructor			virtual void* Clone() const;	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	virtual CompareResult_AC Compare(const Handle item1, const Handle item2) const;		// By default just compare the ordinal value of the items. Subclasses that want to        // use the items as pointers or other types should override if the comparison is        // based on the pointed at value	virtual CompareResult_AC CompareHandles(const Handle item1, const Handle item2) const; // override		// Forward into the Compare function.};//========================================================================================// CAutoSortedHandleList_AC//========================================================================================class EXPORT_AC CAutoSortedHandleList_AC : public CSortedHandleList_AC// The CAutoSortedHandleList_AC class implements a CSortedHandleList_AC that// "owns" its elements and thus calls FreeAll at destructor time. //{public:	CAutoSortedHandleList_AC(ArrayIndex_AC initialSize = 0) :		CSortedHandleList_AC(initialSize) { }		// Constructor	virtual ~CAutoSortedHandleList_AC() { FreeAll(); }		// Destructor};		//========================================================================================// CHandleList_AC: A dynamic list of Handles. It IS permissible for descendants to add more// named fields//========================================================================================class EXPORT_AC CHandleList_AC : public CHandleListBase_AC// The CHandleList_AC class implements a dynamic list of handles. This class includes// methods for using your criteria to insert, delete, and access elements. In addition,// iterator functions are available for performing operations on all or a subset of the// elements in the list.{public:	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	CHandleList_AC(ArrayIndex_AC initialSize = 0) : CHandleListBase_AC(initialSize) { }		// constructor	virtual ~CHandleList_AC();		// Destructor			virtual void* Clone() const;	//------------------------------------------------------------------------------------	// Sorting	//------------------------------------------------------------------------------------	virtual CompareResult_AC Compare(const Handle item1, const Handle item2) const;		// By default just compare the ordinal value of the items. 	virtual CompareResult_AC CompareHandles(const Handle item1, const Handle item2) const; // override		// Forward into the Compare function.};//========================================================================================// CAutoHandleList_AC//========================================================================================class EXPORT_AC CAutoHandleList_AC : public CHandleList_AC// The CAutoHandleList_AC class implements a CHandleList_AC that// "owns" its elements and thus calls FreeAll at destructor time. //{public:	//------------------------------------------------------------------------------------	// Creation/ Destruction Methods	//------------------------------------------------------------------------------------	CAutoHandleList_AC(ArrayIndex_AC initialSize = 0) :		CHandleList_AC(initialSize) { }		// Constructor	virtual ~CAutoHandleList_AC() { FreeAll(); }		// Destructor};	//========================================================================================// CHandleIterator_AC//========================================================================================typedef CSortedHandleListBase_AC::Iterator CHandleIterator_AC;//========================================================================================// CLASS CSortedHandleListBase_AC//========================================================================================//----------------------------------------------------------------------------------------// Insert//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CSortedHandleListBase_AC::Insert(const Handle item){	CSortedVoidList_AC::Insert(item);}#endif//----------------------------------------------------------------------------------------// operator[]//----------------------------------------------------------------------------------------inline Handle CSortedHandleListBase_AC::operator[](ArrayIndex_AC index) const{	return static_cast<Handle>(CSortedVoidList_AC::operator[](index));}//----------------------------------------------------------------------------------------// At//----------------------------------------------------------------------------------------inline Handle CSortedHandleListBase_AC::At(ArrayIndex_AC index) const{	return static_cast<Handle>(CSortedVoidList_AC::At(index));}//----------------------------------------------------------------------------------------// Delete//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CSortedHandleListBase_AC::Delete(const Handle item){	CSortedVoidList_AC::Delete(item);}#endif//----------------------------------------------------------------------------------------// First//----------------------------------------------------------------------------------------inline Handle CSortedHandleListBase_AC::First() const{	return static_cast<Handle>(CSortedVoidList_AC::First());}//----------------------------------------------------------------------------------------// Last//----------------------------------------------------------------------------------------inline Handle CSortedHandleListBase_AC::Last() const{	return static_cast<Handle>(CSortedVoidList_AC::Last());}//----------------------------------------------------------------------------------------// Push//----------------------------------------------------------------------------------------inline void CSortedHandleListBase_AC::Push(const Handle item){	CSortedVoidList_AC::Push(item);}//----------------------------------------------------------------------------------------// Pop//----------------------------------------------------------------------------------------inline Handle CSortedHandleListBase_AC::Pop(){	return static_cast<Handle>(CSortedVoidList_AC::Pop());}//----------------------------------------------------------------------------------------// GetIdentityItemNo//----------------------------------------------------------------------------------------inline ArrayIndex_AC CSortedHandleListBase_AC::GetIdentityItemNo(const Handle item) const{	return CSortedVoidList_AC::GetIdentityItemNo(item);}//----------------------------------------------------------------------------------------// GetEqualItemNo//----------------------------------------------------------------------------------------inline ArrayIndex_AC CSortedHandleListBase_AC::GetEqualItemNo(const Handle item) const{	return CSortedVoidList_AC::GetEqualItemNo(item);}//----------------------------------------------------------------------------------------// DoSearch//----------------------------------------------------------------------------------------inline Handle CSortedHandleListBase_AC::DoSearch(CompareHandleType testHandle, void* yourDataPtr, ArrayIndex_AC& index) const{	return reinterpret_cast<Handle>(CSortedVoidList_AC::DoSearch(reinterpret_cast<CompareVoidType>(testHandle), yourDataPtr, index));}//----------------------------------------------------------------------------------------// Search//----------------------------------------------------------------------------------------inline ArrayIndex_AC CSortedHandleListBase_AC::Search(CompareHandleType testHandle, void* yourDataPtr){	return CSortedVoidList_AC::Search(reinterpret_cast<CompareVoidType>(testHandle), yourDataPtr);}//----------------------------------------------------------------------------------------// SortBy//----------------------------------------------------------------------------------------inline void CSortedHandleListBase_AC::SortBy(CompareHandlesType compareHandles, void* yourDataPtr){	CSortedVoidList_AC::SortBy(reinterpret_cast<CompareVoidsType>(compareHandles), yourDataPtr);}//========================================================================================// CLASS CHandleListBase_AC//========================================================================================//----------------------------------------------------------------------------------------// Insert//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::Insert(const Handle item){	CVoidList_AC::Insert(item);}#endif//----------------------------------------------------------------------------------------// operator[]//----------------------------------------------------------------------------------------inline Handle CHandleListBase_AC::operator[](ArrayIndex_AC index) const{	return static_cast<Handle>(CVoidList_AC::operator[](index));}inline Handle& CHandleListBase_AC::operator[](ArrayIndex_AC index){	void*& h = CVoidList_AC::operator[](index);	return reinterpret_cast<Handle&>(h);}//----------------------------------------------------------------------------------------// At//----------------------------------------------------------------------------------------inline Handle CHandleListBase_AC::At(ArrayIndex_AC index) const{	return static_cast<Handle>(CVoidList_AC::At(index));}inline Handle& CHandleListBase_AC::At(ArrayIndex_AC index){	void*& h = CVoidList_AC::At(index);	return reinterpret_cast<Handle&>(h);}//----------------------------------------------------------------------------------------// AtPut//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::AtPut(ArrayIndex_AC index, Handle newItem){	CVoidList_AC::AtPut(index, newItem);}#endif//----------------------------------------------------------------------------------------// InsertBefore//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::InsertBefore(ArrayIndex_AC index, Handle item){	CVoidList_AC::InsertBefore(index, item);}#endif//----------------------------------------------------------------------------------------// InsertFirst//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::InsertFirst(Handle item){	CVoidList_AC::InsertFirst(item);}#endif//----------------------------------------------------------------------------------------// InsertLast//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::InsertLast(Handle item){	CVoidList_AC::InsertLast(item);}#endif//----------------------------------------------------------------------------------------// Delete//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::Delete(const Handle item){	CVoidList_AC::Delete(item);}#endif//----------------------------------------------------------------------------------------// First//----------------------------------------------------------------------------------------inline Handle CHandleListBase_AC::First() const{	return static_cast<Handle>(CVoidList_AC::First());}//----------------------------------------------------------------------------------------// Last//----------------------------------------------------------------------------------------inline Handle CHandleListBase_AC::Last() const{	return static_cast<Handle>(CVoidList_AC::Last());}//----------------------------------------------------------------------------------------// Push//----------------------------------------------------------------------------------------#if !qCoreDebuginline void CHandleListBase_AC::Push(const Handle item){	CVoidList_AC::Push(item);}#endif//----------------------------------------------------------------------------------------// Pop//----------------------------------------------------------------------------------------inline Handle CHandleListBase_AC::Pop(){	return static_cast<Handle>(CVoidList_AC::Pop());}//----------------------------------------------------------------------------------------// GetIdentityItemNo//----------------------------------------------------------------------------------------inline ArrayIndex_AC CHandleListBase_AC::GetIdentityItemNo(const Handle item) const{	return CVoidList_AC::GetIdentityItemNo(item);}//----------------------------------------------------------------------------------------// GetEqualItemNo//----------------------------------------------------------------------------------------inline ArrayIndex_AC CHandleListBase_AC::GetEqualItemNo(const Handle item) const{	return CVoidList_AC::GetEqualItemNo(item);}//----------------------------------------------------------------------------------------// DoSearch//----------------------------------------------------------------------------------------inline Handle CHandleListBase_AC::DoSearch(CompareHandleType testHandle, void* yourDataPtr, ArrayIndex_AC& index) const{	return static_cast<Handle>(CVoidList_AC::DoSearch(reinterpret_cast<CompareVoidType>(testHandle), yourDataPtr, index));}//----------------------------------------------------------------------------------------// Search//----------------------------------------------------------------------------------------inline ArrayIndex_AC CHandleListBase_AC::Search(CompareHandleType testHandle, void* yourDataPtr){	return CVoidList_AC::Search(reinterpret_cast<CompareVoidType>(testHandle), yourDataPtr);}//----------------------------------------------------------------------------------------// SortBy//----------------------------------------------------------------------------------------inline void CHandleListBase_AC::SortBy(CompareHandlesType compareHandles, void* yourDataPtr){	CVoidList_AC::SortBy(reinterpret_cast<CompareVoidsType>(compareHandles), yourDataPtr);}//========================================================================================// Exports, Etc.//========================================================================================#if _MSC_VERtemplate class TemplateScalarIterator_AC<Handle>;#endif#endif // __CHandleList_AC__
// CButton_AC.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved. // Portions Copyright ©1997 by Trygve Isaacson./*	Change History		01/02/01	Update copyright.		11/04/00	Conditional on qEmulateStandardControls		03/24/99	Update copyright notice.		10/07/98	Clean up includes. */#ifndef __CButton_AC__#define __CButton_AC__// ACS#ifndef __CCustomControl_AC__#include "CCustomControl_AC.h"#endif#ifndef __CTextStyle_AC__#include "CTextStyle_AC.h"#endif#if qEmulateStandardControls//----------------------------------------------------------------------------------------// CButton_AC//----------------------------------------------------------------------------------------class EXPORT_AC CButton_AC : public CCustomButton_AC// CButton_AC implements the standard AGA pushbutton. It automatically// draws the default button outline if you set its fIsDefault property// by calling SetDefault; you specify whether the outline is inside or// outside the object's mBounds.{public:	enum { kIsDefault = true, kIsNotDefault = false };	enum { kFrameInside = true, kFrameOutside = false };	CButton_AC(const CRect_AC& boundsRect, bool isActive,			   const CStr255_AC& itsTitle = kEmptyString_AC,			   const CTextStyle_AC& titleStyle = CTextStyle_AC::kSystemStyle,			   bool isDefault = kIsNotDefault,			   bool frameInside = kFrameOutside);	virtual ~CButton_AC();				virtual ControlPartCode TestMouse(const CPoint_AC& mouseLocation) const;	void			SetDefault(bool isDefault);	inline bool		IsDefault() const { return fIsDefault; }		void			SetFrameInside(bool frameInside);	inline bool		IsFrameInside() const { return fFrameInside; }protected:	virtual void 	SetData(ControlPartCode inPart, ResType inTagName,							Size inSize, Ptr inData);	virtual Size	GetData(ControlPartCode inPart, ResType inTagName,							Size inBufferSize, Ptr inBuffer);	virtual Size	GetDataSize(ControlPartCode inPart, ResType inTagName);		// Get and set default state of a control.protected:	virtual void		DrawButtonNormal(bool deep);	virtual void		DrawButtonPressed(bool deep);	virtual void		DrawButtonDisabled(bool deep);		void				DrawOutlineNormal(bool deep);	void				DrawOutlineDisabled(bool deep);	bool				fIsDefault;		// if true this button gets a default frame	bool				fFrameInside;	// if true the frame is inside mBounds; else outside};#endif // qEmulateStandardControls#endif // __CButton_AC__
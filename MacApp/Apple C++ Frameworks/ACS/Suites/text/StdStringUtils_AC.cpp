//----------------------------------------------------------------------------------------// StdStringUtils_AC.cpp// Copyright © 2000-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History:		04/07/01	[PB] Comparison between signed and unsigned. 		01/19/01	Include Utils_AC.h and MemoryUtils_AC.h.		12/28/00	Added GetResourceString and StringToStr255.		01/02/01	Update copyright.		08/19/00	Initial version. */#ifndef __StdStringUtils_AC__#include "StdStringUtils_AC.h"#endif// ACS Core#ifndef __CException_AC__#include "CException_AC.h"#endif#ifndef __CMultiLocal_AC__#include "CMultiLocal_AC.h"#endif#ifndef __MemoryUtils_AC__#include "MemoryUtils_AC.h"#endif#ifndef __Utils_AC__#include "Utils_AC.h"#endif// ANSI#ifndef __cstdarg__#include <cstdarg>#endif//========================================================================================// Namespacings//========================================================================================using std::va_list;using std::vsprintf;using std::string;//========================================================================================// Global Functions//========================================================================================//----------------------------------------------------------------------------------------// stringprintf//----------------------------------------------------------------------------------------void stringprintf(std::string& s, const char* format, ...){	char buffer[1024];	va_list args;	va_start(args, format);	int n = vsprintf(buffer, format, args);	va_end(args);	s.assign(buffer, n);}//----------------------------------------------------------------------------------------// GetResourceString//----------------------------------------------------------------------------------------string GetResourceString(ResNumber strListID, short index)// GetResourceString retrieves a string from a STR# resource.  The retrieved// string is returned.//// strResourceID:	The resource number of the STR# containing the string to be//					retrieved.// index:			The index into the STR# by which the string is retrieved.//// Call?	As needed.{	Str255	toolboxString;	CMultiLocale_AC::GetIndString(toolboxString, strListID, index);	return string(reinterpret_cast<const char*>(&toolboxString[1]), static_cast<unsigned long>(toolboxString[0]));}//----------------------------------------------------------------------------------------// GetResourceString//----------------------------------------------------------------------------------------string GetResourceString(ResNumber strResourceID)// GetResourceString retrieves a string from a STR resource.  The retrieved// string is returned.//// strResourceID:	The resource number of the STR to be retrieved.//// Call?	As needed.{	StringHandle toolboxStringH = CMultiLocale_AC::GetString(strResourceID);	::ThrowIfNULL_AC(toolboxStringH);	Size		stringSize = ::GetHandleSize(reinterpret_cast<Handle>(toolboxStringH));		return string(reinterpret_cast<const char*>(*toolboxStringH), stringSize);}//----------------------------------------------------------------------------------------// StringToStr255//----------------------------------------------------------------------------------------void StringToStr255(const string& fromString, Str255& toStr255)// StringToStr255 converts a standard string into a Str255.//// fromString:	A constant string that is to be copied into the destination Str255.// toStr255:	A reference to a Str255 that is to receive the string.//// Call?	As needed.{	UInt8	strLength = Min_AC(static_cast<string::size_type>(255), fromString.size());	toStr255[0] = strLength;	BlockMove_AC(static_cast<const void*>(fromString.c_str()), static_cast<void*>(&toStr255[1]), strLength);}//----------------------------------------------------------------------------------------// End of StdStringUtils_AC.cpp
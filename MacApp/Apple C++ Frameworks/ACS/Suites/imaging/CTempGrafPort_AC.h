// CTempGrafPort_AC.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved. /*	Change History		01/02/01	Update copyright.		10/04/00	Allow using with color ports and window references. 		09/18/00	Provide access to the GrafPtr. 		03/24/99	Update copyright notice.		02/18/99	Change QuickDraw.h to Quickdraw.h.		10/07/98	Clean up includes. */#ifndef __CTempGrafPort_AC__#define __CTempGrafPort_AC__// ACS#ifndef __Types_AC__#include "Types_AC.h"#endif// Toolbox#ifndef __MACWINDOWS__#include <MacWindows.h>#endif#ifndef __QUICKDRAW__#include <Quickdraw.h>#endif//========================================================================================// CTempGrafPort_AC//========================================================================================class EXPORT_AC CTempGrafPort_AC// CTempGrafPort_AC is a stack-based object that sets the port, and// restores it upon destruction.{public:	CTempGrafPort_AC(GrafPtr tempPort = NULL)	{		Set(tempPort);	}#if OPAQUE_TOOLBOX_STRUCTS	CTempGrafPort_AC(WindowRef w)	{		Set(GetWindowPort(w));	}#else	CTempGrafPort_AC(CGrafPtr tempPort)	{		Set(reinterpret_cast<GrafPtr>(tempPort));	}#endif	~CTempGrafPort_AC()	{		::MacSetPort(fSavedPort);	}	GrafPtr get() const	{ return fSavedPort; }	operator GrafPtr() const	{ return fSavedPort; }		// Convert to the data	bool operator <(GrafPtr rhs) const	{ return fSavedPort < rhs; }	bool operator <=(GrafPtr rhs) const	{ return fSavedPort <= rhs; }	bool operator ==(GrafPtr rhs) const	{ return fSavedPort == rhs; }	bool operator !=(GrafPtr rhs) const	{ return fSavedPort != rhs; }	bool operator >=(GrafPtr rhs) const	{ return fSavedPort >= rhs; }	bool operator >(GrafPtr rhs) const	{ return fSavedPort > rhs; }		// Pointer comparisons. private:	void Set(GrafPtr tempPort);	GrafPtr fSavedPort;		};#endif // __CTempGrafPort_AC__
// CTemplateRect_AC.h// Copyright © 1996-2001 by Apple Computer. All rights reserved. /*	Change History		01/05/01	Change to use CString_AC instead of std::string.		01/10/01	Revert. 		01/09/01	All template functions must be in header so those in the					implementation file were moved in.		01/02/01	Update copyright.		08/17/00	Get template to compile in a namespace. 		08/17/00	Replace CCharStr with standard strings. 		03/23/00	[G] Do not implemente AsText.		11/16/99	Operator [] can work without a conditional. 		07/26/99	Made Width and Height const.  Added Middle.		03/24/99	Update copyright notice.		11/06/98	Do not EXPORT_AC of CTemplateRect_AC for Windows.		10/07/98	Clean up includes. 					Change operator const char* to operator CChar63_AC. */#ifndef __CTemplateRect_AC__#define __CTemplateRect_AC__// ACS#ifndef __CRect_AC__#include "CRect_AC.h"#endif#ifndef __CTemplatePoint_AC__#include "CTemplatePoint_AC.h"#endif#ifndef __Types_AC__#include "Types_AC.h"#endif#if qDebug	#ifndef __CString_AC__	#include "CString_AC.h"	#endif#endif// Toolbox#ifndef __LIMITS__#include <Limits.h>#endif//----------------------------------------------------------------------------------------// Constants//----------------------------------------------------------------------------------------// const short kMaxTemplate = SHRT_MAX - 1;	// -1 to minimize signed overflow problems	// •••  	//----------------------------------------------------------------------------------------// Types//----------------------------------------------------------------------------------------//template <class T> class CTemplatePoint_AC;//template <class T> class CTemplateRect_AC;//----------------------------------------------------------------------------------------// CTemplateRect_AC is a toolbox compatible class for the old struct CTemplateRect_AC. It is bitwise compatible// with the old struct because it contains no virtual functions.//----------------------------------------------------------------------------------------template <class T>class #if !qMSWindows	EXPORT_AC#endif		CTemplateRect_AC{public:	typedef T Coordinate;	// CONSTRUCTORS:	CTemplateRect_AC() 	{ Clear(); }		CTemplateRect_AC(	Coordinate iLeft, 						Coordinate iTop, 						Coordinate iRight, 						Coordinate iBottom) 								: top(iTop), left(iLeft), bottom(iBottom), right(iRight) 	{}		CTemplateRect_AC(	const CTemplatePoint_AC<T>& topLeftPt, 						const CTemplatePoint_AC<T>& botRightPt) 	{ 	*(CTemplatePoint_AC<T>*)&top = *(const CTemplatePoint_AC<T>*)&topLeftPt; 		*(CTemplatePoint_AC<T>*)&bottom = *(const CTemplatePoint_AC<T>*)&botRightPt; }		CTemplateRect_AC(const CRect_AC& aRect) 		:	top(aRect.top), left(aRect.left), bottom(aRect.bottom), right(aRect.right) 		{ }		CTemplateRect_AC(const Rect& aRect) 		:	top(aRect.top), left(aRect.left), bottom(aRect.bottom), right(aRect.right) 		{ }#if qPowerPC	CTemplateRect_AC(const CTemplateRect_AC<T>& inRect) 	{ 	*(CTemplatePoint_AC<T>*)&top = *(const CTemplatePoint_AC<T>*)&inRect.top; 		*(CTemplatePoint_AC<T>*)&bottom = *(const CTemplatePoint_AC<T>*)&inRect.bottom; }	CTemplateRect_AC& operator=(const CTemplateRect_AC<T>& inRect) 	{ 	*(CTemplatePoint_AC<T>*)&top = *(const CTemplatePoint_AC<T>*)&inRect.top; 		*(CTemplatePoint_AC<T>*)&bottom = *(const CTemplatePoint_AC<T>*)&inRect.bottom; 		return *this; }#else	CTemplateRect_AC(const CTemplateRect_AC<T>& inRect) 		:	top(inRect.top), left(inRect.left), right(inRect.right), bottom(inRect.bottom)	{}	CTemplateRect_AC<T>& operator=(const CTemplateRect_AC<T>& inRect) 	{ 	top = inRect.top; left = inRect.left; right = inRect.right; bottom = inRect.bottom; 		return *this; }#endif				// SELECTION:		CTemplatePoint_AC<T>& operator[](PointSelector sel) 	{ return reinterpret_cast<CTemplatePoint_AC<T> *>(&top)[sel]; }		const CTemplatePoint_AC<T>& operator[](PointSelector sel) const 	{ return reinterpret_cast<const CTemplatePoint_AC<T> *>(&top)[sel]; }	// CONVERSION:	#if qDebug	CString_AC AsText() const;	operator CString_AC() const { return AsText(); }		// Conversion operator, for converting CTemplateRect_AC to a textual representation of a	    // CTemplateRect_AC. "CTemplateRect_AC(top,righ,bottom,left)".#endif		// ARITHMETIC:	CTemplateRect_AC<T>& operator+=(const CTemplateRect_AC<T>& rt);	CTemplateRect_AC<T>& operator-=(const CTemplateRect_AC<T>& rt);	CTemplateRect_AC<T>& operator*=(const CTemplateRect_AC<T>& rt);	CTemplateRect_AC<T>& operator/=(const CTemplateRect_AC<T>& rt);	CTemplateRect_AC<T> operator+(const CTemplateRect_AC<T>& rt) const;	CTemplateRect_AC<T> operator-(const CTemplateRect_AC<T>& rt) const;	CTemplateRect_AC<T> operator*(const CTemplateRect_AC<T>& rt) const;	CTemplateRect_AC<T> operator/(const CTemplateRect_AC<T>& rt) const;	CTemplateRect_AC<T>& operator+=(const CTemplatePoint_AC<T>& pt);	CTemplateRect_AC<T>& operator-=(const CTemplatePoint_AC<T>& pt);	CTemplateRect_AC<T>& operator*=(const CTemplatePoint_AC<T>& pt);	CTemplateRect_AC<T>& operator/=(const CTemplatePoint_AC<T>& pt);	CTemplateRect_AC<T> operator+(const CTemplatePoint_AC<T>& pt) const;	CTemplateRect_AC<T> operator-(const CTemplatePoint_AC<T>& pt) const;	CTemplateRect_AC<T> operator*(const CTemplatePoint_AC<T>& pt) const;	CTemplateRect_AC<T> operator/(const CTemplatePoint_AC<T>& pt) const;		CTemplateRect_AC<T> operator-() const;	// COMPARISON:	bool operator==(const CTemplateRect_AC<T>& rt) const;		bool operator!=(const CTemplateRect_AC<T>& rt) const 	{ return !(*this == rt); }	// GEOMETRY:	CTemplateRect_AC<T>& operator &=(const CTemplateRect_AC<T>& rt);	CTemplateRect_AC<T>& operator |=(const CTemplateRect_AC<T>& rt);	CTemplateRect_AC<T> operator&(const CTemplateRect_AC<T>& rt) const;	CTemplateRect_AC<T> operator|(const CTemplateRect_AC<T>& rt) const;	// Two simple area operators, the intersection & (bitwise and in C++) and the	// union | (bitwise or in C++). The definition of union here is to return a CRect_AC	// that exactly encloses its operands.		Coordinate Width() const { return right - left; }	Coordinate Height() const { return bottom - top; }	CTemplatePoint_AC<T> Middle() const		{ return CTemplatePoint_AC<T>((left + right) / 2, (top + bottom) / 2); }	// UTILITY:	CRect_AC AsRect() const 	{ return CRect_AC(static_cast<short>(left), 						static_cast<short>(top), 						static_cast<short>(right), 						static_cast<short>(bottom)); }		// Creates a CRect_AC by casting the Coordinate values to short values. In other		// words, it throws away the top 16 bits of each long word value. If the values		// don't fit within 16 bits you will get strange results (i.e. 32769 -> 1). 		// Danger: Don't use this in TView methods for converting from view space to QD		// space. Instead, use TView::ViewToQDRect()!	CRect_AC ToRect() const;		// Creates a CRect_AC by limiting the Coordinate values to +/- 32K and casting		// them to short values. This produces reasonable results if the values were		// more than 16 bits (i.e. 32769 -> 32767). 		// Danger: Don't use this in TView methods for converting from view space to QD		// space. Instead, use TView::ViewToQDRect()!		void Clear() 	{ top = left = bottom = right = 0; }	void Set(Coordinate iLeft, Coordinate iTop, Coordinate iRight, Coordinate iBottom) 	{ top = iTop; left = iLeft; bottom = iBottom; right = iRight; }		void Set( const CTemplatePoint_AC<T>&, Coordinate width, Coordinate height );		void Set(const CTemplatePoint_AC<T>& topLeftPt, const CTemplatePoint_AC<T>& botRightPt) 	{ *(CTemplatePoint_AC<T>*)&top = topLeftPt; *(CTemplatePoint_AC<T>*)&bottom = botRightPt; }		void Offset( Coordinate iH, Coordinate iV );		void Offset(const CTemplatePoint_AC<T>& delta) 	{ *this += delta; }	void Inset( Coordinate iH, Coordinate iV );	void Inset(const CTemplatePoint_AC<T>& delta);	void Inset(const CTemplateRect_AC<T>& delta);	bool IsEmpty() const;	bool Empty() const 	{ return IsEmpty(); }	// Return true if the CTemplateRect_AC is empty (right - left = 0 and bottom - top = 0)		bool IsValid() const;	bool Valid() const 	{ return IsValid(); }	// Returns true if a valid rectangle (left < right and top < bottom).			void Validate();	// Validate rectagle by switching the points if right-bottom is above or to the    // left of left-top.			Coordinate GetLength(VHSelect sel) const;	// Returns the length of a CTemplateRect_AC in a given dimension.			CTemplatePoint_AC<T> GetSize() const;	// Returns the size of a CTemplateRect_AC in a CTemplatePoint_AC<T>.		bool Contains(const CTemplatePoint_AC<T>& pt) const;	bool Contains(const CTemplateRect_AC<T>& rt) const;protected:	Coordinate Min(const Coordinate a, const Coordinate b) const 	{ return a < b ? a : b; }	Coordinate Max(const Coordinate a, const Coordinate b) const 	{ return a > b ? a : b; }	Coordinate MinMax(Coordinate MinVal, Coordinate expression, Coordinate MaxVal) const 	{ return Min(Max(expression, MinVal), MaxVal); }	// Returns the bounded minimum and maximum 	// CONSTANTS:	public:	static const CTemplateRect_AC<T> kZeroRect;	Coordinate top;	Coordinate left;	Coordinate bottom;	Coordinate right;};#endif // __CTemplateRect_AC__
// CPolarPoint_AC.h// Copyright © 1998-2001 by Apple Computer. All rights reserved. /*	Change History		03/01/01	Cosmetic. 		01/02/01	Update copyright.		03/24/99	Update copyright notice.		10/07/98	Clean up includes. 		----- R13u4 -----		09/22/98	[2275627] Include CLongGeometry_AC.h.		06/18/98	Added missing EXPORT_AC		06/17/98	Implemented addition and subtraction as variants of addto/subfrom		06/17/98	Gave to Apple for MacApp/ACS*/#pragma once#ifndef __CPolarPoint_AC__#define __CPolarPoint_AC__// ACS#ifndef __CLongGeometry_AC__#include "CLongGeometry_AC.h"#endif//========================================================================================// CLASS CPolarPoint_AC//========================================================================================class EXPORT_AC CPolarPoint_AC{public:	// C O N S T R U C T O R S	inline CPolarPoint_AC() :	fAngle(0),	fRadius(0)	{ }	CPolarPoint_AC(const CLongPoint_AC& thePt) { Set(thePt); }	// Construct from a cartesian coordinate	CPolarPoint_AC(float inAngle, float inRadius) :		fAngle(inAngle),		fRadius(inRadius)	{ }	CPolarPoint_AC(const CPolarPoint_AC& rhs);	// copy constructor	void Set(const CLongPoint_AC& thePt);	// Set from a cartesian coordinate	// TYPE  CONVERSIONS	operator CLongPoint_AC() const;	// convert from polar space to cartesian space	// OPERATOR OVERLOADS	CPolarPoint_AC& operator+=(const CPolarPoint_AC& rhs);	CPolarPoint_AC& operator-=(const CPolarPoint_AC& rhs);	CPolarPoint_AC operator+(const CPolarPoint_AC& rhs) const { return CPolarPoint_AC(*this) + rhs; }	CPolarPoint_AC operator-(const CPolarPoint_AC& rhs) const { return CPolarPoint_AC(*this) - rhs; }	CPolarPoint_AC operator-() const;	// ACCESSORS	float GetAngle() const { return fAngle; }	// returns the angle in radians	float GetRadius() const { return fRadius; }	void SetAngle(float inAngle) { fAngle = inAngle; }	// set the angle in radians	void SetRadius(float inRadius) { fRadius = inRadius; }	// CONVERTORS	static float ToRadians(float inDegrees);	// convert degrees to radians	static float ToDegrees(float inRadians);	// convert radians to degreesprotected:	float fAngle;								// the angle of the point in Radians	float fRadius;								// the radius to the point	// Staticspublic:	static float pi;	static float twoPi;	static float halfPi;};#endif // __CPolarPoint_AC__
// TextBox_AC.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		03/01/01	Cosmetic. 		01/02/01	Update copyright.		12/15/00	Made Ptr passed to TextBox_AC const char*.		11/22/00	Properly type the wordBreak proc. 		03/24/99	Update copyright notice.		10/07/98	Clean up includes. 		----- R13u4 -----		----- R13u3 -----		06/05/97	EXPORT_AC, EXPORT_MA, no-load fixes, Memory_AC, Windows_AC, no CW11orLATER, removed segmentation, etc...		03/25/97	AC_ to _AC Name change.		03/19/97	Fixed default parameters for MacApp compatibility.		03/11/97	Moved from UMacAppUtilities. */ #ifndef __TextBox_AC__#define __TextBox_AC__// ACS#ifndef __CRect_AC__#include "CRect_AC.h"#endif#ifndef __CTextStyle_AC__#include "CTextStyle_AC.h"#endif// Toolbox#ifndef __TEXTEDIT__#include <TextEdit.h>#endif//----------------------------------------------------------------------------------------// Constants//----------------------------------------------------------------------------------------const bool kAutoWrap = true;					// Want to AutoWrap (for MATextBox)const bool kNoAutoWrap = !kAutoWrap;			// Don't want to AutoWrap (for MATextBox) const bool kEraseFirst = true;					// Want to erase first (for MATextBox) const bool kNoEraseFirst = !kEraseFirst;		// Don't want to erase first (for                                                // MATextBox)const bool kSpaceForCaret = true;				// Want to leave space for an insertion                                                // caret(forMATextBox)const bool kNoSpaceForCaret = !kSpaceForCaret;	// Don't want to leave space for an                                                // insertioncaret(forMATextBox)//----------------------------------------------------------------------------------------// Global Functions//----------------------------------------------------------------------------------------EXPORT_AC void TextBox_AC(const char* text,						  short itsLength,						  const CRect_AC& box,						  short itsJust,						  bool autoWrap = kAutoWrap,						  WordBreakProcPtr wordBreak = NULL,						  bool eraseFirst = kNoEraseFirst,						  bool spaceForCaret = kSpaceForCaret,						  bool preferOutline = kDontPreferOutlineFonts);// Implements a superset of TETextBox functionality, with defaultable arguments so it can be used// as a replacement call.	#endif // __TextBox_AC__
//----------------------------------------------------------------------------------------// CTidyHeapActions_AC.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/02/01	Update copyright.		03/24/99	Update copyright notice.*/#ifndef __CTidyHeapActions_AC__#define __CTidyHeapActions_AC__#ifndef __CTidyHeap_AC__#include "CTidyHeap_AC.h"#endif#ifndef __CTidyHeapAction_AC__#include "CTidyHeapAction_AC.h"#endif//========================================================================================// 	class CActionBreakOnDelete_AC////	This action is meant to be a CTidyHeapBlock_AC's delete action. This basically calls// 	CTidyHeap_AC::PrintDebugString and logs the break to Logf. You can see when a specific block is being // 	deleted this way.//========================================================================================class CTidyHeapBlock_AC;class EXPORT_AC CActionBreakOnDelete_AC: public CTidyHeapAction_AC{public:	CActionBreakOnDelete_AC(const char* outStr);	CActionBreakOnDelete_AC() { fStr[0] = '\0'; }	virtual ~CActionBreakOnDelete_AC() {}		virtual void DoAction(CTidyHeapBlock_AC* inBlock);private:	char fStr[256];};//========================================================================================// 	class CMakeNewFail//// 	This action will cause new to fail when new is called x times. Send the count you// 	want to fail into the constructor.//// 	This needs to be a global new action.//========================================================================================class EXPORT_AC CActionMakeNewFail_AC: public CTidyHeapAction_AC{public:	CActionMakeNewFail_AC(long whenToFail);	virtual ~CActionMakeNewFail_AC() {}		virtual void DoAction(CTidyHeapBlock_AC* inBlock);		virtual void SetFailNum(long inCount) { fFailNum = inCount; }	long GetFailNum() { return fFailNum; }		virtual void SetNewTally(long inCount) { fNewTally = inCount; }	long GetNewTally() const { return fNewTally; }	protected:	long 		fNewTally;	long 		fFailNum;};//========================================================================================// class CActionVerifyAll_AC//// This action causes *ALL* blocks to be verified when its DoAction method is called..//// This can be either a new or a delete action.////========================================================================================class EXPORT_AC CActionVerifyAll_AC: public CTidyHeapAction_AC{public:	CActionVerifyAll_AC(bool logToFile = true) : fLog(logToFile) {}	virtual ~CActionVerifyAll_AC() {}		virtual void DoAction(CTidyHeapBlock_AC* inBlock);		bool DoLog() const { return fLog; }		void DoLog(bool inLog) { fLog = inLog; }	private:	bool fLog;};//========================================================================================// class CActionVerify_AC//// This action causes *ALL* blocks to be verified when its DoAction method is called..//// This can be either a new or a delete action.////========================================================================================class EXPORT_AC CActionVerifyBlock_AC: public CTidyHeapAction_AC{public:	CActionVerifyBlock_AC(bool logToFile = true) : fLog(logToFile) {}	virtual ~CActionVerifyBlock_AC() {}		virtual void DoAction(CTidyHeapBlock_AC* inBlock);	bool DoLog() const { return fLog; }		void DoLog(bool inLog) { fLog = inLog; }private:	bool fLog;};//========================================================================================// class CActionExemptBlocks_AC//// This action causes *ALL* blocks to not be tracked while it is installed.//// This needs to be a global new action////========================================================================================class EXPORT_AC CActionExemptBlocks_AC: public CTidyHeapAction_AC{public:	CActionExemptBlocks_AC(long inStopper = 0) : fStopper(inStopper) {}		// if inStopper > 0 this class will only except blocks of		// id <= fStopper.		virtual ~CActionExemptBlocks_AC() {}		virtual void DoAction(CTidyHeapBlock_AC* inBlock);private:	long fStopper;};//========================================================================================// class CActionBreakOnNew_AC//// This action causes a call to CTidyHeap_AC::PrintDebugString when new is called for a// block with the preset Id.//// This needs to be a global new action////========================================================================================class EXPORT_AC CActionBreakOnNew_AC: public CTidyHeapAction_AC{public:	CActionBreakOnNew_AC(long inID) : fID(inID) {}	virtual ~CActionBreakOnNew_AC() {}		virtual void DoAction(CTidyHeapBlock_AC* inBlock);	private:	long fID;};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/25/96	[1345531] Added missing includes for noload buids		06/10/96	Added comments. Changed DoIt to DoAction. Added two new							action classes.		06/05/96	Initial Implementation*/
//----------------------------------------------------------------------------------------// MTidyHeapAllocator_AC.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/02/01	Update copyright.		03/24/99	Update copyright notice.		11/12/98	[2279231] Include TidyHeapSwitches.h for Export_AC.		10/06/98	Need ConditionalMacros_AC for noload build. */#ifndef __MTidyHeapAllocator_AC__#define __MTidyHeapAllocator_AC__// ACS#ifndef __TidyHeapSwitches_AC__#include "TidyHeapSwitches_AC.h"#endif#ifndef __Types_AC__#include "Types_AC.h"#endif// Toolbox#ifndef __STDDEF__#include <stddef.h>#endif//----------------------------------------------------------------------------------------// class MTidyHeapAllocator_AC//----------------------------------------------------------------------------------------class EXPORT_AC MTidyHeapAllocator_AC{public:	MTidyHeapAllocator_AC() 	{}		virtual ~MTidyHeapAllocator_AC() 	{}	static void UseTempMem(bool doUse);		// This must be called before the first allocation, else it defaults		// to true.		static bool UseTempMem();	static long MaxMemory()		// return max memory used by tidyheap itself	{	return fgMaxMemory; }		static long MemTally()		// returns total (cumulative) mem used by tidyheap itself	{	return fgMemTally; }		static long CurMemory()		// return cur memory used by tidyheap itself	{	return fgCurMemory; }public:		static bool FirstTime();	void* operator new(size_t inSize);		void operator delete(void* inBlock);		void* operator new(size_t inSize, const char* /*inFileName*/, short /*inLine*/ ) // make it compatible with macro		{ return operator new(inSize); }private:	static bool fgUseTempMem;	static long fgMaxMemory;	static long fgMemTally;	static long fgCurMemory;	friend class CTidyHeap_AC;#if qDebugTidyHeap	static long fgAllocationCounter;#endif};#endif
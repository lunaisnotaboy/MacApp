//----------------------------------------------------------------------------------------// MDynamicPopups.cpp// Copyright © 1991-2001 by Apple Computer, Inc. All rights reserved. //----------------------------------------------------------------------------------------/*	Change History		01/02/01	Update copyright.		05/24/00	Include exception and scope exception.		12/08/99	Fixed file name in banner.  Add try block in main.		03/24/99	Update copyright notice.*/// DynamicPopups#ifndef __UApplicationDynamicPopups__#include "UApplicationDynamicPopups.h"#endif// MacApp#ifndef __UBUSYCURSOR__#include "UBusyCursor.h"#endif#ifndef __UDIALOG__#include "UDialog.h"#endif#ifndef __UERRORMGR__#include "UErrorMgr.h"#endif#ifndef __UGRIDVIEW__#include "UGridView.h"#endif#ifndef __UMACAPPGLOBALS__#include "UMacAppGlobals.h"#endif#ifndef __UMACAPPUTILITIES__#include "UMacAppUtilities.h"#endif#ifndef __UPRINTING__#include "UPrinting.h"#endif#ifndef __UBACKGROUNDADORNER__#include "UBackgroundAdorner.h"#endif// ANSI#include <exception>//----------------------------------------------------------------------------------------// main: //----------------------------------------------------------------------------------------void main(){	try	{		InitUMacApp(4);										// Initialize MacApp with 4 calls to MoreMasters		TBusyCursor::InitUBusyCursor();						// Optionally, install the auto busy cursor		InitUPrinting();									// Initialize the printing gear		InitUTEView();										// Initialize the text editing unit 		InitUDialog();										// Initialize the dialog unit		InitUGridView();									// Initialize the GridView unit				InitUBackgroundAdorner();				/*	Typically, you will have a few of your own initialization routines here			which are used to prevent dead code stripping. */		TApplicationDynamicPopups aApplicationDynamicPopups;	// Allocate application object		aApplicationDynamicPopups.Run();						// Well lets run it then!	}	catch(CException_AC& theException)	{		DoCatchMessageAndStop_AC(theException);		::BailOutAlert(theException);	}	catch(std::exception& ex)			{		DoCatchISOMessageAndStop_AC(ex);		::BailOutAlert(ex);	}	catch(...)	{		DoCatchForeignMessageAndStop_AC();		::BailOutAlert();	}}//----------------------------------------------------------------------------------------// End of MDynamicPopups.cp
// URegionPicture.h// Copyright © 1999-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		12/19/99	Add the link dialog. 		12/07/99	Add SetBackgroundColor. 		11/16/99	Add an EndLink method. 		09/24/99	Initial version. */#ifndef __URegionPicture__#define __URegionPicture__// MacApp#ifndef __UPICTURE__#include "UPicture.h"#endif// ACS#ifndef __CTempRegion_AC__#include "CTempRegion_AC.h"#endif// Toolbox// External//========================================================================================// TRegionPicture//========================================================================================class TRegionPicture : public TPicture{	MA_DECLARE_CLASS;public:	//------------------------------------------------------------------------------------	// Static	//------------------------------------------------------------------------------------	static void Register();		// Register TRegionPicture so it can be created by name. 	//------------------------------------------------------------------------------------	// Initialize and Free	//------------------------------------------------------------------------------------	TRegionPicture(const CViewPoint& itsLocation = gZeroVPt,				   const CViewPoint& itsSize = gZeroVPt,				   SizeDeterminer itsHSizeDet = sizeVariable,				   SizeDeterminer itsVSizeDet = sizeVariable,				   ResNumber itsRsrcID = kNoResource);		// Constructor. 	TRegionPicture(const TRegionPicture& inPicture);		// Copy constructor. 	virtual ~TRegionPicture();		// Destructor. 	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		// Compute the shape region from the mask picture specified in fUserArea. 		//------------------------------------------------------------------------------------	// Data member accessors.	//------------------------------------------------------------------------------------	virtual void SetShape(ResNumber itsRsrcID);		// Computes the shape region from the mask picture with the specified ID. 	virtual void SetShape(PicHandle maskPict);		// Computes the shape region from the mask picture. 	virtual void SetShape(RgnHandle theShape);		// Set the shape to the specified region. 	//------------------------------------------------------------------------------------	// Miscellaneous	//------------------------------------------------------------------------------------	virtual void GetExtentRegion(RgnHandle itsExtentRgn) const;		// Use the shape for the view's extent region. 	//------------------------------------------------------------------------------------	// Data	//------------------------------------------------------------------------------------protected:	CTempRegion_AC		fShape;};#endif // __URegionPicture__
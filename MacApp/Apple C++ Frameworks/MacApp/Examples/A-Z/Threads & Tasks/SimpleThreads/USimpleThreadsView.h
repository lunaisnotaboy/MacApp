// USimpleThreadsView.h// Copyright © 1991-2001 by Apple Computer, Inc. All rights reserved. /*	Change History		01/02/01	Update copyright.		01/31/00	Don't have to allocate CThreadRunner on the heap. 		11/09/99	Change the API for GetContextualMenu so it has access to the click					event and can fill in all of the parameters for the menu. 		03/24/99	Update copyright notice.		01/15/99	CAEDesc_AC is now in ACS. */#ifndef __USIMPLETHREADSVIEW__#define __USIMPLETHREADSVIEW__// MacApp#ifndef __UVIEW__#include "UView.h"#endif// ACS#ifndef __CThreadRunner_AC__#include "CThreadRunner_AC.h"#endif// Externalclass TSimpleThread;//========================================================================================// TSimpleThreadsView//========================================================================================class TSimpleThreadsView: public TView{	MA_DECLARE_CLASS;	  public:	TSimpleThreadsView(const CViewPoint& itsLocation = gZeroVPt,					   const CViewPoint& itsSize = gZeroVPt); 		// Constructor		TSimpleThreadsView(const TSimpleThreadsView& from); 		// Copy constructor		virtual ~TSimpleThreadsView();		// Destructor		virtual void DoPostCreate(TDocument* itsDocument); // Override						   	// Drawing	virtual void Draw(const CViewRect& area); // Override		// Commands	virtual void DoEvent(EventNumber eventNumber, TEventHandler* source, TEvent* event); // Override			// Thread example code		void StartThreadExample();		// Creates a thread and runs it, storing its thread runner in fThreadRunner.			void StopThreadExample();		// Stops fThreadRunner by requesting a shutdown and joining the thread (waiting for it to stop).		void DrawSpirograph(int& iter);		// Draws a portion of a spirograph.#if qWantsContextualMenus	// Commands	virtual void  DoSetupMenus();	virtual void  DoMenuCommand(CommandNumber aCommandNumber);			virtual void GetContextualMenu(const CViewPoint& theMouse,								   TToolboxEvent* event,								   CContextualMenu& contextualMenu);	// Contextual menu example code#endif  private:	CThreadRunner_AC fThreadRunner;		// Runner for the thread that is calling DrawSpirograph.};//----------------------------------------------------------------------------------------// Global initialization procedure//----------------------------------------------------------------------------------------extern void InitUSimpleThreadsView();	// Call this routine at initialization time#endif // __USIMPLETHREADSVIEW__
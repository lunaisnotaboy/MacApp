// ULinkBehavior.h// Copyright © 1999-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		12/19/99	Add the link dialog. 		12/07/99	Add SetBackgroundColor. 		11/16/99	Add an EndLink method. 		09/24/99	Initial version. */#ifndef __ULinkBehavior__#define __ULinkBehavior__// MacApp#ifndef __UBEHAVIOR__#include "UBehavior.h"#endif#ifndef __UWECOMMANDS__#include "UWECommands.h"#endif// ACS// Externalclass TWEView;//========================================================================================// class TLinkBehavior//========================================================================================class TLinkBehavior : public TBehavior{	MA_DECLARE_CLASS;public:	//------------------------------------------------------------------------------------	// Static	//------------------------------------------------------------------------------------	static void Initialize();		// Initialize LinkBehavior unit.	//------------------------------------------------------------------------------------	// Initialize and Free	//------------------------------------------------------------------------------------	TLinkBehavior(IDType itsIdentifier = kNoIdentifier);		// Constructor	TLinkBehavior(const TLinkBehavior& inLinkBehavior);		// Copy constructor	virtual ~TLinkBehavior();		// Destructor	//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	enum { kStandardSignature = 'Blnk' };		// The standard signature for this class. 	virtual IDType GetStandardSignature() const; // override		// Returns this class's standard signature.	//------------------------------------------------------------------------------------	//  Commands and Menus	//------------------------------------------------------------------------------------	virtual void DoSetupMenus(); // override		// Sets up menus	virtual void DoMenuCommand(CommandNumber aCommandNumber); // override		// Handles menu commands	virtual char* PoseLinkDialog();	TWEView* GetOwner() const;		// Get the owning view. 	WEReference GetWEReference() const;		// Get the owning view's WASTE reference. };//========================================================================================// TLinkCommand//========================================================================================class TLinkCommand : public TWEStylerCommand// TLinkCommand applies a new styler to the WEView.{public:	//------------------------------------------------------------------------------------	// Initialization	//------------------------------------------------------------------------------------	TLinkCommand(TWEView* itsView, char* itsLink, CommandNumber itsCommandNumber);		// Initialize the command; if unsuccessful, signalled by Failure mechanism	virtual ~TLinkCommand();	virtual WEStylerReference GetStyler();		// Makes a new styler if the command doesn't already have one. 	virtual char* GetLink();	//------------------------------------------------------------------------------------	// Data Members	//------------------------------------------------------------------------------------public:	char*	fLink;};#endif // __ULinkBehavior__
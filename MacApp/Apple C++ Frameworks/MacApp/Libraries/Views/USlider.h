// USlider.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved. // Portions Copyright ©1997 by Trygve Isaacson./*	Change History		01/02/01	Update copyright.		12/21/00	Conditionalize controls based on whether they require the					Appearance Manager and/or an emulated control. 		11/04/00	TLabelsSlider is emulated, so it is not compatible with a					toolbox slider. 		07/26/99	Made TLabelsSlider functions public and declare the class.		03/24/99	Update copyright notice.*/#ifndef __USLIDER__#define __USLIDER__// MacApp#ifndef __UCONTROL__#include "UControl.h"#endif#if qAppearance || qEmulateStandardControls//----------------------------------------------------------------------------------------// Global functions.//----------------------------------------------------------------------------------------EXPORT_MA void InitUSlider();#if qEmulateStandardControlsEXPORT_MA void InitULabelsSlider();#endif//----------------------------------------------------------------------------------------// TSlider//----------------------------------------------------------------------------------------class EXPORT_MA TSlider : public TCtlMgr// TCtlMgr subclass. To set/get the value, use GetValue and SetValue. // When instantiated from a view resource, an additional field is used// as the resource ID of an 'STR#' resource// that contains the slider labels; in this case, the range is// automatically set to match the number of labels, and the slider// will have a pointy indicator. If the ID is zero or invalid, the// slider will be rectangular.{MA_DECLARE_CLASS;public:	// TControl-compatible constructor	TSlider(const CViewPoint& itsLocation = gZeroVPt,							 const CViewPoint& itsSize = gZeroVPt,							 SizeDeterminer itsHSizeDet = sizeVariable,							 SizeDeterminer itsVSizeDet = sizeVariable,						     const CTextStyle_AC& itsTextStyle = gSystemStyle,						     ResNumber itsLabelsID = kNoResource,						     bool preferLiveFeedback = true);	TSlider(const TSlider& inSlider);		// Copy Constructor	virtual ~TSlider();	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);	virtual void WriteFields(CStream_AC* aStream) const;	//------------------------------------------------------------------------------------	// MacApp overrides.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;		// Return the signature of this view.					virtual void DoControlAction(ControlPartCode partCode);		// Notifier	callback		virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled,									const CStr255_AC& itsTitle);		// Factory method instantiates the control.	void SetPreferLiveFeedback(bool preferLiveFeedback);		// Changes live feedback setting.  Requires deleting and re-creating the control.		// Use this method as a model for implementing other features that are only		// available at the time of creating the control.		protected:	ResNumber 			fLabelsID;		// String list resource containing labels for this slider.		bool 				fRightJustify;	// Text justification for this slider. Only used by										// emulated control.		ViewCoordinate		fInitialVal;	// The control's initial value in view coordinate										// units. You must use SetValue to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetValue.	ViewCoordinate		fInitialMin;	// The control's minimum value in view coordinate										// units. You must use SetMinimum to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetMinimum.	ViewCoordinate		fInitialMax;	// The control's maximum value in view coordinate										// units. You must use SetMaximum to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetMaximum.	ViewCoordinate 		fInitialViewSize;										// The control's view (page) value in view coordinate										// units. You must use SetViewSize to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetViewSize.		// New fields for the slider.	bool 			fPreferLiveFeedback;// whether live feedback is used.};//----------------------------------------------------------------------------------------// TSlider//----------------------------------------------------------------------------------------#if qEmulateStandardControlsclass TLabelsSlider : public TSlider// Emulation-only subclass of TSlider that supports drawing labels associated with// slider tick marks.{MA_DECLARE_CLASS;public:	// TControl-compatible constructor	TLabelsSlider(const CViewPoint& itsLocation = gZeroVPt,							 const CViewPoint& itsSize = gZeroVPt,							 SizeDeterminer itsHSizeDet = sizeVariable,							 SizeDeterminer itsVSizeDet = sizeVariable,						     const CTextStyle_AC& itsTextStyle = gSystemStyle,						     ResNumber itsLabelsID = kNoResource,						     bool preferLiveFeedback = true);	virtual ~TLabelsSlider();		TLabelsSlider(const TLabelsSlider& inSlider);		virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled,									const CStr255_AC& itsTitle);		// Factory method instantiates the control.	};#endif // qEmulateStandardControls#endif // qAppearance || qEmulateStandardControls#endif // __USLIDER__
// UProgressIndicator.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved. // Portions Copyright ©1997 by Trygve Isaacson./*	Change History		01/02/01	Update copyright.		12/21/00	Conditionalize controls based on whether they require the					Appearance Manager and/or an emulated control. 		03/24/99	Update copyright notice.*/#ifndef __UPROGRESSINDICATOR__#define __UPROGRESSINDICATOR__// MacApp#ifndef __UCONTROL__#include "UControl.h"#endif#if qAppearance || qEmulateStandardControlsEXPORT_MA  void InitUProgressIndicator();//----------------------------------------------------------------------------------------// TProgressIndicator// TControl subclass. Access the CProgressIndicator_AC directly. For// determinate progress, set the min/max range, and then call SetValue// or Increment repeatedly; you can also update the origin value if// desired. For indeterminate progress, set min=max and then call Animate// repeatedly.//----------------------------------------------------------------------------------------class EXPORT_MA TProgressIndicator : public TCtlMgr{MA_DECLARE_CLASS;public:	// TControl-compatible constructor	TProgressIndicator(const CViewPoint& itsLocation = gZeroVPt,							 const CViewPoint& itsSize = gZeroVPt,							 SizeDeterminer itsHSizeDet = sizeVariable,							 SizeDeterminer itsVSizeDet = sizeVariable,							 ViewCoordinate itsVal = 0,							 ViewCoordinate itsMin = 0,							 ViewCoordinate itsMax = 0,							 bool isDeterminate = true);	TProgressIndicator(const TProgressIndicator& inProgressIndicator);		// Copy Constructor	virtual ~TProgressIndicator();	virtual IDType GetStandardSignature() const;		// Return the signature of this view.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);	virtual void WriteFields(CStream_AC* aStream) const;	virtual void CreateControl();		//------------------------------------------------------------------------------------	// MacApp overrides.	//------------------------------------------------------------------------------------	virtual bool DoIdle(IdlePhase phase);			virtual void SetDeterminate(bool isDeterminate);		// Sets the determinate state of the control, adding or removing an idler behavior.			virtual bool IsDeterminate() const;		// Returns whether this control is determinate.		inline void Increment(SInt32 increment = 1, bool redraw = kRedraw) { SetValue(GetValue() + increment, redraw); }		// Increment progress indicator by 'increment'.	// For an indeterminate progress indicator, you can use these functions	// to set it running or stop it. It will create an idler behavior to	// ensure it gets idle time.	inline void StartAutoAnimate() { SetDeterminate(false); }	inline void StopAutoAnimate() { SetDeterminate(true); }		#if qBackwardCompatible	inline void SetState(bool isIndeterminate) { SetDeterminate(!isIndeterminate); }		// SetState is an obsolete synonym for SetDeterminate.#endif		virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled, 									const CStr255_AC& itsTitle);		// Factory method instantiates the control.protected:	ViewCoordinate		fInitialVal;	// The control's initial value in view coordinate										// units. You must use SetValue to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetValue.	ViewCoordinate		fInitialMin;	// The control's minimum value in view coordinate										// units. You must use SetMinimum to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetMinimum.	ViewCoordinate		fInitialMax;	// The control's maximum value in view coordinate										// units. You must use SetMaximum to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetMaximum.	bool fInitialDeterminate;			// Whether this is a determinate indicator.};#endif // qAppearance || qEmulateStandardControls#endif // __UPROGRESSINDICATOR__
// UIconButton.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved. // Portions Copyright ©1997 by Trygve Isaacson./*	Change History		01/02/01	Update copyright.		12/21/00	Conditionalize controls based on whether they require the					Appearance Manager and/or an emulated control. 		11/05/00	Don't allow the minimum to ever be set > 0 for a popup button. 					Override SetMinimum instead of CreateControl. 		09/23/00	Override IsDefault.  Remove unnecessary inline keywords.		07/31/00	[W] Re-enable TIconPopup for Windows.		06/05/00	Added override of CreateControl.		02/22/00	TIconPopup not available for Windows.		07/06/99	Made functions of TIconButton, TIconCheckBox, TIconPopup and					TIconRadio public.		03/24/99	Update copyright notice.*/#ifndef __UICONBUTTON__#define __UICONBUTTON__// MacApp#ifndef __UNUMBERTEXT__#include "UNumberText.h"#endif#ifndef __UCLUSTER__#include "UCluster.h"#endif#ifndef __UBUTTON__#include "UButton.h"#endif#ifndef __UCHECKBOX__#include "UCheckBox.h"#endif#ifndef __URADIO__#include "URadio.h"#endif#ifndef __UPOPUP__#include "UPopup.h"#endif#if qAppearance || qEmulateStandardControlsEXPORT_MA void InitUIconButton();	// Registers all of the icon button views.	// If you don't want to register all of them, call REGISTER_SIGNATURE_AC	// specifically for each one you need.//----------------------------------------------------------------------------------------// TIconButton//----------------------------------------------------------------------------------------class EXPORT_MA TIconButton : public TButton// TButton replacement subclass that implements an icon button that behaves// like a TButton -- you click it and it sends its fEventNumber. When// instantiated from a view resource, the resource is used to obtain the// button's icon ID. You can also set the content type to cicn or PICT.{	MA_DECLARE_CLASS;public:	// TButton-compatible constructor	TIconButton(const CViewPoint& itsLocation = gZeroVPt,							const CViewPoint& itsSize = gZeroVPt,							SizeDeterminer itsHSizeDet = sizeVariable,							SizeDeterminer itsVSizeDet = sizeVariable,							const CStr255_AC& itsTitle = gEmptyString,							ResNumber itsRsrcID = kNoResource,							ControlContentType contentType = kControlContentIconSuiteRes);	virtual ~TIconButton();	virtual IDType GetStandardSignature() const;	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;	virtual void SetContentRsrcID(ControlContentType contentType, ResNumber rsrcID);		// Sets the content type and resource ID.	virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled, 									const CStr255_AC& itsTitle);		// Factory method instantiates the control.	//------------------------------------------------------------------------------------	// Accessors	//------------------------------------------------------------------------------------		ResNumber GetRsrcID() const { return fRsrcID; }	ControlContentType GetContentType() const { return fContentType; }	bool IsDefault() { return false; }protected:	ResNumber fRsrcID;	ControlContentType fContentType;};//----------------------------------------------------------------------------------------// TIconCheckBox//----------------------------------------------------------------------------------------class EXPORT_MA TIconCheckBox : public TCheckBox// TCheckBox replacement subclass that implements an icon button that behaves// like a check box -- you click it and it toggles its on/off state. When// instantiated from a view resource, the fUserArea is used to obtain the// button's icon ID. You can also set the content type to cicn or PICT.// If you want the on/off states to have different icons, you must subclass// and create only the emulated icon class in an override of DoMakeControl.{	MA_DECLARE_CLASS;public:	// TCheckBox-compatible constructor	TIconCheckBox(const CViewPoint& itsLocation = gZeroVPt,							const CViewPoint& itsSize = gZeroVPt,							SizeDeterminer itsHSizeDet = sizeVariable,							SizeDeterminer itsVSizeDet = sizeVariable,							const CStr255_AC& itsTitle = gEmptyString,							SInt16 isTurnedOn = kControlCheckBoxUncheckedValue,							ResNumber itsRsrcID = kNoResource,							ControlContentType contentType = kControlContentIconSuiteRes);	virtual ~TIconCheckBox();	virtual IDType GetStandardSignature() const;		// Return the signature of this view.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;		virtual void SetContentRsrcID(ControlContentType contentType, ResNumber rsrcID);		// Sets the content type and resource ID.		virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled, 									const CStr255_AC& itsTitle);		// Factory method instantiates the control.	//------------------------------------------------------------------------------------	// Accessors	//------------------------------------------------------------------------------------		ResNumber GetRsrcID() const { return fRsrcID; }	ControlContentType GetContentType() const { return fContentType; }protected:	ResNumber fRsrcID;	ControlContentType fContentType;};//----------------------------------------------------------------------------------------// TIconRadio//----------------------------------------------------------------------------------------class EXPORT_MA TIconRadio : public TRadio// TRadio replacement subclass that implements an icon button that behaves// like a radio button -- you click it and it turns on, and notifies its// cluster so that the others in its group are turned off. When// instantiated from a view resource, the fUserArea is used to obtain the// button's icon ID.  You can also set the content type to cicn or PICT.// If you want the on/off states to have different icons, you must subclass// and create only the emulated icon class in an override of DoMakeControl.{	MA_DECLARE_CLASS;public:	// TRadio-compatible constructor	TIconRadio(const CViewPoint& itsLocation = gZeroVPt,							const CViewPoint& itsSize = gZeroVPt,							SizeDeterminer itsHSizeDet = sizeVariable,							SizeDeterminer itsVSizeDet = sizeVariable,							const CStr255_AC& itsTitle = gEmptyString,							SInt16 isTurnedOn = kControlRadioButtonUncheckedValue,							ResNumber itsRsrcID = kNoResource,							ControlContentType contentType = kControlContentIconSuiteRes);	virtual ~TIconRadio();	virtual IDType GetStandardSignature() const;		// Return the signature of this view.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;		virtual void SetContentRsrcID(ControlContentType contentType, ResNumber rsrcID);		// Sets the content type and resource ID.	virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled, 									const CStr255_AC& itsTitle);		// Factory method instantiates the control.	//------------------------------------------------------------------------------------	// Accessors	//------------------------------------------------------------------------------------		ResNumber GetRsrcID() const { return fRsrcID; }	ControlContentType GetContentType() const { return fContentType; }protected:	ResNumber fRsrcID;	ControlContentType fContentType;};//----------------------------------------------------------------------------------------// TIconPopup//----------------------------------------------------------------------------------------class EXPORT_MA TIconPopup : public TPopup// TPopup replacement subclass that implements an icon button that behaves// like a popup -- you click it and it displays a popup menu. When// instantiated from a view resource, the fUserArea is used to obtain the// button's icon ID. You can also set the content type to cicn or PICT.// If you want the on/off states to have different icons, you must subclass// and create only the emulated icon class in an override of DoMakeControl.{	MA_DECLARE_CLASS;public:	// TPopup-compatible constructor	TIconPopup(const CViewPoint& itsLocation = gZeroVPt,							const CViewPoint& itsSize = gZeroVPt,							SizeDeterminer itsHSizeDet = sizeVariable,							SizeDeterminer itsVSizeDet = sizeVariable,							short itsMenuID = kNoResource,				// rsrc ID of 'MENU' or 'CMNU' rsrc 							short itsCurrentItem = 0,							short itsItemOffset = 0,					// width of the popup title area 							short itsStrListID = kNoResource,			// popup's title:	STR# rsrc id							short itsIndex = kNoResource,				//					index into STR#							/* Style */ short itsStyle = popupTitleNoStyle, // title's style 							short itsJust= popupTitleLeftJust,			// title's justification 							bool useAddResMenu = false,					// true if want to AddResMenu (can use																		// this option iff menuID != kNoResource)							ResType useAddResMenuResType = kNoIdentifier, // used if useAddResMenu is true							const CTextStyle_AC& itsTextStyle = gSystemStyle, // popup's text style							ResNumber itsRsrcID = kNoResource,							ControlContentType contentType = kControlContentIconSuiteRes);		// Initializes the popup.	virtual ~TIconPopup();	virtual IDType GetStandardSignature() const;		// Return the signature of this view.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;	virtual void SetContentRsrcID(ControlContentType contentType, ResNumber rsrcID);		// Sets the content type and resource ID.	virtual CControl_AC* DoMakeControl(WindowRef theWindow,									const CRect_AC& boundsRect, 									bool isVisible, bool isEnabled, 									const CStr255_AC& itsTitle);		// Factory method instantiates the control.	virtual ViewCoordinate GetValue() const;		// Override to get the current item instead of the button state.		virtual void SetValue(ViewCoordinate newVal, bool redraw);		// Override to set the current item instead of the button state.	virtual void SetValues(ViewCoordinate itsVal,						   ViewCoordinate itsMin,						   ViewCoordinate itsMax,						   bool redraw);		// Override to set the current item instead of the button state.		virtual void SetMinimum(ViewCoordinate itsMin, bool redraw);		// Override to not mess with the minimum. 		virtual short GetState() const;		// Get the button state. 	virtual void SetState(short newState, bool redraw);		// Set the button state.			//------------------------------------------------------------------------------------	// Accessors	//------------------------------------------------------------------------------------		ResNumber GetRsrcID() const { return fRsrcID; }	ControlContentType GetContentType() const { return fContentType; }protected:	ResNumber fRsrcID;	ControlContentType fContentType;};#endif // qAppearance || qEmulateStandardControls#endif // __UICONBUTTON__
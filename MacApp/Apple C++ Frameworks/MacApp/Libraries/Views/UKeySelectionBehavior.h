// UKeySelectionBehavior.h // Copyright © 1985-2001 by Apple Computer. All rights reserved. /*	Change History		01/02/01	Update copyright.		11/09/00	Ticks are unsigned. 		03/24/99	Update copyright notice.*/#ifndef __UKEYSELECTIONBEHAVIOR__#define __UKEYSELECTIONBEHAVIOR__// MacApp#ifndef __UBEHAVIOR__#include "UBehavior.h"#endif//----------------------------------------------------------------------------------------// class TKeySelectionBehavior// The TKeySelectionBehavior class provides item selection that// allows the user to type just enough keys to identify an item// uniquely. For example, you can use this behavior to allow users// to select a name from a list of names by typing only the first// few characters. A timeout interval specifies the maximum time// between keystrokes for them to be considered part of the same// selection sequence.//    This behavior is typically attached to a TTextListView object,// which displays a one-dimensional (vertical) list of text strings.// When the user types characters, the characters are collected// by this behavior and forwarded to the event handler's DoKeySelection// method. If the event handler is a TTextListView object, the view// is scrolled, if necessary, and the first partially matched item// is selected.//----------------------------------------------------------------------------------------class EXPORT_MA TKeySelectionBehavior : public TBehavior// The TKeySelectionBehavior class provides item selection that// allows the user to type just enough keys to identify an item// uniquely. For example, you can use this behavior to allow users// to select a name from a list of names by typing only the first// few characters. A timeout interval specifies the maximum time// between keystrokes for them to be considered part of the same// selection sequence.//    This behavior is typically attached to a TTextListView object,// which displays a one-dimensional (vertical) list of text strings.// When the user types characters, the characters are collected// by this behavior and forwarded to the event handler's DoKeySelection// method. If the event handler is a TTextListView object, the view// is scrolled, if necessary, and the first partially matched item// is selected.{	MA_DECLARE_CLASS;	public: // member functions	TKeySelectionBehavior(IDType itsIdentifier = kNoIdentifier);		// Constructor	virtual ~TKeySelectionBehavior();		// Destructor			virtual void DoKeyEvent(TToolboxEvent* event);		// Accumulate the keystrokes.	virtual void DoKeySelection(const CStr255_AC& keySequence);		// Called with a the current sequence of characters that have been typed in        // 'keySequence'. Derived classes can override. Default action is to called        // 'DoKeySelection' on the fOwner field of this behavior.			virtual void DoKeyUp(TToolboxEvent* event);		// Set time of keyUp events for timing out and reseting the CPascalStr_AC of keys hit.					  	virtual void SetTimeOutInterval(long timeOutInterval);		// Change the default time out interval. Default is the value of auto-key        // threshold.protected: // fields	CStr255_AC fKeySequence;	long fTimeOutInterval;	unsigned long fLastKeyUpTime;};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		04/30/96	Make all constructors use initialization lists if they weren't already							doing do. Switch several more empty constructors to inlines.		----- R10 -----		----- 3.3b1 -----		09/01/95	private -> protected.		  7/5/95	Cosmetic. 		 3/13/95	Change Free methods to destructors. */
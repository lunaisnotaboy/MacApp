// UDateTime.h// Copyright © 1998-2001 by Apple Computer, Inc. All rights reserved. /*	Change History		01/02/01	Update copyright.		12/21/00	Conditionalize controls based on whether they require the					Appearance Manager and/or an emulated control. 		12/08/99	Initialize longDate to CLongDate_AC() for compilers					lacking long long support.		03/24/99	Update copyright notice.*/#ifndef __UDATETIME__#define __UDATETIME__// MacApp#ifndef __UCONTROL__#include "UControl.h"#endif#if qAppearance// ACS#ifndef __DateTimeUtils_AC__#include "DateTimeUtils_AC.h"#endifenum{	kStdDateControl = 'date',	kStdTimeControl = 'time'};//----------------------------------------------------------------------------------------// TDateControl//----------------------------------------------------------------------------------------class TDateControl : public TCtlMgr{	MA_DECLARE_CLASS;  public:	TDateControl(const CViewPoint& itsLocation = gZeroVPt,				 const CViewPoint& itsSize = gZeroVPt,				 SizeDeterminer itsHSizeDet = sizeVariable,				 SizeDeterminer itsVSizeDet = sizeVariable,				 const CTextStyle_AC& itsTextStyle = gSystemStyle,				 const CLongDate_AC& longDate = CLongDate_AC(),				 bool showDays = true, 				 short clockFlags = kControlClockNoFlags); 		// Constructor		TDateControl(const TDateControl& inDateControl);		// Copy constructor			virtual ~TDateControl();		// Destructor	virtual IDType GetStandardSignature() const; // Override		//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;	virtual CControl_AC* DoMakeControl(WindowRef theWindow, 									   const CRect_AC& boundsRect, 									   bool isVisible, bool isEnabled, 									   const CStr255_AC& itsTitle); // Override		// Date methods.		void SetDate(const CLongDate_AC& longDate, bool redraw);	CLongDate_AC GetDate() const;	protected:  	ResNumber		fStrListID;						// date as string:	STR# rsrc id	short			fIndex;							//					index into STR#	CLongDate_AC	fDate;	   	bool			fShowDays;	short			fClockFlags;};//----------------------------------------------------------------------------------------// TTimeControl//----------------------------------------------------------------------------------------class TTimeControl : public TCtlMgr{	MA_DECLARE_CLASS;  public:	TTimeControl(const CViewPoint& itsLocation = gZeroVPt,				 const CViewPoint& itsSize = gZeroVPt,				 SizeDeterminer itsHSizeDet = sizeVariable,				 SizeDeterminer itsVSizeDet = sizeVariable,				 const CTextStyle_AC& itsTextStyle = gSystemStyle,				 const CLongDate_AC& longDate = CLongDate_AC(),					 bool showSeconds = false, 				 short clockFlags = kControlClockNoFlags); 		// Constructor		TTimeControl(const TTimeControl& inTimeControl);		// Copy constructor			virtual ~TTimeControl();		// Destructor	virtual IDType GetStandardSignature() const; // Override		//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;	virtual CControl_AC* DoMakeControl(WindowRef theWindow, 									   const CRect_AC& boundsRect, 									   bool isVisible, bool isEnabled, 									   const CStr255_AC& itsTitle); // Override	// Time methods.	void SetTime(const CLongDate_AC& longDate, bool redraw);	CLongDate_AC GetTime() const;	protected:	ResNumber		fStrListID;						// time as string:	STR# rsrc id	short			fIndex;							//					index into STR#	CLongDate_AC	fDate;	   	bool			fShowSeconds;	short			fClockFlags;};//----------------------------------------------------------------------------------------// Global initialization procedure//----------------------------------------------------------------------------------------extern void InitDateControl();extern void InitTimeControl();	// Call this routine at initialization time#endif // qAppearance#endif // __UDATETIME__
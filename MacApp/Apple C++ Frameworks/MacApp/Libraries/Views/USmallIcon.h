//----------------------------------------------------------------------------------------// USmallIcon.h// Copyright © 1988-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/02/01	Update copyright.		11/11/00	Cosmetic.		08/28/00	Added embedded documentation.		03/24/99	Update copyright notice.*/#ifndef __USMALLICON__#define __USMALLICON__#ifndef __UICON__#include "UIcon.h"#endif//----------------------------------------------------------------------------------------// TSmallIcon// //	This class now inherits from the revamped TIcon class, which uses the IconSuite toolbox// 	stuff. This class is here for compatibility reasons only. The only difference between//  this class and the new TIcon is the old TSmallIcon interface remains, and by default// 	it tells TIcon to only load the small icons by default.//----------------------------------------------------------------------------------------class EXPORT_MA TSmallIcon : public TIcon// The TSmallIcon class is a subclass of class TIcon that, by default,// loads only the small icons from the icon suite into memory. TSmallIcon// objects can serve as a simple form of button if enabled, but// this feature is disabled in the default initialization method.// The TSmallIcon class implements an icon that is smaller than the one// implemented in the TIcon class. TSmallIcon objects can serve as a simple// form of button if enabled, but this feature is disabled in the default// initialization method.// As members of a subclass of TControl, TSmallIcon objects always track the// mouse and highlight automatically if the mouse pointer strays within their// active control area. TSmallIcon objects also share the standard properties of// other Macintosh small icons: they are created from resources of type 'SICN', they// are 16 x 16 bits in size, and they can be moved or resized by the application.{	MA_DECLARE_CLASS;	public:	TSmallIcon(const CViewPoint&	itsLocation = gZeroVPt,			   const CViewPoint&	itsSize = gZeroVPt,			   SizeDeterminer		itsHSizeDet = sizeVariable,			   SizeDeterminer		itsVSizeDet = sizeVariable,			   ResNumber			itsRsrcID = kNoResource,			   eIconSelectorFlag	selector = kAllSmallData);		// Constructor	virtual ~TSmallIcon();		// Destructor			//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;	// Override 		// Returns this class's standard signature.	//------------------------------------------------------------------------------------	// Data member accessors.	//------------------------------------------------------------------------------------	virtual void ReleaseSmallIcon();	virtual void SetSmallIcon(ResNumber				theSmallIcon, 							  bool					redraw,							  eIconSelectorFlag		selector = kAllSmallData);	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;};EXPORT_MA  void InitUSmallIcon();#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/18/96	Made it inherit from TIcon. Removed obsolete functions handled							correctly by TIcon. Removed Data already in TIcon. Left functions							that exist correctly in TIcon so it remains compatible with							previous implementation.		04/17/96	InitUSmallIcon		04/15/96	Phase 1 of View Breakup changes		----- Release 10 -----*/
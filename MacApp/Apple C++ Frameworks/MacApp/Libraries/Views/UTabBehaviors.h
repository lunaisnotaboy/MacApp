// UTabBehaviors.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		03/24/99	Update copyright notice.*/#ifndef __UTABBEHAVIORS__#define __UTABBEHAVIORS__// MacApp#ifndef __UBEHAVIOR__#include "UBehavior.h"#endif//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class TView;//----------------------------------------------------------------------------------------// TTabber//----------------------------------------------------------------------------------------// An abstract parent class for tabbing behaviors which intercept the Tab key event// and find the next view which wishes to become the targetconst IDType kTabBehavior = 'tabb';	// Behavior identifierclass EXPORT_MA TTabber : public TBehavior// TTabber is an abstract class that implements a behavior that// responds to the Tab key being pressed. MacApp provides two subclasses// of TTabber: TMultiWindowTabber handles tabbing within a window// and across windows; TViewTabber handles tabbing within a view// or window but does not allow tabbing across windows. By default,// MacApp creates an instance of TMultiWindowTabber for the application.// To suppress multiwindow tabbing, you can associate a TViewTabber// instance with a view or window.{	MA_DECLARE_CLASS;	public:	//------------------------------------------------------------------------------------	// Constructor	//------------------------------------------------------------------------------------	TTabber(bool recursive = true);		// Constructor	virtual ~TTabber();		// Destructor			//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFrom(CStream_AC* aStream);	virtual void WriteTo(CStream_AC* aStream) const;	//------------------------------------------------------------------------------------	// Event handling.	//------------------------------------------------------------------------------------	virtual void DoKeyEvent(TToolboxEvent* event);	virtual void Tab(bool tabBackward);	virtual void Reset();	virtual void FindSubViewTargets(TView* parent,										   bool tabBackward);	virtual void FindTargets(bool tabBackward);	virtual IDType GetStandardSignature() const;		// Return the standard signature for this classpublic:	TView*	fNext;			// The view which becomes the new target.	TView*	fFirst;			// Save the first potential target (to do wraparound)	bool fRecursive;		// True if the search should include the entire view hierarchy							// false if it should be limited to immediate subviews	bool fFoundCurrent;		// Set to true when the current target has been found};//----------------------------------------------------------------------------------------// TMultiWindowTabber//----------------------------------------------------------------------------------------// Attached to the application object. Handles tabbing across multiple active// windows (i.e. when windoids are present)class EXPORT_MA TMultiWindowTabber : public TTabber// TMultiWindowTabber is a concrete subclass of TTabber whose methods// allow the user to use the Tab key to move from window to window// when there are multiple active windows, such as tearoff menus.// This class is instantiated by the application to provide default// tab behavior. If you do not want to allow windowtowindow tabbing,// you can attach a TViewTabber behavior to a window, which prevents// the user from tabbing out of the window.{	MA_DECLARE_CLASS;	public:	//------------------------------------------------------------------------------------	// Constructor	//------------------------------------------------------------------------------------	inline TMultiWindowTabber(bool recursive = true) : TTabber(recursive) { }		// constructor	virtual ~TMultiWindowTabber();		// Destructor			//------------------------------------------------------------------------------------	// Event handling.	//------------------------------------------------------------------------------------	virtual void FindTargets(bool tabBackward);	// Override};//----------------------------------------------------------------------------------------// TViewTabber//----------------------------------------------------------------------------------------// Can be attached to any view to intercept the Tab key before it gets to the// MultiWindowTabber in the application class EXPORT_MA TViewTabber : public TTabber// The TViewTabber class handles tab keys in a way that prevents// tabbing across multiple active windows. Attach a TViewTabber// behavior to a window if you do not want the user to tab out of// the active window.{	MA_DECLARE_CLASS;	public:	//------------------------------------------------------------------------------------	// Constructor	//------------------------------------------------------------------------------------	inline TViewTabber(bool recursive = true) : TTabber(recursive) { }		// constructor	virtual ~TViewTabber();		// Destructor			//------------------------------------------------------------------------------------	// Event handling.	//------------------------------------------------------------------------------------	virtual void FindTargets(bool tabBackward);	// Override};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		10/11/95	Add TSectionBehavior::GetStandardSignature.		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. */
//----------------------------------------------------------------------------------------// UCluster.h // Copyright © 1988-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/24/01	Change cluster label to be a CString_AC, not a StringHandle.  It					is not fLabel, not fDataHandle.  Eliminated ReleaseLabel.		01/20/01	GetLabel and SetLabel now use CString_AC.  Include CString_AC.h.		01/02/01	Update copyright.		09/01/00	Stripped dead code.		07/26/99	Removed EraseFrameGap.  Added ClipOutGap.		03/24/99	Update copyright notice.		10/15/98	[2279094] Include Types_AC.h for definition of kNoIdentifier_AC.*/#ifndef __UCLUSTER__#define __UCLUSTER__// ACS#ifndef __Types_AC__#include "Types_AC.h"#endif#ifndef __CString_AC__#include "CString_AC.h"#endif// MacApp#ifndef __UCONTROL__#include "UControl.h"#endif// Toolbox#ifndef __APPEARANCE__#include <Appearance.h>#endif//----------------------------------------------------------------------------------------// CLASS TClusterBehavior//----------------------------------------------------------------------------------------class EXPORT_MA TClusterBehavior : public TBehavior// Provides a mechanism whereby radio buttons can be grouped in clusters, such// that when one of the buttons is selected the others are all deselected.{	MA_DECLARE_CLASS;	public:	TClusterBehavior(IDType itsIdentifier = kNoIdentifier);		// Constructor.			virtual ~TClusterBehavior();		// Destructor.	virtual void DoEvent(EventNumber eventNumber,						 TEventHandler* source,						 TEvent* event);		// Responsible for resetting the radio buttons so that the last selected one is        // the only one selected.	virtual IDType GetCurrentChoice();		// Returns the ID of the currently selected button.	virtual void SetCurrentChoice(IDType newChoice);		// Highlights the specified button, and turns off all others off.	//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;	// Override 		// Returns this class's standard signature.protected:	IDType		fCurrentChoice;		// The ID of the radio button that is currently selected.};//----------------------------------------------------------------------------------------// CLASS TCluster//----------------------------------------------------------------------------------------class EXPORT_MA TCluster : public TControl// The TCluster class implements a view that can be used to contain// controls, such as groups of radio buttons. TCluster is usually// used to localize the handling of logically grouped controls;// the default version of this class provides methods that can receive// an mRadioHit message from a subview, select the chosen control,// and deselect the others.//    TCluster can also be used to provide labeled adornments around// controls and other groups of dialog items.//    TCluster automatically adds a TClusterBehavior.{	MA_DECLARE_CLASS;	public:	TCluster(const CViewPoint& itsLocation = gZeroVPt,								 const CViewPoint& itsSize = gZeroVPt,								 SizeDeterminer itsHSizeDet = sizeVariable,								 SizeDeterminer itsVSizeDet = sizeVariable,								 ResNumber itsRsrcID = kNoResource,								 short itsIndex = kEmptySTRIndex);		// Constructor			TCluster(const TCluster& inCluster);		// Copy Constructor			virtual ~TCluster();		// Release the label and call Inherited::Free.	//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;	// Override 		// Returns this class's standard signature.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;	//------------------------------------------------------------------------------------	// Drawing.	//------------------------------------------------------------------------------------	virtual void Draw(const CViewRect& area);		// Frame the cluster if there is a label, then draw the label.		virtual bool IsDrawingBorder() const;		// Returns whether to draw a border for the cluster.		// The default is to draw if there is a label; if there is no label, no drawing will occur.  		// A new-style cluster (TPrimaryCluster or TSecondaryCluster) always returns true; 		// The base class TCluster returns false if the title string is empty, for backward compatibility.  	void DrawCluster();		// Draw cluster: border frame, and title gap and title, if any.			virtual void DrawFrame(const CRect_AC& boundsRect, ThemeDrawState state, SInt16 topIndent, SInt16 gapSize);		// Draw the frame as a primary group box.			SInt16 GetGapLeftInset(const CRect_AC& boundsRect, SInt16 gapSize);		// Get the gap left inset, based on actual runtime justification.	//------------------------------------------------------------------------------------	// Title and current choice.	//------------------------------------------------------------------------------------				virtual CString_AC GetLabel() const;		// Returns the text in the clusters label.	virtual void SetLabel(const CString_AC& theLabel, bool redraw);		// Sets the clusters label to be theLabel, redrawing if redraw is true.	virtual void SetLabelWithStrListID(ResNumber theStrListID, short theIndex, bool redraw);		// Set fDataHandle to the string specified by theStrListID and theIndex and sets fStrListID		// and fIndex accordingly.	virtual IDType GetCurrentChoice();		// Returns the ID of the currently selected button	virtual void SetCurrentChoice(IDType newChoice);		// Highlights the specified button, and turns off all others off		//------------------------------------------------------------------------------------	// Activation and mouser-ing.	//------------------------------------------------------------------------------------	virtual void Activate(bool entering);		// Override the TControl to do nothing.  We draw ourselves inactive.			virtual void DoMouseCommand(CViewPoint& theMouse, TToolboxEvent* event, CPoint_AC hysteresis);		// Override so that a TControlTracker isn't created.		protected:			CRegion_AC ClipOutGap(const CRect_AC& frameRect, SInt16 gapSize, SInt16 gapInset);		// Set the clip so that the title gap is omitted from drawing.	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------public:	CString_AC			fLabel;				// The label for the cluster.	TClusterBehavior*	fClusterBehavior;	// Manages the currently selected radio button.	ResNumber			fStrListID;			// The ID of the 'STR#' resource containing the label of the cluster in the position specified by the fIndex field.	short				fIndex;				// The index specifying the position occupied by the cluster's label in the 'STR#' resource specified by the fStrListID field.	SInt16				fTitleGap;			// Horizontal gap for other object in title.	bool				fWasActive;			// Last known active state.};//----------------------------------------------------------------------------------------// CLASS TPrimaryCluster//----------------------------------------------------------------------------------------class EXPORT_MA TPrimaryCluster : public TCluster// This class differs from TCluster in that it always draws a border even if the title is empty, // and title view's width may be specified for the title gap.{	MA_DECLARE_CLASS;public:		// TCluster-compatible constructor	TPrimaryCluster(const CViewPoint& itsLocation = gZeroVPt,								 const CViewPoint& itsSize = gZeroVPt,								 SizeDeterminer itsHSizeDet = sizeVariable,								 SizeDeterminer itsVSizeDet = sizeVariable,								 ResNumber itsRsrcID = kNoResource,								 short itsIndex = kEmptySTRIndex,								 IDType titleViewID = kNoIdentifier_AC);	virtual ~TPrimaryCluster();	virtual IDType GetStandardSignature() const;		virtual bool IsDrawingBorder() const;		// Returns whether to draw a border for the cluster.  		// A new-style cluster (TPrimaryCluster, TSecondaryCluster) always returns true.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		virtual void WriteFields(CStream_AC* aStream) const;	virtual void DoPostCreate(TDocument* itsDocument);		// Makes sure the title gap has been initialized.	void SetViewTitleGap(IDType titleViewID);		// Set the title gap to be the width of the specified view.		protected:		IDType	fTitleViewID;	// use to specify title gap. };//----------------------------------------------------------------------------------------// CLASS TSecondaryCluster//----------------------------------------------------------------------------------------class EXPORT_MA TSecondaryCluster : public TPrimaryCluster// This class differs from a TPrimaryCluster in that it draws its frame as a // secondary group box.{	MA_DECLARE_CLASS;public:		// TCluster-compatible constructor	TSecondaryCluster(const CViewPoint& itsLocation = gZeroVPt,								 const CViewPoint& itsSize = gZeroVPt,								 SizeDeterminer itsHSizeDet = sizeVariable,								 SizeDeterminer itsVSizeDet = sizeVariable,								 ResNumber itsRsrcID = kNoResource,								 short itsIndex = kEmptySTRIndex,								 IDType titleViewID = kNoIdentifier_AC);	virtual ~TSecondaryCluster();	virtual IDType GetStandardSignature() const;	virtual void DrawFrame(const CRect_AC& boundsRect, ThemeDrawState state, SInt16 topIndent, SInt16 gapSize);		// Draw the frame as a secondary group box.	};//----------------------------------------------------------------------------------------// GLOBALS//----------------------------------------------------------------------------------------EXPORT_MA  void InitUCluster(bool initPrimaryCluster = true, 							 bool initSecondaryCluster = true);	// Initialize this unit.  Optionally initialize primary and secondary cluster support.	// Note: the bool parameter defaults are likely to change from 'true' to 	// 'false' in a future release, and this function will then require explicitly 	// passing true.	#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		05/31/96	[1352846] Cluster behavior change. Added DimState and SetEnabled  							override to propigate the changes to to subviews.		04/17/96	InitUCluster		04/15/96	Phase 1 of View Breakup changes		----- Release 10 -----		*/
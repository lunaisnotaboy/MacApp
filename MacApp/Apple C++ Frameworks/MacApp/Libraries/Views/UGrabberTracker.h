// UGrabberTracker.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		03/24/99	Update copyright notice.*/#ifndef __UGRABBERTRACKER__#define __UGRABBERTRACKER__// MacApp#ifndef __UGEOMETRY__#include "UGeometry.h"#endif#ifndef __UTRACKER__#include "UTracker.h"#endif//----------------------------------------------------------------------------------------// Constants//----------------------------------------------------------------------------------------const ResNumber kGrabberHand = 9;					// ID of the grabberhand cursor//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class TDocument;class TView;class TScroller;//----------------------------------------------------------------------------------------// TGrabberTracker: Tracks a scrolling view with a MacPaint-like Grabber Hand//----------------------------------------------------------------------------------------class EXPORT_MA TGrabberTracker : public TTracker// The TGrabberTracker class contains methods that implement scrolling// in both directions at once (in the same fashion as MacPaint when// the user is working with the Hand tool).//    MacApp does not create or use grabber-tracker objects; they// are provided for your convenience. For an example of a TGrabberTracker// object, see the Calc sample program.{public:	TGrabberTracker(CommandNumber itsCommandNumber = cNoCommand,										TDocument* itsDocument = NULL,										TView* itsView = NULL,										TScroller* itsScroller = NULL,										const CViewPoint& itsMouse = gZeroVPt);		// constructor	virtual ~TGrabberTracker();		// Destructor			virtual TTracker* TrackMouse(TrackPhase aTrackPhase,										CViewPoint& anchorPoint,										CViewPoint& previousPoint,										CViewPoint& nextPoint,										bool mouseDidMove);	virtual void TrackFeedback(TrackPhase aTrackPhase,										const CViewPoint& anchorPoint,										const CViewPoint& previousPoint,										const CViewPoint& nextPoint,										bool mouseDidMove,										bool turnItOn);	virtual void AutoScroll(const CViewPoint& delta);};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		 12/8/95	Move TTracker from UCommand to UTracker. 		----- 3.3 -----		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. */
//----------------------------------------------------------------------------------------// UReporter.cpp// Copyright © 1985-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		03/01/01	Don't include Core_AC, Memory_AC. 		01/02/01	Update copyright.		11/01/00	[PB] Include MacErrors instead of Errors. 		03/24/99	Update copyright notice.*/#ifndef __UReporter__#include "UReporter.h"#endif// MacApp#ifndef __UDEBUG__#include "UDebug.h"#endif#ifndef __UCOREUTILITIES__#include "UCoreUtilities.h"#endif#ifndef __UTHEDEBUGGER__#include "UTheDebugger.h"#endif// ACS#ifndef __CPascalString_AC__#include "CPascalString_AC.h"#endif#ifndef __CReporter_AC__#include "CReporter_AC.h"#endif// Toolbox#ifndef __LOWMEM__#include <LowMem.h>#endif#ifndef __MACERRORS__#include <MacErrors.h>#endif#ifndef __MACMEMORY__#include <MacMemory.h>#endif#ifndef __RESOURCES__#include <Resources.h>#endif#ifndef __PROCESSES__#include <Processes.h>#endif// ANSI#ifndef __STDIO__#include <stdio.h>#endif#ifndef _STDLIB#include <stdlib.h>#endif//----------------------------------------------------------------------------------------// Class TReporter//----------------------------------------------------------------------------------------class TReporter : public CReporter_AC{public:	TReporter();	virtual ~TReporter();};static CAutoPtr_AC<TReporter> gMacAppReporter;void InitUReporter(){	gMacAppReporter = TH_new TReporter;}//----------------------------------------------------------------------------------------// TReporter ctor//----------------------------------------------------------------------------------------TReporter::TReporter(){	// What this odd looking code does is make sure that if a MacApp	// application makes a subclass of CReporter_AC, that it always	// ends up the current instance with this TReporter as the previous	// reporter. This will work despite unknown order of contructors getting	// called at static constructor time.	CReporter_AC* old = SetIsCurrentInstance();	if(old)	{		// if old is true, this means theres another installed reporter, probably		// installed by the MacApp client, so we wan't that to be the current 		// reporter. 			old->SetIsCurrentInstance();			// this will make this TReporter the previous reporter.	}			}//----------------------------------------------------------------------------------------// TReporter dtor//----------------------------------------------------------------------------------------TReporter::~TReporter(){}//----------------------------------------------------------------------------------------// End of UReporter.cpp
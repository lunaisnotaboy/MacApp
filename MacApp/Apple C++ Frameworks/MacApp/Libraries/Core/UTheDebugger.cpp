//----------------------------------------------------------------------------------------// UTheDebugger.cpp// Copyright й 1984-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/02/01	Update copyright.		11/09/00	TheDbgr_CallBack was declared for qDebug || qTheDebugger					but used only for qDebugger. 		09/20/00	No need to coerce const string to StringPtr in					TheDbgr_Add_Object and TheDbgr_Delete_Object.		12/27/99	Minor cosmetic cleanup.		12/08/99	Fixed file name in banner.		10/25/99	PRAGMA_ALIGN_SUPPORTED now PRAGMA_STRUCT_ALIGN.		03/24/99	Update copyright notice.*/#ifndef __UTHEDEBUGGER__#include "UTheDebugger.h"#endif// MacApp#ifndef __UCOREUTILITIES__#include "UCoreUtilities.h"#endif//========================================================================================// GLOBAL Procedures//========================================================================================#if qTheDebuggerstatic Boolean TheDbgr_CallBack();//========================================================================================// Support for Jasik's The Debugger//========================================================================================T_ExtDbgr gExtDbgr_Info;		// define struct block for Jasik Dbgrconst long c_MA_Objs = 512;		// obj list collector increment size//----------------------------------------------------------------------------------------// TheDbgr_Add_Object://----------------------------------------------------------------------------------------void TheDbgr_Add_Object(void* objHdl){	long		i,si;	T_ObjList*	obj_list;		if (!gExtDbgr_Info.hMA_OBJ_List) return;	lp:	{		obj_list = *gExtDbgr_Info.hMA_OBJ_List;		if (obj_list->L_used == obj_list->max_objs)		{			if (obj_list->n_holes > 0) 	// squeeze out empty entries			{				obj_list->n_holes = 0;				si = 0;				for (i = 0; i < obj_list->L_used; i++)					if (obj_list->objs[i] != 0)					{						obj_list->objs[si++] = obj_list->objs[i];					}				obj_list->L_used = si;			}			else			{				obj_list->max_objs = obj_list->max_objs + c_MA_Objs;				SetHandleSize((Handle)gExtDbgr_Info.hMA_OBJ_List, 12 + 4*obj_list->max_objs );				obj_list = *gExtDbgr_Info.hMA_OBJ_List;				if (MemError() != noErr) {					DebugStr("\pObject Collector could NOT get space to expand OBJ List");					return;				};				goto lp;			}				}		obj_list->objs[obj_list->L_used] = objHdl;		obj_list->L_used++;	}}//----------------------------------------------------------------------------------------// TheDbgr_Delete_Object://----------------------------------------------------------------------------------------void TheDbgr_Delete_Object(void* objHdl){	long	i;	T_ObjList*	obj_list;	if (gExtDbgr_Info.hMA_OBJ_List)	{		obj_list = *gExtDbgr_Info.hMA_OBJ_List;		for (i = 0; i < obj_list->L_used; i++)			if (obj_list->objs[i] == objHdl)			{				if (i == obj_list->L_used - 1)				{					obj_list->L_used--;				}				else				{					obj_list->objs[i] = 0;  obj_list->n_holes++;				}				return;			}		DebugStr("\pTheDbgr_Delete_Object - Object not found");	}}//----------------------------------------------------------------------------------------// TheDbgr_CallBack://----------------------------------------------------------------------------------------Boolean TheDbgr_CallBack(){	long	ConsWR_Proc;#if defined(powerc) || defined (__powerc)	short proc_code[] = {  // adj stk and Call Dbgr		0x201F,	//	move.l	(SP)+,D0	; rtn addr		0x4267,	//	CLR	-(SP)		; setup Excpt stk frame		0x2F00,	//	move.l	D0,-(SP)		0x4267,	//	CLR	-(SP)		0x7007,	//	MOVEQ	#7,D0 - isDbgrTask code		0x2078, 0x009C,	//	MOVE.L	4*(32+7),A0	; trap #7 Handler addr		0x4ED0	//	JMP	(A0)	};	enum {	TD_ProcInfo = kPascalStackBased		 | RESULT_SIZE(SIZE_CODE(sizeof(Boolean)))		 | STACK_ROUTINE_PARAMETER(1, SIZE_CODE(sizeof(Ptr)))		 | STACK_ROUTINE_PARAMETER(2, SIZE_CODE(sizeof(Ptr)))};	Boolean rtnVal;	UniversalProcPtr upp = NewRoutineDescriptor((ProcPtr)proc_code , TD_ProcInfo , kM68kISA );	rtnVal = CallUniversalProc(upp,TD_ProcInfo,&gExtDbgr_Info,&ConsWR_Proc);	DisposeRoutineDescriptor(upp);	return (rtnVal);#else	return (TheDbgr_IsDbgrTask(&gExtDbgr_Info,&ConsWR_Proc));#endif}//----------------------------------------------------------------------------------------// TheDbgr_Init_Ext_Dbgr://----------------------------------------------------------------------------------------void TheDbgr_Init_Ext_Dbgr(){		// initialize remaining fields in gExtDbgr_Info:	// note that n_classes and pOrderedClass have already	// been set in ClassDesc::InitUClassDesc; all other	// fields were zeroed at global initialization time╔	gExtDbgr_Info.isBeingDebugged = false;	gExtDbgr_Info.MA_Version = 3;		if (TheDbgr_CallBack())	{		gExtDbgr_Info.isBeingDebugged = true;		// allocate space for an Object collection LIst		gExtDbgr_Info.hMA_OBJ_List = (T_ObjList**) NewHandleClear((3*sizeof(long))+(c_MA_Objs*sizeof(void*)));				if (gExtDbgr_Info.hMA_OBJ_List)		{			(**gExtDbgr_Info.hMA_OBJ_List).max_objs = c_MA_Objs;		}	}}//	code to sort the class Descriptors so that the Class ID's have the property//	that TObject is 1st and the class ID of a subclass > class ID of parent//----------------------------------------------------------------------------------------// sort4://----------------------------------------------------------------------------------------static void sort4(long * v, short n)		// Shell Sort from H & S 2nd Edition{	short gap,i,j;	long temp;	gap = 1;	do (gap = 3*gap+1); while (gap <= n);	for (gap /= 3; gap > 0; gap /= 3)	{		for (i = gap; i < n; i++)		{			temp = v[i];			for (j = i-gap; (j >= 0) && (v[j] > temp); j -= gap)				v[j+gap] = v[j];			v[j+gap] = temp;		}	}}#if PRAGMA_STRUCT_ALIGN#pragma options align=mac68k#endiftypedef struct T_cl_Sort{	short Cl_ID;	short subCl_ID;}T_cl_Sort;#if PRAGMA_STRUCT_ALIGN#pragma options align=reset#endifstatic	ClassRegistration** pCL_AT;	// ptr to the arraystatic	short* pCL_IT;static	T_cl_Sort*	psubc_IT = NULL;//static	short	g_Lvl = 0;//----------------------------------------------------------------------------------------// follow://----------------------------------------------------------------------------------------static void follow(short f_id){	static	short	new_CL_ID = 0;		ClassRegistration* pCD = pCL_AT[f_id];	if (pCD)	{		pCL_AT[f_id] = 0;		pCD->SetClassID(++new_CL_ID);	// assign a new Class ID			short indx = pCL_IT[f_id];		// index to sub class List		//g_Lvl = g_Lvl + 1;		while ((indx >= 0) && (psubc_IT[indx].Cl_ID == f_id))		{			follow(psubc_IT[indx].subCl_ID);			indx--;		}		//g_Lvl = g_Lvl + 1;	// еее  probably should be -1	}}//----------------------------------------------------------------------------------------// TheDbgr_Adjust_ClassDescr_Ids://----------------------------------------------------------------------------------------void TheDbgr_Adjust_ClassDescr_Ids(ClassRegistration* pclassList, short numClasses){	numClasses++;	// for safety	pCL_IT	 = (short*)		 NewPtrClear(sizeof(short) * numClasses);		// point to arrays that follow accesses	psubc_IT = (T_cl_Sort*)  NewPtrClear(sizeof(T_cl_Sort) * numClasses);	pCL_AT	 = (ClassRegistration**) NewPtrClear(sizeof(ClassRegistration*) * numClasses);		short *CL_objNum = (short*)  NewPtrClear(sizeof(short) * numClasses);   // < FIX	if (pCL_IT && psubc_IT && pCL_AT)	// check our allocations	{		short i;		short n_cl = 0;		short n_objNum = 0; // <== FIX		short CL_Tobj = 0;				ClassRegistration* pCD = pclassList;		while (pCD)		{			psubc_IT[n_cl].subCl_ID = i = pCD->GetClassID();		// id of this class is sub Class ID			pCL_AT[i] = pCD;			if (pCD->CountBaseClasses() > 0)	// MacApp 3.3 can't do GetBaseClass(1) if the count is 0			{				const ClassRegistration* pBC = pCD->GetBaseClass(1);				psubc_IT[n_cl].Cl_ID = pBC->GetClassID();			}			else			{// еее  we don't use TObject				if (pCD == TObject::GetClassDescStatic())// еее  we don't use TObject					CL_Tobj = i;// еее  we don't use TObject				else							// <== FIX					CL_objNum[n_objNum++] = i;  // <== FIX				psubc_IT[n_cl].Cl_ID = 0;			}			pCD = (ClassRegistration*) pCD->GetNextClassDesc();		// const cast away			n_cl++;		}			sort4((long *)&(psubc_IT[0]), n_cl);		psubc_IT[n_cl].Cl_ID = -1;		// terminator for loop in follow				for (i = 0; i <= n_cl; i++) pCL_IT[i] = 0;				short last_cl = psubc_IT[0].Cl_ID;		for (i = 0; i <= n_cl; i++)		{			short j = psubc_IT[i].Cl_ID;			if (j != last_cl)			{				pCL_IT[last_cl] = i - 1;				last_cl = j;			}		}		follow(CL_Tobj);		for (i = 0; i < n_objNum; i++) follow(CL_objNum[i]);   // <== FIX	}	::DisposeIfPtr_AC((Ptr)pCL_IT);		// point to arrays that follow accesses	::DisposeIfPtr_AC((Ptr)psubc_IT);	::DisposeIfPtr_AC((Ptr)pCL_AT);	::DisposeIfPtr_AC((Ptr)CL_objNum);	// <== FIX}#endif	// #if qTheDebugger//----------------------------------------------------------------------------------------// End of UTheDebugger.cp/*	Historical Change Record:		----- R11 -----		06/26/96	Changed nil back to NULL.		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		11/28/95	Move to MacApp core library. 		----- 3.3 -----		 9/19/95	Split off from UDebug. 		----- 3.3b1 -----		 8/31/95	Incorporate Jasik's modified TheDbgr_Adjust_ClassDescr_Ids.		 7/18/95	PRAGMA_ALIGN_SUPPORTED. 		 7/11/95	TWB's CW debugging fixes.		  7/7/95	CW6 Update.		 6/18/95	CheckRsrcUsage is not conditioned on #qDebug anymore. It is		 					conditioned internally by gCheckRsrcUsage. 		 5/17/95	#Include for USegments		 5/16/95	Remove support for pre-system 7. 		05/05/95	Use a structure to hold code seg info		  4/8/95	Don't need to take address of functions. 		  4/4/95	Update support for The Debugger. 		 3/30/95	Include UDialog and UWindow for qPerform. 		 3/21/95	Don't include Unmangler with Rainbow. 		  3/6/96	Include UCoreGlobals.h		 2/27/95	Move view to UDebugView. Move don't dead strip of TDebugFlagsView		 					to UApplication. 		02/22/95	1222850 MacApp should use BlockMove_AC and BlockMoveOverlap_AC everywhere		02/22/95	1184041 Change NULL to NULL		02/13/95	Remove unnecessary initialization of static variables. See ARM д8.4 		02/08/95	replace usage of staticLink with yourDataPtr. 		02/07/95	Create missing prototypes for static functions used internally. 		12/15/94	Close and reopen standard I/O around the call to							_addDevHandler. Required to work safely with both							the new and old versions of console I/O support in							StdCLib. 		 12/7/94	Add support for debug output with CodeWarrior 5.0. 		 11/9/94	Metrowerks now supports _addDevHandler. 		 11/7/94	Added qSegments. 		 10/5/94	Put back includes of FCntl and IOCtl. 		 7/12/94	Don't use _addDevHandler with Metrowerks. 		 7/12/94	Change the constructor CStackFrame(long framePtr = 1)							to CStackFrame(void) so it doesn't conflict with							CStackFrame(ptrdiff_t);*/
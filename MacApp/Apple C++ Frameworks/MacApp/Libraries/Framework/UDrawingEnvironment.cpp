//----------------------------------------------------------------------------------------// UDrawingEnvironment.cpp// Copyright © 1991-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/02/01	Update copyright.		11/09/00	[PB] Change initialization order to match declaration. 		09/07/00	Added embedded documentation - TDrawingEnvironment::Complete.		08/31/00	Use MStreamable_AC instead of MStreamable.		02/22/00	[W] Need to set pen pattern in TDrawingEnvironment::TDrawingEnvironment.		03/29/99	Rename Patterns_AC to CPattern_AC to avoid confusion with the					include file. 		03/24/99	Update copyright notice.		03/23/99	Do not directly refer to qd.black.		11/02/98	Carbon - no direct access to qd fields*/#ifndef __UDRAWINGENVIRONMENT__#include "UDrawingEnvironment.h"#endif// ACS#ifndef __ImagingStreaming_AC__#include "ImagingStreaming_AC.h"#endif// MacApp#ifndef __UMACAPPUTILITIES__#include "UMacAppUtilities.h"#endif#ifndef __USTREAM__#include "UStream.h"#endif// ANSI#ifndef __STRING__#include <string.h>#endif//========================================================================================// CLASS TDrawingEnvironment//========================================================================================MA_DEFINE_CLASS(TDrawingEnvironment);//----------------------------------------------------------------------------------------// TDrawingEnvironment constructor//----------------------------------------------------------------------------------------TDrawingEnvironment::TDrawingEnvironment()	:	fForegroundColor(gRGBBlack),		fBackgroundColor(gRGBWhite),#if qMSWindows	#pragma message("TDrawingEnvironment::TDrawingEnvironment: Need to set pen pattern to qd.black equivalant.")#else		fPenPattern(*CPattern_AC::Black()),#endif		fPenSize(1,1),		fPenMode(patCopy){}//----------------------------------------------------------------------------------------// TDrawingEnvironment destructor//----------------------------------------------------------------------------------------TDrawingEnvironment::~TDrawingEnvironment(){}//----------------------------------------------------------------------------------------// TDrawingEnvironment::ReadFrom: //----------------------------------------------------------------------------------------void TDrawingEnvironment::ReadFrom(CStream_AC* aStream)	 {	MStreamable_AC::ReadFrom(aStream);		*aStream >> fPenSize >> fPenMode;		aStream->ReadBytes(&fPenPattern, sizeof(Pattern));		*aStream >> fForegroundColor >> fBackgroundColor;}//----------------------------------------------------------------------------------------// TDrawingEnvironment::WriteTo: //----------------------------------------------------------------------------------------void TDrawingEnvironment::WriteTo(CStream_AC* aStream) const	 {	MStreamable_AC::WriteTo(aStream);	*aStream << fPenSize << fPenMode;		aStream->WriteBytes(&fPenPattern, sizeof(Pattern));		*aStream << fForegroundColor << fBackgroundColor;}//----------------------------------------------------------------------------------------// TDrawingEnvironment::GetGrafPort: //----------------------------------------------------------------------------------------GrafPtr TDrawingEnvironment::GetGrafPort() const{	return NULL;}//----------------------------------------------------------------------------------------// TDrawingEnvironment::Prepare: //----------------------------------------------------------------------------------------void TDrawingEnvironment::Prepare()	 {	GetPenState(&fSavedPenState);	fSavedForegroundColor = GetIfColor();	fSavedBackgroundColor = GetIfBkColor();}//----------------------------------------------------------------------------------------// TDrawingEnvironment::Setup: //----------------------------------------------------------------------------------------void TDrawingEnvironment::Setup()	 {	PenNormal();	PenSize(fPenSize.h,fPenSize.v);	PenMode(fPenMode);	PenPat(&fPenPattern);	SetIfColor(fForegroundColor);	SetIfBkColor(fBackgroundColor);}//----------------------------------------------------------------------------------------// TDrawingEnvironment::Complete: //----------------------------------------------------------------------------------------void TDrawingEnvironment::Complete()	 // Complete is intended to specify the actions for this drawing environment // to take when the view that uses it finishes drawing itself.  This also// resets the pen and the foreground and background colors to their previous states.//// Call?		Seldom.// Override?	As needed.{	SetPenState(&fSavedPenState);	SetIfColor(fSavedForegroundColor);	SetIfBkColor(fSavedBackgroundColor);	}//----------------------------------------------------------------------------------------// End of UDrawingEnvironment.cpp/*	Historical Change Record:		----- R11 -----		06/26/96	Changed nil back to NULL.		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/09/96	Removed UStreamable to a separate file from UObject		 5/08/96	Cleanup CRGBColor_AC and add new class CTextStyle_AC		04/29/96	Create and use functions that return their data as results for:							TStream::ReadCharacter/CCoordPoint_AC/Rect/CCoordRect_AC/RGBColor/String. Create compatible inlines							so users don't have to change their code.		----- R10 -----		 3/25/96	CopyPat not needed anymore. 		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. 		02/22/95	1222899 Use CopyPat when appropriate*/
// UThreads.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		12/08/99	Changed itsContext from TCommandHandler to TEventHandler.		03/24/99	Update copyright notice.*/#ifndef __UTHREADS__#define __UTHREADS__// MacApp#ifndef __MACAPPTYPES__#include "MacAppTypes.h"#endif#if qWantsThreadManager	#ifndef __UCOMMAND__#include "UCommand.h"#endif// ACS#ifndef __CThreadRunner_AC__#include "CThreadRunner_AC.h"#endif//----------------------------------------------------------------------------------------//	Forward Declaration.class TEventHandler;EXPORT_MA void InitUThreads();//----------------------------------------------------------------------------------------//	TThreadExceptionHandler//----------------------------------------------------------------------------------------class EXPORT_MA TThreadExceptionHandler : public CThreadExceptionHandler_AC{public:	TThreadExceptionHandler();		// constructor			virtual ~TThreadExceptionHandler();		// destructor			virtual void HandleException(CException_AC& theException); // override		// Posts a command in the main thread		// which notifies the user that "A task has unexpectedly terminated."		virtual void HandleException(); // override		// Currently, unhandled unknown exceptions are not propagated to the user,		// but this may change in the future.};//----------------------------------------------------------------------------------------//	TThreadExceptionCommand//----------------------------------------------------------------------------------------class EXPORT_MA TThreadExceptionCommand : public TCommand{public:	TThreadExceptionCommand(CException_AC& theException,							CommandNumber itsCommandNumber = cNoCommand,							TEventHandler* itsContext = NULL,							bool canUndo = false,							bool causesChange = false,							MDependable_AC* objectToNotify = NULL);		// constructor with exception			virtual ~TThreadExceptionCommand();		// destructor			virtual void DoIt(); // override		// Pose an alert to the user		protected:	CException_AC fException;};#endif  // #if qWantsThreadManager#endif // __UTHREADS__
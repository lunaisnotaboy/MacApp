// UFile.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		03/24/99	Update copyright notice.		01/15/99	CAEDesc_AC is now in ACS. */#ifndef __UFILE__#define __UFILE__// MacApp#ifndef __MACAPPTYPES__#include "MacAppTypes.h"#endif#if qAppleEventAware	#ifndef __USCRIPTABLEOBJECT__	#include "UScriptableObject.h"	#endif#endif// Toolbox#ifndef __CFile_AC__#include "CFile_AC.h"#endif//----------------------------------------------------------------------------------------// TFile//----------------------------------------------------------------------------------------class EXPORT_MA TFile : public CFile_AC#if qAppleEventAware						, public MScriptableObject#endif// Each disk file associated with a particular document is represented// by an instance of the TFile class. TFile objects call the File// Manager to perform file I/O. Multiple TFile objects can be associated// with the single instance of TFileHandler, which manages file// access for the document. The methods of TFile use a file trio// consisting of the volume reference number, directory ID, and// filename to indicate which file to open, close, move, rename,// delete, and so on.{	MA_DECLARE_CLASS;	public:	TFile(OSType itsFileType = kGenericType_AC,			  OSType itsCreator = kGenericCreator_AC,			  bool usesDataFork = false,			  ResourceUsage usesRsrcFork = noResourceFork,			  bool keepsDataOpen = false,			  bool keepsRsrcOpen = false);		// Constructor	virtual ~TFile();#if qAppleEventAware	//----------------------------------------------------------------------------------------	// Scripting Support	//----------------------------------------------------------------------------------------	virtual bool GetObjectProperty(CAEDesc_AC& thePropertyValue,									  DescType whichProperty,									  const CAEDesc_AC& desiredType);	virtual void SetObjectProperty(const CAEDesc_AC& thePropertyValue,								   DescType whichProperty);	virtual void DoAEOpen(TAppleEvent* message, TAppleEvent* reply);#endif	//----------------------------------------------------------------------------------------	// Overrides	//----------------------------------------------------------------------------------------	virtual OSErr InteractWithUser();	// };//----------------------------------------------------------------------------------------// Global function declarations//----------------------------------------------------------------------------------------EXPORT_MA TFile* NewFile(OSType itsFileType,						 OSType itsCreator,						 bool usesDataFork,						 ResourceUsage usesRsrcFork,						 bool keepsDataOpen,						 bool keepsRsrcOpen);	// A convenience function. Create a TFile, initialize it, and return a reference to    // it. Signals Failure if it cannot allocate the object.#endif/*	Historical Change Record:		----- R11 -----		06/26/96	[1356153] Added Clone method to fix alias problem.		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		 6/24/96	[1345531] Added #include "UToolbox.h" for noload builds.		 5/08/96	Create and use CFSSpec_AC. You should search/replace in your code to switch to		 					CFSSpec_AC.		----- R10 -----		 3/01/96	Re-arranged data members, natural alignment results in 		 					reduced storage size.		11/15/95	Added TFile::UpdateFileSpec and flias.		 11/7/95	Add back the old interface. 		11/07/95	GetFinderInfo didn't work for Folders.		 9/26/95	[1276050] Add TFile::SetObjectProperty.		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. 		 1/26/95	Add desiredType to GetObjectProperty. 		 2/14/95	TFile::GetObjectProperty now returns a boolean.		----- 3.5d3 Release -----		 7/26/94	Include UScriptableObject. 		 7/14/94	Merged with Ken Ryall's MAScript changes. */
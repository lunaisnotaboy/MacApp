// UOSAScript.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/15/00	Use CString_AC instead of CStr255_AC.		01/02/01	Update copyright.		11/09/00	Ticks are unsigned. 		12/08/99	Moved qAppleEventAware test below MacAppRequired.h include so					that the switch get set properly.		03/24/99	Update copyright notice.		01/15/99	CAEDesc_AC is now in ACS. 		11/12/98	[2279245] Included MacAppRequired.h for qAppleEventAware.		10/08/98	Merge TCommandHandler into TEventHandler. */#ifndef __UOSASCRIPT__#define __UOSASCRIPT__// MacApp#ifndef __MacAppRequired__#include "MacAppRequired.h"#endif#if qAppleEventAware#ifndef __UCOMMAND__#include "UCommand.h"#endif// ACS#ifndef __CString_AC__#include "CString_AC.h"#endif// Toolbox#ifndef __COMPONENTS__#include <Components.h>#endif#ifndef __OSA__#include <OSA.h>#endif// Hack to prevent Universal Headers macros from interfering with our functions#ifdef GetScript#undef GetScript#endif#ifdef SetScript#undef SetScript#endif//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class CAEDesc_AC;class TAppleEvent;class TOSAScriptRep;class TFile;class CStream_AC;//========================================================================================// COSAScript: Object representation of an OSA Script.//========================================================================================class EXPORT_MA COSAScript// The COSAScript class describes an Open Scripting Architecture// (OSA) script as an object. You pass the script to the constructor// at instantiation to attach it to this object. You can attach// a new script at any time by calling SetScript. The Execute method// runs the script. DisposeScript unattaches the script from this// object.//    Specify scripts as being empty, shared, or global in scope// using static inline methods. Get/Set the attached script or the// parent script with COSAScript accessor methods. COSAScript objects// use the DoOSAEvent method to respond to Apple events.//    See "Inside Macintosh:Interapplication Communication" for// Current information about scripting.//    See the GetGlobalScript, GetSharedScript, GetEmptyScript,// SetGlobalScript, SetSharedScript, and methods for Current information// about static scripts.//    See the SetScript and GetScript methods for Current information// about getting and setting scripts.//    See the SetParentScript and GetParentScript methods for Current// information about chaining scripts to an object.//    See the GetDefaultComponent method for Current information// about the default scripting component used by all COSAScript// objects.{protected:	static ComponentInstance fgGenericScriptingComponent;			// The protected and static fgGenericScriptingComponent field contains the scripting			// component for all COSAScript objects. This scoped global is static so that all instances			// of COSAScript objects can share the same component and so you can access it without			// using a "this" pointer. MacApp, by default, works with the AppleScript scripting			// component. GetDefaultComponent returns this field.	static COSAScript fgGlobalScript;			// The protected and static fgGlobalScript field holds the global script for all			// objects. This scoped global is static so that all instances of COSAScript objects can			// share the same script and so you can access it without using a "this" pointer. The inline			// static SetGlobalScript method sets it.	static COSAScript fgSharedScript;			// The protected and static fgSharedScript field holds the shared script for all			// objects. This scoped global is static so that all instances of COSAScript objects can			// share the same script and so you can access it without using a "this" pointer. The			// inline static SetSharedScript method sets it.	static COSAScript fgEmptyScript;			// The protected and static fgEmptyScript field holds the empty script for all objects. This			// scoped global is static so that all instances of COSAScript objects can use the same			// script and so you can access it without using a "this" pointer.	public:	static ComponentInstance GetDefaultComponent();		static COSAScript GetEmptyScript();	inline static COSAScript GetGlobalScript();	inline static void SetGlobalScript(OSAID globalScript);		inline static COSAScript GetSharedScript();	inline static void SetSharedScript(OSAID sharedScript);	public:	OSAID	fScriptID;	// The fScriptID field holds the script that this COSAScript object describes. You						// must supply an Open Scripting Architecture (OSA) script to COSAScript's						// constructor to set the initial value for the fScriptID field. GetScript uses						// the fScriptID field to retrieve a script. SetScript sets it to a new						// script. (The new script's description is passed to SetScript).	COSAScript();	COSAScript(OSAID theScript);	operator OSAID() const			{ return fScriptID; }	operator OSAID*()				{ return &fScriptID; }		OSAID operator=(OSAID theScript)	{ fScriptID = theScript; return fScriptID; }	bool IsEmpty();	OSAError Execute(ComponentInstance theComponent, OSAID theContext, long executeFlags, OSAID& theResult);	OSAError GetScript(ComponentInstance theComponent, DescType whichType, CAEDesc_AC& scriptDesc, long storeFlags);	OSAError SetScript(ComponentInstance theComponent, const CAEDesc_AC& scriptDesc, long compileFlags, long loadFlags);	OSAError DisposeScript(ComponentInstance theComponent);	OSAError SetParentScript(ComponentInstance genericComponent, OSAID itsParent);	OSAError GetParentScript(ComponentInstance genericComponent, OSAID& itsParent);	OSAError DoOSAEvent(ComponentInstance theComponent, TAppleEvent* message, TAppleEvent* reply, long modeFlags);};//----------------------------------------------------------------------------------------// COSAScript inlines//----------------------------------------------------------------------------------------inline COSAScript COSAScript::GetGlobalScript(){	return COSAScript::fgGlobalScript;}inline void COSAScript::SetGlobalScript(OSAID globalScript){	COSAScript::fgGlobalScript = globalScript;}inline COSAScript COSAScript::GetSharedScript(){	return COSAScript::fgSharedScript;}inline void COSAScript::SetSharedScript(OSAID sharedScript){	COSAScript::fgSharedScript = sharedScript;}//========================================================================================// CLASS COSAScriptCntPtr//========================================================================================class EXPORT_MA COSAScriptCntPtr// The COSAScriptCntPtr class is used to handle MacApp script reference// counting.{	//----------------------------------------------------------------------------------------	// Static members	//----------------------------------------------------------------------------------------public:	static TOSAScriptRep* NewOSAScript();	public:	COSAScriptCntPtr();		// Creates a "NULL" pointer.	virtual ~COSAScriptCntPtr();		// Destroy the pointer.  		// Decrements the count in the rep, and deletes the rep if count is zero.			COSAScriptCntPtr(const COSAScriptCntPtr& other);		// FW_SPlatformPoint this pointer to the same rep as the other pointer points to.	COSAScriptCntPtr(TOSAScriptRep* rep);		// Creates a pointer pointing at rep.		COSAScriptCntPtr& operator=(const COSAScriptCntPtr& other);		// Assignment, point this ponter to the same rep as the other pointer points to.			COSAScriptCntPtr& operator=(TOSAScriptRep* rep);		// Assignment, point this ponter to rep.			TOSAScriptRep* operator->() const;	// Must be overlodded by subclass		// Provide access to members of rep.				TOSAScriptRep& operator*() const;		// Provide access to rep.		// This operator should be used only if operator->() is not sufficient.		bool operator==(const COSAScriptCntPtr& other) const;		// Points to the same representation object?	bool operator!=(const COSAScriptCntPtr& other) const;		// Points to a different representation object?	operator const void*() const;		// Use to test if this is a "NULL" pointer.protected:	void	SetRep(TOSAScriptRep* rep);		void UpCount();		// Implementation method, increment the reference count in rep.			void DownCount();		// Implementation method, decrement the reference count in rep.		// Deletes rep if reference count is zero.	TOSAScriptRep*	fRep;		// The fRep field points to the TOSAScriptRep object whose								// references this object is counting.};//----------------------------------------------------------------------------------------// COSAScriptCntPtr::operator->//----------------------------------------------------------------------------------------inline TOSAScriptRep* COSAScriptCntPtr::operator->() const{	return fRep;}//----------------------------------------------------------------------------------------// COSAScriptCntPtr::operator*//----------------------------------------------------------------------------------------inline TOSAScriptRep& COSAScriptCntPtr::operator*() const{	return *fRep;}//----------------------------------------------------------------------------------------// COSAScriptCntPtr::operator==//----------------------------------------------------------------------------------------inline bool COSAScriptCntPtr::operator==(const COSAScriptCntPtr& other) const{	return (fRep == other.fRep);}//----------------------------------------------------------------------------------------// COSAScriptCntPtr::operator!=//----------------------------------------------------------------------------------------inline bool COSAScriptCntPtr::operator!=(const COSAScriptCntPtr& other) const{	return !operator==(other);}//----------------------------------------------------------------------------------------// COSAScriptCntPtr::operator const void*//----------------------------------------------------------------------------------------inline COSAScriptCntPtr::operator const void*() const{ return fRep; }//========================================================================================// TOSAScriptRep: MacApp Object representation of an OSA Script.//========================================================================================class EXPORT_MA TOSAScriptRep// The TOSAScriptRep class is used to represent OSA scripts in MacApp.{  public:	//----------------------------------------------------------------------------------------	// Construction	//----------------------------------------------------------------------------------------	TOSAScriptRep();	virtual ~TOSAScriptRep();		//----------------------------------------------------------------------------------------	// Attached Scripts	//----------------------------------------------------------------------------------------	virtual bool HasOSAScript();		// Return TRUE if this object has a script attached.	virtual void FreeOSAScript();		// Throws out the script attached to this object.	//	virtual ComponentInstance GetScriptingComponent();	//		// Returns an instance of the scripting component used with scripts	//		// attached to this object. Default is to return the Generic component.	virtual void SetOSAScript(const CAEDesc_AC& scriptDesc);		// Attaches a script described in the scriptDesc	virtual void GetOSAScript(CAEDesc_AC& scriptDesc,							  const CAEDesc_AC& desiredType);		// Returns the script attached to this object as the desiredType.	virtual OSAID ExecuteOSAScript();		// Executes the attached script and returns the result.		virtual bool HandleOSAEvent(CommandNumber aCommandNumber,									TAppleEvent* message,									TAppleEvent* reply);		// Gives an attached script a chance to handle an AppleEvent.		// Returns TRUE if the script handled the event.	virtual void IdleOSAScript();		// Gives an attached script idle time so its idle event handler can do some work.		// Respects the object's fNextScriptIdle so you can call it as often as you like.	virtual OSAID GetOSAParent();		// Returns this object's parent script. If the scripting component supports		// inheritance the HandleOSAEvent method will use this parent script to		// set up the inheritance chain.		// The default parent script is TScriptEventDispatcher's shared handler script.	virtual void ReadOSAScript(CStream_AC* aStream);		// Reads a script from a stream.	virtual void ReadOSAScript(TFile* aFile,								ResType resourceType,								short resourceID);		// Reads a script from a resource		virtual void WriteOSAScript(CStream_AC* aStream, DescType asType);		// Writes a script to aStream.		virtual void WriteOSAScript(TFile* aFile, 								DescType theType,								short resourceID,								CString_AC& resourceName);		// Writes an attached script to aStream.	//----------------------------------------------------------------------------------------	// Reference Counting Support - We need reference counting in order to prevent	//	premature death of a script while it is still being executed.  This could	//	happen if the object to which the script is attached is killed off while	//	the script is being run (such as in an "on close" attached script).	//----------------------------------------------------------------------------------------		long	IncrementReferenceCount();	long	DecrementReferenceCount();	//----------------------------------------------------------------------------------------	// Instance data	//----------------------------------------------------------------------------------------protected:	unsigned long 	fRefCount;public:	COSAScript			fScript;				// the script	unsigned long		fNextScriptIdle;		// when to next give idle time to attached scripts	long				fLoadFlags;				// Flags for OSALoad	long				fCompileFlags;			// Flags for OSACompile	long				fStoreFlags;			// Flags for OSAStore	long				fExecuteFlags;			// Flags for OSAExecute	long				fDoEventFlags;			// Flags for OSADoEvent		ComponentInstance	fScriptingComponent;	// the scripting component for this script};//----------------------------------------------------------------------------------------// TOSAScriptRep inlines//----------------------------------------------------------------------------------------inline long TOSAScriptRep::DecrementReferenceCount(){	return --fRefCount;}inline long TOSAScriptRep::IncrementReferenceCount(){	return ++fRefCount;}//========================================================================================// TExecuteScriptCommand: Command to execute an OSA script.//========================================================================================class EXPORT_MA TExecuteScriptCommand : public TCommand// TExecuteScriptCommand The TExecuteScriptCommand class is a command// object to run an Open Scripting Architecture script. Initialize// the command by calling the TExecuteScriptCommand method. You// will pass in all of the necessary information to set up the command// (context, causes changes, etc). Execute the command to run the// script by calling the DoIt method. If the fDisposeScript field// is true the destructor calls DisposeScript.{public:	TExecuteScriptCommand(CommandNumber itsCommandNumber = cNoCommand,							   TEventHandler* itsContext = NULL,							   bool canUndo = false,							   bool causesChange = false,							   MDependable_AC* objectToNotify = NULL,							   OSAID scriptToExecute = 0,							   OSAID theScriptContext = 0,							   long executeFlags = kOSAModeNull,							   bool disposeOfScript = false,							   ComponentInstance whichComponent = NULL);	virtual ~TExecuteScriptCommand();			virtual void DoIt();			// Executes the scriptpublic:	COSAScript	fScript;	COSAScript	fScriptContext;	bool		fDisposeScript;	ComponentInstance	fComponent;	long		fExecuteFlags;};#endif	// #if qAppleEventAware#endif	// __UOSASCRIPT__/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/24/96	[1345531] Add fwd ref to TStream for noLoad build.		04/30/96	Make all constructors use initialization lists if they weren't already							doing do. Switch several more empty constructors to inlines.		----- R10 -----		  4/3/96	Add undef Get/SetScript so we can compile with OLDROUTINENAMES=1. 		10/25/95	Add ReadOSAScript/WriteOSAScript to/from resource methods.		----- 3.3b1 -----		  9/5/95	TOSAScriptCntPtr -> COSAScriptCntPtr.		09/01/95	private -> protected.		 8/14/95	Added reference counting to the TOSAScript class		 					due to some undesired destruction of said scripts							while executing them during document closing.  The							reference counting code was plagerized from FWCouPtr.h.		 7/27/95	Remove SetEmptyScript. Make GetEmptyScript non-inline. 							No longer need MakeGlobalScripts. 							SetParentScript no longer takes a reference. 		 7/24/95	Initial version. */
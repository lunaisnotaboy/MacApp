// UDialogBehavior.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		03/24/99	Update copyright notice.*/#ifndef __UDIALOGBEHAVIOR__#define __UDIALOGBEHAVIOR__// MacApp#ifndef __UBEHAVIOR__#include "UBehavior.h"#endifconst IDType kDialogBehavior = 'dlog';		// id of dialog behaviorsconst IDType kStdDialogBehavior = 'dlgb';	// class id of dialog behaviors//----------------------------------------------------------------------------------------// TDialogBehavior// // When installed in a window, this behavior handles events appropriately for both modal// and modeless dialogs. One subview can be designated the "default item" (eg. OK button)// and another the "cancel item" (eg. Cancel button in a modal dialog). Pressing the OK// button in a modal dialog causes the window to be validated. This behavior also treats the // pressing of the Enter and Return keys as a click in the default item, and Esc and // Command-Period as a click in the cancel item. //// TWindow.PoseModally calls this behavior's PoseModally method to place the window in// a modal state.//----------------------------------------------------------------------------------------class EXPORT_MA TDialogBehavior : public TBehavior// A TDialogBehavior object handles modal and modeless dialogs.// This behavior is installed if you open a TDialogView object or// specify the behavior in a MacApp 3.0 'View' resource.//    You can designate a subview for a "default item," such as// the OK button, and a subview for a "cancel item," such as the// Cancel button in a modal dialog. A TDialogBehavior object provides// the following behavior: an OK response in a modal dialog causes// the window to be validated; an Enter or Return response acts// as a click on the default item; and an Escape or Command-period// acts as a click on the cancel item.{public:	//------------------------------------------------------------------------------------	// Initializer and I<Method>.	//------------------------------------------------------------------------------------	TDialogBehavior(bool isModal = false,										IDType itsDefaultItem = kNoIdentifier,										IDType itsCancelItem = kNoIdentifier);		// Constructor	virtual ~TDialogBehavior();		// Destructor	virtual void* Clone() const;		// Clone					//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFrom(CStream_AC* aStream);	virtual void WriteTo(CStream_AC* aStream) const;	//------------------------------------------------------------------------------------	// Event handling.	//------------------------------------------------------------------------------------	virtual void Dismiss(IDType dismisser, bool validate);		// Dismiss the owning view (window), validating if requested.			virtual void DoEvent(EventNumber eventNumber,								TEventHandler* source,								TEvent* event);	// Override		// Handles the mDefaultKey and mCancelKey events. Translates events from		// the default and cancel items into these events.			virtual void DoKeyEvent(TToolboxEvent* event);	// Override		// Handles the Enter and Return keys.			virtual void DoCommandKeyEvent(TToolboxEvent* event); // Override		// Handles the Esc key and Command-Period.			virtual void PoseModally();		// Places the window owning this behavior in a modal state.		// Processes events until fDismissed is true.		virtual IDType GetStandardSignature() const;	// Override 		// Returns this class's standard signature.	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------public:	IDType	fDefaultItem;	// The identifier of the view that is the default choice;							// typically the OK button in a modal dialog.	IDType	fCancelItem;	// The identifier of the view used to cancel the dialog;							// typically the Cancel button for a modal dialog and							// kNoIdentifier for a modeless dialog.	IDType 	fDismisser;		// The identifier of the view that caused the dialog to be dismissed.	bool	fModal;			// true if this behavior is for modal dialogs.	bool	fDismissed;		// true if a modal dialog is dismissed.};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		 3/01/96	Re-arranged data members, natural alignment results in 		 					reduced storage size.		10/11/95	Define kStdDialogBehavior. Add GetStandardSignature.							(N.Nallick)		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. */
// UScrollBar.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		05/22/01	Add a setter for the direction. 		04/10/01	Added redraw parameter to UpdateScrollAmounts function with					default of true.		01/24/01	[W] Added SetWindowsWindow.		01/02/01	Update copyright.		11/10/00	Cosmetic.		08/28/00	Added embedded documentation.		08/01/00	[W] Added GetMaximum, GetMinimum, GetValue, SetMaximum,					SetMinimum, SetValue, SetValues, GetWindowsClassName and					HandleWindowsEvent.  Cosmetic.  Added DoPostCreate.		03/16/00	Use EXPORT_CLASS_MA.		03/24/99	Update copyright notice.*/#ifndef __USCROLLBAR__#define __USCROLLBAR__// ACS#ifndef __CList_AC__#include "CList_AC.h"#endif// ACS Appearance#ifndef __CControl_AC__#include "CControl_AC.h"#endif// MacApp#ifndef __UCONTROL__#include "UControl.h"#endif// Included for gSystemStyle#ifndef __UMACAPPGLOBALS__#include "UMacAppGlobals.h"#endif#ifndef __UTRACKER__#include "UTracker.h"#endif#ifndef __UVIEW__#include "UView.h"#endif// Toolbox#ifndef __CONTROLS__#include <Controls.h>#endif//----------------------------------------------------------------------------------------// Forward and external class declarations.//----------------------------------------------------------------------------------------class TScroller;class TToolboxEvent;EXPORT_CLASS_MA(stl_operator_delete_auto_ptr<CStr255_AC>)EXPORT_CLASS_MA(MA_operator_delete_auto_ptr<CStr255_AC>)//----------------------------------------------------------------------------------------// TScrollBar: A subclass of TCtlMgr that maps between longint values for our view// coordinates and the integers of a toolbox scrollbar.//----------------------------------------------------------------------------------------class EXPORT_MA TScrollBar : public TCtlMgr// The TScrollBar class implements a standard Macintosh scroll bar,// either vertical or horizontal, depending on the parameters with// which it is created. The scroll bar is suitable for use as an// analog dial or a slider control in a dialog box. It can be used// to scroll text or graphics in the content region of a window.// However, an instance of TScrollerScrollBar is current easily// used to implement content-region scrolling.{	MA_DECLARE_CLASS;public:	//------------------------------------------------------------------------------------	// Initialize and Free	//------------------------------------------------------------------------------------	TScrollBar(const CViewPoint&	itsLocation = gZeroVPt,			   const CViewPoint&	itsSize = gZeroVPt,			   SizeDeterminer		itsHSizeDet = sizeVariable,			   SizeDeterminer		itsVSizeDet = sizeVariable,			   VHSelect				itsDirection = hSel,			   long					itsVal = 0,			   long					itsMin = 0,			   long					itsMax = 0);		// Constructor	TScrollBar(const TScrollBar& inScrollBar);		// Copy constructor	virtual ~TScrollBar();		// Destructor	//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;		// Returns this class's standard signature.#if qMSWindows	virtual const char* GetWindowsClassName() { return "SCROLLBAR"; }	virtual void SetWindowsWindow(HWND windowsWindow);#endif	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);	virtual void WriteFields(CStream_AC* aStream) const;	virtual void DoPostCreate(TDocument* itsDocument);	//------------------------------------------------------------------------------------	// View method overrides	//------------------------------------------------------------------------------------	virtual void DeltaValue(ViewCoordinate delta);		// Does range checking on 'delta' checking it against fLongMin & fLongMax for the        // scrollbar.	virtual void DoMouseCommand(CViewPoint&		theMouse,								TToolboxEvent*	event,								CPoint_AC		hysteresis);		// Handles the mouse down in the various parts of the scrollbar.	virtual void TrackScrollBar(short partCode);		// Tracks the scrollbar by calling DeltaValue.	virtual void DoControlAction(ControlPartCode partCode); //ActionProc(short partCode);		// Forwarded to from the global routine: ActionProcForTScrollBar which was set as        // the ScrollBar's action proc. Typically forwards again to TrackScrollBar after        // bounds checking and Updates before returning to the ScrollBar.	virtual CControl_AC* DoMakeControl(	WindowRef			theWindow,										const CRect_AC&		boundsRect,										bool				isVisible,										bool				isEnabled,										const CStr255_AC&	itsTitle);		// Factory method instantiates the control.	VHSelect GetDirection() const { return fDirection; }		// Returns whether this scroll bar is horizontal or vertical.	void SetDirection(VHSelect itsDirection) { fDirection = itsDirection; }		// Change the direction. #if qMSWindows	virtual ViewCoordinate GetMaximum();		// Returns the current maximum value.	virtual ViewCoordinate GetMinimum();		// Returns the current minimum value.	virtual ViewCoordinate GetValue() const;		// Returns the current value.	virtual void SetMaximum(ViewCoordinate itsMax,								   bool redraw);		// Sets maximum to 'itsMax', redrawing if required.	virtual void SetMinimum(ViewCoordinate itsMin, bool redraw);		// Sets minimum to 'itsMin', redrawing if required.	virtual void SetValue(ViewCoordinate itsVal, bool redraw);		// Sets value to 'itsVal' redrawing if required.	virtual void SetValues(ViewCoordinate	itsVal,						   ViewCoordinate	itsMin,						   ViewCoordinate	itsMax,						   bool				redraw);		// Sets all of the values at once, redrawing if required.	virtual long HandleWindowsEvent(HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam);#endif	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------#if qBackwardCompatiblepublic:#elseprotected:#endif	VHSelect			fDirection;		// is this a horizontal or vertical                                        // scrollbar	ViewCoordinate		fInitialVal;	// The control's initial value in view coordinate										// units. You must use SetValue to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetValue.	ViewCoordinate		fInitialMin;	// The control's minimum value in view coordinate										// units. You must use SetMinimum to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetMinimum.	ViewCoordinate		fInitialMax;	// The control's maximum value in view coordinate										// units. You must use SetMaximum to set the										// value of this field. The default value, as supplied										// by the constructor, is 0. Rather than										// inspecting the value of this field directly, use										// the accessor method GetMaximum.};//----------------------------------------------------------------------------------------// TScrollerScrollBar: A subclass of TScrollBar that is associated with one or more// scrollers.//----------------------------------------------------------------------------------------class EXPORT_MA TScrollerScrollBar : public TScrollBar// TScrollerScrollBar, a subclass of TScrollBar, simplifies content-region// scrolling by interacting with TScroller objects. TScrollerScrollBar// objects inherit much of their behavior from the superclass TScrollBar.// In addition, the TScrollerScrollBar class implements mechanisms// to easily pass messages to multiple TScroller objects in response// to user actions. The TScroller objects can then make the appropriate// adjustments in the coordinate space of the window's content region// so that the programmer doesn't have to calculate offsets and// change the coordinates' origin.//    A single scroll bar can be associated with any number of scrollers.// Each scroller is responsible for its own scrolling according// to messages sent to it from its scroll bars. TScrollerScrollBar// replaces the behavior implemented by the TSScrollBar class in// MacApp 2.0.{	MA_DECLARE_CLASS;public:	//------------------------------------------------------------------------------------	// Initialize and Free	//------------------------------------------------------------------------------------	TScrollerScrollBar(const CViewPoint&	itsLocation = gZeroVPt,					   const CViewPoint&	itsSize = gZeroVPt,					   SizeDeterminer		itsHSizeDet = sizeVariable,					   SizeDeterminer		itsVSizeDet = sizeVariable,					   VHSelect				itsDirection = hSel,					   long					itsMax = 0,					   TScroller*			itsScroller = NULL);		// Constructor	TScrollerScrollBar(const TScrollerScrollBar& inScrollerScrollBar);		// Copy Constructor	virtual ~TScrollerScrollBar();		// If the scrollers are not NULL then first detach the scrollbars from them before        // freeing the scrollers, then call Inherited::Free.	//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;		// Returns this class's standard signature.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);	virtual void WriteFields(CStream_AC* aStream) const;	//------------------------------------------------------------------------------------	// Methods	//------------------------------------------------------------------------------------	virtual void UpdateScrollAmounts(bool redraw = kRedraw);		// Sets the view (proportional thumb) size according to the first scroller's size.		// Override to defeat proportional behavior.	virtual void AttachScroller(TScroller* itsScroller);		// IF 'itsScroller' is not NULL then add it to our list and attach ourselves to it.	virtual void SetFrame(const CViewRect& newFrame, bool invalidate);		// Update scroll amounts for proportional thumb.	virtual void BeInPort(GrafPtr itsPort);		// Call Inherited::BeInPort and then set our visibility based on the state of the        // window we are in.	virtual void Activate(bool entering);		// If IsActive() is true then show ourselves, otherwise just call Draw to frame        // our area.	virtual void DoMouseCommand(CViewPoint&		theMouse,								TToolboxEvent*	event,								CPoint_AC		hysteresis);		// Handles mouse downs in the scrollbar.	virtual void Draw(const CViewRect& area);		// Draws a frame around the controls area before calling Inherited::Draw.	virtual void TrackScrollBar(short partCode);		// Called to handle tracking of the scrollbar by calling DeltaValue.#if qMSWindows	virtual void CreateWindowsWindow();	virtual long HandleWindowsEvent(HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam);#endif	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------public:	CList_AC fScrollers;							// the associated scrollers if any};#endif // __USCROLLBAR__
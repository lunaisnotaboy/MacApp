// UDesignator.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		02/28/01	Don't include Memory_AC. 		01/02/01	Update copyright.		11/10/00	Cosmetic.		08/31/00	Use MStreamable_AC instead of MStreamable and include MStreamable_AC.h					instead of UStreamable.  Rearranged includes.		08/30/00	Added embedded documentation.		08/10/99	Added MA_DECLARE_CLONE to TDesignator.		03/24/99	Update copyright notice.		02/11/99	[C] Must use Bounds instead of rgnBBox.*/#ifndef __UDESIGNATOR__#define __UDESIGNATOR__//----------------------------------------------------------------------------------------// OVERVIEW// ========// TDesignator is designed as an abstract superclass which designates a portion of the// document. Each specialized document will require its own specialized designator.// Specialized subclasses of TDesignator are required to designate portions of different// documents.//  // CLASS HIERARCHY// ===============// TDesignator			abstract superclass for designators// TLinearDesignator	a linear data designator (e.g. for text models)// TRectDesignator		a rectangular designator (e.g. for spreadsheet models)// TRgnDesignator		a region designator (e.g. for discontiguous designations)//// USAGE// =====// TDesignator is used with the Edition Manager support that is built into MacApp.// Whenever a TSection object is instantiated, it requires a TDesignator object that lets// the TSection object determine which portion of the document is being designated as a// section, whether a publisher or a subscriber.//----------------------------------------------------------------------------------------// MacApp#ifndef __UGEOMETRY__#include "UGeometry.h"#endif#ifndef __UMACAPPGLOBALS__#include "UMacAppGlobals.h"#endif#if qAppleEventAware	#ifndef __USCRIPTABLEOBJECT__	#include "UScriptableObject.h"	#endif#endif// ACS#ifndef __CTempRegion_AC__#include "CTempRegion_AC.h"#endif#ifndef __MStreamable_AC__#include "MStreamable_AC.h"#endif// Toolbox#ifndef __MACMEMORY__#include <MacMemory.h>#endif#ifndef __QUICKDRAW__#include <Quickdraw.h>#endif//----------------------------------------------------------------------------------------// Containment constants//----------------------------------------------------------------------------------------enum ContainmentType { kNotContained, kPartiallyContained, kExactlyContained, kFullyContained };//----------------------------------------------------------------------------------------// Object Signtures: These follow the same reservation rules as resource types. The// signature is used to search the signature table and create an object "by signature"If// the signature's entry in the table has been updated with a new class then an object of// that class will be created.//----------------------------------------------------------------------------------------const IDType kLinearDesignator = 'ldsg';const IDType kVRectDesignator = 'vdsg';const IDType kRegionDesignator = 'rdsg';//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class TStream;//----------------------------------------------------------------------------------------// TDesignator: is designed as an abstract superclass. Use subclasses of TDesignator.//----------------------------------------------------------------------------------------class EXPORT_MA TDesignator :#if qAppleEventAware							   public MScriptableObject,#endif	// #if qAppleEventAware							   public MStreamable_AC// TDesignator is an abstract class that allows you to designate// portions of a document. For example, MacApp uses a designator// to identify the user's selection. MacApp also uses designators// to identify portions of documents that publish or subscribe to// editions.//    MacApp provides three subclasses of TDesignator: TLinearDesignator,// which identifies a "linear" portion of a document, such as lines// of text; TVRectDesignator, which identfies a rectangle, such// as contiguous rows and columns of cells in a spreadsheet; and// TRegionDesignator, which identifies noncontiguous areas in a// document. You typically use instances of these subclasses or// instances of a subclass that you create to designate portions// of your document.{	MA_DECLARE_CLONE;public:	//----------------------------------------------------------------------------------------	// ¥Êcontructors/destructors	//----------------------------------------------------------------------------------------	TDesignator();		// constructor	virtual ~TDesignator();		// Destructor			//----------------------------------------------------------------------------------------	// ¥Êutility methods	//----------------------------------------------------------------------------------------	virtual Size GetSize() const;		// returns the size of this designation	virtual ContainmentType IsContained(TDesignator* aDesignator);		// Override this; returns a ContainmentType indicating to what extent aDesignator        // is contained in this	virtual bool IsEmpty() const;		// Returns false if the size of this designation is greater than 0};//----------------------------------------------------------------------------------------// TLinearDesignator//----------------------------------------------------------------------------------------class EXPORT_MA TLinearDesignator : public TDesignator// TLinearDesignator objects designate data that can be identified// by a start position and an end position; for example, a string// of characters in a word processing document.{	MA_DECLARE_CLONE;	public:	//----------------------------------------------------------------------------------------	// ¥ contructors/ destructors	//----------------------------------------------------------------------------------------	TLinearDesignator(long startPos = 0,										  long endPos = 0);		// Constructor	virtual ~TLinearDesignator();		// Destructor			//----------------------------------------------------------------------------------------	// ¥Êutility methods	//----------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;		// returns the signature for this class	virtual Size GetSize() const;		// returns the size of this designation	virtual ContainmentType IsContained(TDesignator* aDesignator);		// OVERRIDE this; returns a ContainmentType indicating to what extent aDesignator        // is contained in this	//----------------------------------------------------------------------------------------	// ¥ saving	//----------------------------------------------------------------------------------------	virtual void WriteTo(CStream_AC* aStream) const;		// OVERRIDE this; writes specification of designator to stream	virtual void ReadFrom(CStream_AC* aStream);		// initializes this object from a stream specificationpublic:	long fStartPos;								// offset to start	long fEndPos;								// offset to end};//----------------------------------------------------------------------------------------// TViewRectDesignator//----------------------------------------------------------------------------------------class EXPORT_MA TViewRectDesignator : public TDesignator// TViewRectDesignator: is designed to designate any rectangular portion of data that may be// designated by top, left, bottom, and right ViewCoordinates.{	MA_DECLARE_CLONE;	public:	//----------------------------------------------------------------------------------------	// ¥ contructors/destructors	//----------------------------------------------------------------------------------------	TViewRectDesignator(const CViewRect& itsDesignation = gZeroVRect);	virtual ~TViewRectDesignator();		// Destructor			//----------------------------------------------------------------------------------------	// ¥Êutility methods	//----------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;		// returns the signature for this class	virtual Size GetSize() const;		// returns the size of this designation	virtual ContainmentType IsContained(TDesignator* aDesignator);		// OVERRIDE this; returns a ContainmentType indicating to what extent aDesignator        // is contained in this	//----------------------------------------------------------------------------------------	// ¥Êsaving	//----------------------------------------------------------------------------------------	virtual void WriteTo(CStream_AC* aStream) const;		// OVERRIDE this; writes specification of designator to stream	virtual void ReadFrom(CStream_AC* aStream);		// initializes this object from a stream specificationpublic:	CViewRect fDesignation;							// CViewRect representing designation};//----------------------------------------------------------------------------------------// TRegionDesignator//----------------------------------------------------------------------------------------class EXPORT_MA TRegionDesignator : public TDesignator// TRegionDesignator objects designate data that can be specified// by a region, for example, portions of a bitmap or noncontiguous// data.  The caller must create the region, and the designator optionally frees the// region. In general, the TStream passed into this object should be a THandleStream.{	MA_DECLARE_CLONE;	public:	//----------------------------------------------------------------------------------------	// ¥Êcontructors/destructors	//----------------------------------------------------------------------------------------	TRegionDesignator(RgnHandle itsDesignation = NULL, bool freeRegionOnFree = true);		// Constructor			TRegionDesignator(const TRegionDesignator& inRegionDesignator);		// Copy Constructor	virtual ~TRegionDesignator();		// Frees fDesignation and calls Inherited::Free.	//----------------------------------------------------------------------------------------	// ¥Êutility methods	//----------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;		// returns the signature for this class	virtual Size GetSize() const;		// returns the size of this designation	virtual ContainmentType IsContained(TDesignator* aDesignator);		// returns a ContainmentType indicating to what extent aDesignator is contained in this	virtual void SetDesignationRect(const CRect_AC& theDesignation);		// sets fDesignation to contain this rectangle	inline CRect_AC GetBounds() const { return fDesignation.Bounds(); }	// Return the smallest enclosing rectangle that contains the designation	//----------------------------------------------------------------------------------------	// ¥ saving	//----------------------------------------------------------------------------------------	virtual void WriteTo(CStream_AC* aStream) const;		// OVERRIDE this; writes specification of designator to stream	virtual void ReadFrom(CStream_AC* aStream);		// initializes this object from a stream specificationpublic:	bool fFreeRegionOnFree;							// whether to free the RgnHandle when this                                                	// designator is freed	CTempRegion_AC fDesignation;					// rgn representing designation};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/09/96	Removed UStreamable to a separate file from UObject		 4/26/96	Made C/CViewPoint/Rect default constructors set the point/rect to a cleared value. Made		 					clearing very fast/compact. Eliminated the now unnecessary hand clearing of C/CViewPoint/Rects.		----- R10 -----		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. 		 7/14/94	Merged with Ken Ryall's MAScript changes. */
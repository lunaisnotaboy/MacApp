// UErrorMgr.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/02/01	Update copyright.		08/15/00	Add a convenience macro for generating temp UPP classes. 		05/15/00	CW Pro 6: namespacing. 		10/07/99	Use TemplateTempUPP_AC instead of TmplTmpRtnDescrptr.		09/23/99	Carbon: Change TmplTmpRtnDescrptr to TmplTmpOpaqueRtnDescrptr.		03/24/99	Update copyright notice.		01/18/99	Changed kBailOutNow from global to enum.*/#ifndef __UERRORMGR__#define __UERRORMGR__// ACS#ifndef __TemplateUPP_AC__#include "TemplateUPP_AC.h"#endif// MacApp#ifndef __MACAPPTYPES__#include "MacAppTypes.h"#endif#ifndef __UCOREERRORMGR__#include "UCoreErrorMgr.h"#endif// Toolbox#ifndef __DIALOGS__#include <Dialogs.h>#endif//----------------------------------------------------------------------------------------// Types//----------------------------------------------------------------------------------------TEMP_UPP(ModalFilter) CTempModalFilterUPP;//----------------------------------------------------------------------------------------// Global variables//----------------------------------------------------------------------------------------EXPORT_MA extern ModalFilterProcPtr gMacAppAlertFilter;	// The default filter proc. Will be automatically set to point to MacAppAlertFilter.    // Set this to NULL in your code if you don't want it or call it from your own filter    // proc if you just want to add behavior. Someday we will fix all this to be Object    // based.EXPORT_MA extern bool gInFilter;	// Flag that is set when we are in the MacAppAlertFilter, used to indicate if we    // should re-enter MacApp to handle updates or notEXPORT_MA extern bool gInhibitNestedHandling;	// True if Failure was executed and the outer- most event loop has not been reached or    // the ErrorAlert routing has not yet shown the failure to the user. Also may be set    // to true to inhibit continued operation of MacApp Activate/ Update handling and    // idling during MacAppAlerts.//----------------------------------------------------------------------------------------// Global routines//----------------------------------------------------------------------------------------EXPORT_MA void ErrorAlert(OSErr err, long message);	// Displays an error alert box. The message displayed in the alert is constructed base    // upon err and message, as described in the "Failure Handling" section of the    // "Cookbook" chapter of the MacApp manual.EXPORT_MA short MacAppAlert(ResNumber alertID, ModalFilterProcPtr filterProc);	// Does SetCursor(arrow) before calling Alert.EXPORT_MA void StdAlert(ResNumber alertID);	// no filterProc, reply ignoredenum{	kBailOutNow = true};EXPORT_MA void BailOutAlert(const CException_AC& theException,							bool exitProgram = kBailOutNow);	// Bail-out alert function used to notify the user of reason for abnormal exit.EXPORT_MA void BailOutAlert(const std::exception& ex, bool exitProgram = kBailOutNow);EXPORT_MA void BailOutAlert(ResNumber alertID = phUnknownFatalError,							bool exitProgram = kBailOutNow);	#endif // __UERRORMGR__/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		12/08/95	Move a lot of stuff to UCoreErrorMgr. 		----- 3.3 -----		11/02/95	Added error errNoPagesToPrint [-20012] and errTooManyChars [-20013].		09/25/95	[1276050] Added errAECantSetReadOnly [-21017] and		  					errAENoSuchProperty [-21018].		----- 3.3b1 -----	    08/14/95	added resnumbers for phNoSubject, phNoRecipient		08/09/95	Incorporated apple event related errors.		07/05/95	Cosmetic. 		02/22/95	1184041 Change nil to NULL		09/14/94	Added "const phNoNextLetter" as part of Ken							Ryall's AOCE support.*/
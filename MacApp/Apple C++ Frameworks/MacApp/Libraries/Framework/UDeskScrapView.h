// UDeskScrapView.h// Copyright © 1984-2001 by Apple Computer, Inc. All rights reserved./*	Change History		05/01/01	Protect the fields. 		04/23/01	Change fScrapCount to a long so the same field can be used for Carbon. 		01/02/01	Update copyright.		03/24/99	Update copyright notice.*/#ifndef __UDESKSCRAPVIEW__#define __UDESKSCRAPVIEW__// MacApp#ifndef __UGEOMETRY__#include "UGeometry.h"#endif#ifndef __UVIEW__#include "UView.h"#endif//----------------------------------------------------------------------------------------// Forward and external class declarations//----------------------------------------------------------------------------------------class TDocument;//----------------------------------------------------------------------------------------// TDeskScrapView: The default view in the Clipboard, able to display only standard text// and pictures.//----------------------------------------------------------------------------------------class EXPORT_MA TDeskScrapView : public TView// TDeskScrapView provides the basic functionality necessary for// displaying the Macintosh desk scrap. Normally you will use TDeskScrapView// to display 'TEXT' or 'PICT' data in Clipboard windows.//    You typically do not call the methods of this class. You must// override the CheckScrapContents and Draw methods of TDeskScrapView// if you wish to support other data formats.{	MA_DECLARE_CLASS;	public:	//------------------------------------------------------------------------------------	// Initialization and destruction	//------------------------------------------------------------------------------------	TDeskScrapView();		// Constructor			virtual ~TDeskScrapView();		// Destructor. 	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);		// Initialize the fScrapCount.	//------------------------------------------------------------------------------------	// Other methods	//------------------------------------------------------------------------------------	virtual void CheckScrapContents();		// Checks the contents of the deskscrap.	virtual CViewRect CalcMinFrame();		// Responsible for calculating the correct size for the deskscrap view.	virtual void Draw(const CViewRect& area);		// Handles the drawing of the contents of the deskscrap, which by default is        // either a PICT or text.	virtual void SuperViewChangedFrame(const CViewRect& oldFrame,											const CViewRect& newFrame,											bool invalidate);		// If our superview changes size then call AdjustSize & ForceRedraw.	virtual void WriteToDeskScrap();		// This view represents data that is already written to the deskscrap, so this        // does nothing.	//------------------------------------------------------------------------------------	// static member functions	//------------------------------------------------------------------------------------protected:	static bool LookForScrapType(ResType theResType, ResType& resTypeFound);	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------#if qBackwardCompatiblepublic:#elseprotected:#endif	Handle	fDataHandle;		// The handle to the desk scrap. If this field is not NULL,								// it contains either a PicHandle to the 'PICT' data or a								// handle to the 'TEXT' data.	long	fScrapCount;		// The scrap count of the scrap represented by the view. MacApp								// uses fScrapCount to determine whether the contents of the								// desk scrap have changed.	bool	fHavePicture;		// true if the data in the desk scrap is of type 'PICT'; false								// if not.	bool	fHaveText;			// true if the desk scrap is of type 'TEXT'; false if not.	//----------------------------------------------------------------------------------------	// static data members	//----------------------------------------------------------------------------------------public:	static bool gVarClipPicSize;		// true if pictures in the Clipboard are treated as being of variable size, depending		// on the window size. The default value false causes pictures in the Clipboard to be		// drawn and pasted at the size defined in the 'PICT' resource.};#endif/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		05/03/96	Removed TDeskScrapView::Free. Now we check in TView::FreeFromClipboard instead.		----- R10 -----		03/28/96	CalcMinFrame now returns a CViewRect		 3/01/96	Re-arranged data members, natural alignment results in 		 					reduced storage size.		----- 3.3b1 -----		 3/16/95	Add back the Free method to prevent the clip orphanage from		 					being freed. 		 3/13/95	Change Free methods to destructors. 		02/22/95	1184041 Change nil to NULL*/
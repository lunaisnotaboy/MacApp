// UClipboardMgr.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		07/20/01	Add a WriteToDeskScrap utility method. 		04/25/01	Replace ClearCurrent with ClearForPutting. 		04/23/01	Redesigned CClipboard_AC for Carbon. 					Move fOldScrapStuff and fNewScrapStuff out of CClipboard_AC. 					Now they are CClipboard_AC instances. 					Remove error return from PutDeskScrapData. Errors are thrown. 					Remove GetClipboard accessor. Add methods that call through					to CClipboard_AC as appropriate. 					Protect all the fields and add accessors for the ones that					needed them. 		01/02/01	Update copyright.		03/16/00	Use EXPORT_CLASS_MA.		11/04/99	Carbon: Move PutDeskScrapData API back to what it was (remove ScrapRef).					The clipboard will get the current scrap, instead of the caller.		10/27/99	Add a GetClipboard(), GetClipboardView, GetClipboardDocument accessor routines.		07/23/99	[C] PutDeskScrapData API changed.		03/24/99	Update copyright notice.		10/08/98	Merge TCommandHandler into TEventHandler. */#ifndef __UCLIPBOARDMGR__#define __UCLIPBOARDMGR__// ACS#ifndef __MDependable_AC__#include "MDependable_AC.h"#endif// MacApp#ifndef __UBEHAVIOR__#include "UBehavior.h"#endif#ifndef __UVIEW__#include "UView.h"#endif#ifndef __UWINDOW__#include "UWindow.h"#endif// ACS#ifndef __CClipboard_AC__#include "CClipboard_AC.h"#endif// Toolbox//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class TEventHandler;class TView;class TWindow;class CClipboard_AC;EXPORT_CLASS_MA(CAutoPtr_AC<CClipboard_AC>)EXPORT_CLASS_MA(CAutoPtr_AC<TWindow>)EXPORT_CLASS_MA(CAutoPtr_AC<TView>)//----------------------------------------------------------------------------------------// TClipboardMgr: This object is responsible for interaction with the scrap manager.//----------------------------------------------------------------------------------------const IDType kClipboardManager = 'clip'; // The behavior identifier class EXPORT_MA TClipboardMgr : public TBehavior, public MDependable_AC// The TClipboardMgr object is a behavior that manages the application's// interaction with the Scrap Manager. The methods of the TClipboardMgr// manage the scrap.//    MacApp creates a single TClipboardMgr object for you when// the application starts and assigns it to gClipboardMgr. This// object automatically provides a Show Clipboard menu item that// displays 'TEXT' and 'PICT' data in the scrap. You must implement// additional methods if you want to show a private scrap type,// or if you want to provide copy, cut, or paste operations for// any kind of data.//    If you want to show data in a private scrap format, you must// override your application's MakeViewForAlienClipboard method// to return a view that contains data in the private scrap format.//    If you want to implement copy, cut, or paste operations, you// must provide a DoSetupMenus method for your view that calls gClipboardMgr->CanPaste// for each kind of data that can be placed in the clipboard. You// must also implement the Copy, Cut, and Paste commands in your// view's DoMenuCommand method. See TTEView::DoMenuCommand for an// example.{	MA_DECLARE_CLASS;	public:	//------------------------------------------------------------------------------------	// Initialize, I<Method>.	//------------------------------------------------------------------------------------	TClipboardMgr();		// Constructor	virtual ~TClipboardMgr();		// Destructor			//------------------------------------------------------------------------------------	// Rest of methods.	//------------------------------------------------------------------------------------	virtual void Close();	virtual void Launch();	virtual long GetDataToPaste(Handle aDataHandle, ResType& dataType);		// Returns the number of bytes in the paste data. Will signal Failure if it gets        // an error.	virtual TWindow* MakeClipboardWindow();		// Called by to make the clipboard window. ClipView is the view to be initially        // installed in the clipboard--it is fClipOrphanage.	virtual TView* MakeViewForAlienClipboard();		// Calls the application's MakeViewForAlienClipboard.If it returns a view then        // that view is returned to our caller else it returns fClipOrphanage, which is        // capable of displaying scrap of type TEXT or PICT. Override this to create a        // view to handle other types of scrap data.	void ClearForPutting();		// Must be called before a cut or copy operation. 	void PutFlavor(ScrapFlavorType flavorType, ScrapFlavorFlags flavorFlags,				   Size flavorSize, const void* flavorData)		{ fScrapStuff.PutFlavor(flavorType, flavorFlags, flavorSize, flavorData); }	void PutScrap(Size flavorSize, ScrapFlavorType flavorType, const void* flavorData)		{ fScrapStuff.PutScrap(flavorSize, flavorType, flavorData); }	void PutDeskScrapData(ResType aResType, Handle aDataHandle);		// Call this from your TCommand method 'WriteToDeskScrap' (for a Command which        // changes the Clipboard) to write out data to the actual Desk Scrap. The return        // code from the Scrap Manager is returned as the function value -- will be noErr        // unless something went wrong. This procedure leaves aDataHandle UNLOCKED. Rather        // than calling this, you can call the ToolBox routine PutScrap yourself	virtual void AboutToLoseControl(bool convertClipboard);	virtual void AbsorbScrapStuff();		// Called to retrieve the current InfoScrap record from low memory. This will be        // used to determine if the scrap has changed.	virtual void WriteToDeskScrap();		// Export the clipboard view to the desk scrap.	virtual void CheckDeskScrap();		// Checks to see if the desk scrap has changed by calling AbsorbScrapStuff and        // comparing the current scrap change count with the previous change count. If the        // scrap has changed, then the current clipboard becomes the undo clipboard and        // ReadFromDeskScrap is called to read in the new desk scrap.	virtual void CanPaste(ResType aClipType);		// Call this in your SetUpMenus code to register an ability to paste a particular        // type of Clipboard data	virtual void DoSetupMenus();		// Handles the show/hide clipboard command and enabling of paste	virtual void DoMenuCommand(CommandNumber aCommandNumber);		// Handles the show/hide clipboard command	virtual void ReadFromDeskScrap();		// Called at launch and when the desk scrap changes. Calls        // MakeViewForAlienClipboard, then ClaimClipboard	virtual void RegainControl(bool checkClipboard);		// Called when switching in or when leaving a desk accessory	virtual void SetClipView(TView* clipView, TEventHandler* itsContext);		// Installs the given view into the clipboard window.	TWindow* GetClipWindow() const	{ return fClipWindow; }	TView* GetClipOrphanage() const	{ return fClipOrphanage; }		void ReleaseClipOrphanage()	{ fClipOrphanage.release(); }		TView* GetClipView() const		{ return fClipView; }	TView* GetClipboardView() const	{ return fClipView; }		TDocument* GetClipboardDocument() const	{		return fClipView ? fClipView->GetDocument() : NULL;	}	TEventHandler* GetClipContext() const	{ return fClipContext; }	void ResetClipContext()	{ fClipContext = NULL; }	void ResetClipType()	{ fGotClipType = false; }		// Resets fGotClipType so the Paste menu item will be disabled 		// until a call to CanPaste finds the specified type in the scrap. 	long GetScrapCount() const	{ return fScrapStuff.GetScrapCount(); }		// Gets the current scrap count. In Carbon it's actually a scrap reference,		// but let's not quibble. 	long GetFlavorSize(ScrapFlavorType flavorType) const	{ return fScrapStuff.GetFlavorSize(flavorType); }	void GetFlavorData(ScrapFlavorType flavorType, Size* byteCount, void* destination) const		{ fScrapStuff.GetFlavorData(flavorType, byteCount, destination); }	void GetScrap(Handle destination, ScrapFlavorType flavorType) const		{ fScrapStuff.GetScrap(destination, flavorType); }	void WrittenToDeskScrap()	{ fClipWrittenToDeskScrap = true; }		// Used by TDeskScrapView. Violates encapsulaton. Will be eliminated. 	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------protected:	CClipboard_AC			 	fOldScrapStuff;	// The previous scrap.	CClipboard_AC				fScrapStuff;	// The current scrap. 	CAutoPtr_AC<TWindow>		fClipWindow;	// The window that holds the Clipboard display.	CAutoPtr_AC<TView>			fClipOrphanage;	// The view that represents the Clipboard when the application cannot display the Clipboard's contents.	TView*						fClipView;		// The view that is currently associated with the Clipboard.	TEventHandler*				fClipContext;	// The context of the command which created the current clipboard view.	ResType						fPrefClipType;	// The preferred data type. This field is set by TClipboardMgr::CanPaste.	bool						fGotClipType;	// true if the data in the Clipboard can be pasted.	bool						fClipWrittenToDeskScrap;  // true if the data shown by the Clipboard view has been written to the desk scrap.};//----------------------------------------------------------------------------------------// Definitions for externally available variables and functions.//----------------------------------------------------------------------------------------EXPORT_MA extern  TClipboardMgr* gClipboardMgr;EXPORT_MA extern  void InitUClipboardMgr();	// called to initialize the clipboard manager#endif // __UCLIPBOARDMGR__/*	Historical Change Record:		----- R13 -----		06/29/97	CDependable_AC.h is now MDependable_AC.h.  Changed include.		06/05/97	EXPORT_AC, EXPORT_MA, no-load fixes, Memory_AC, Windows_AC, no CW11orLATER, removed segmentation, etc...		04/13/97	Update List and Dependency names for ACS.		03/25/97	AC_ to _AC Name change.		03/24/97	Migrated Dependencies and Lists to ACS		03/07/97	Massive Changes: Use Exceptions_AC, new to TH_new, use Blah_AC,							Update Copyright, Moved Obsolete APIs into Compatibility.h, etc..		09/19/96	Eliminate all memory leaks identified by TidyHeap.		08/13/96	Use constructors for all TBehavior descendents		08/08/96	Reduce number of orphaned objects at quit-time by using CAutoPtr_AC with static pointers		07/15/96	Boolean->bool		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		04/15/96	Made TClipboardMgr MDependable_AC		----- R10 -----		 3/01/96	Re-arranged data members, natural alignment results in 		 					reduced storage size.		----- 3.3b1 -----		 3/13/95	Change Free methods to destructors. */
//----------------------------------------------------------------------------------------// USubstitution.cpp// Copyright © 1988-2001 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		01/24/01	NO CHANGE - TESTING MAReplaceText.		01/05/01	Change to use CString_AC instead of std::string.		01/02/01	Update copyright.		12/26/00	[W] Remove Windows customizations.		12/20/00	Added a temporary MAReplaceText that takes CStr255_AC.		12/15/00	Replace CStr255_AC with string.  This required modifying MAReplaceText.		11/13/00	[PB] sizeof is unsigned. 		08/01/00	[W] Added MAParamText.		05/15/00	CW Pro 6: namespacing. 		02/22/00	[W] Temporarily short-circuited.		03/24/99	Update copyright notice.		03/19/99	Removed commented out TAssociation definition of pParamText.		01/19/99	Removed old TAssociation code.		12/18/98	Replaced use of TAssociation with map.*/#ifndef __USUBSTITUTION__#include "USubstitution.h"#endif// ACS#ifndef __CListIterator__#include "CListIterator_AC.h"#endif// MacApp#ifndef __CTidyHeap_AC__#include "CTidyHeap_AC.h"#endif// Standard C++ Library#ifndef _MAP#include <map>#endif#ifndef _ALGORITHM#include <algorithm>#endifusing std::map;//----------------------------------------------------------------------------------------typedef map<CString_AC, CString_AC>	StringMap;typedef StringMap::iterator			StringMapIter;	typedef StringMap::const_iterator	StringMapConstIter;	StringMap	pParamTxt;//========================================================================================// GLOBAL Procedures//========================================================================================//----------------------------------------------------------------------------------------// MAParamText: //----------------------------------------------------------------------------------------void MAParamText(const CString_AC& keyStr, const CString_AC& valueStr)// MAParamText is used to register key strings and substitute substrings that can be used// later in calls to MAReplaceText.//// keyStr:		A string representing the substring value for which to search each string//				passed to MAReplaceText.  We recommend that you use unique key values//				so that accidental substitutions do not take place.  For example, using//				'^errcode' would be better than 'errcode'.// valueStr:	The string which will replace each occurance of the keyStr whenever//				MAReplaceText is called.//// Call?		As needed.{	pParamTxt[keyStr] = valueStr;}//----------------------------------------------------------------------------------------// MAReplaceText: //----------------------------------------------------------------------------------------void MAReplaceText(CString_AC& theText)// MAReplaceText will scan the string looking for substrings that have been registered// via calls to MAParamText, replacing them with the alternate substring.  Note that// it is certainly possible to have substitutions made earlier on in the scan of the// supplied string be replaced or modified by later key substitutions.  This behavior// cannot be relied on the be consistent since you have no control over the ordering// of the substitution map.//// theText:		A string for which all substitutable strings are too be replaced.//// Call?		As needed.{	typedef StringMapConstIter TextTag;	for (TextTag aTag = pParamTxt.begin(); aTag != pParamTxt.end(); ++aTag)	{		CString_AC::size_type index;		while ((index = theText.find(aTag->first)) != CString_AC::npos)			theText.replace(index, aTag->first.size(), aTag->second);	}}//----------------------------------------------------------------------------------------// End of USubstitution.cpp/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		06/19/96	Found and removed some more declarations in while loops by turning							on "ARM conformance" in CW. This disables the ability to declare in the expression of a while. 		06/19/96	SCpp and MrCpp can't handle declarations in the expression portion of while loops							so we just changed them all to for loops		04/18/96	Use post-increment with iterators in while loops		04/16/96	Use more "while" style iteration		----- R10 -----		 12/4/95	Split off from UDialog. */
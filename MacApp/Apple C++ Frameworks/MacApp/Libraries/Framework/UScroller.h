// UScroller.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/24/01	[W] Added ContainsMouse.		01/02/01	Update copyright.		03/16/00	Use EXPORT_CLASS_MA.		03/24/99	Update copyright notice.*/#ifndef __USCROLLER__#define __USCROLLER__// MacApp#ifndef __UGEOMETRY__#include "UGeometry.h"#endif#ifndef __UVIEW__#include "UView.h"#endif#ifndef __USCROLLBAR__#include "UScrollBar.h"#endif//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class TDocument;class TScrollerScrollBar;class TToolboxEvent;EXPORT_CLASS_MA(CAutoPtr_AC<TScroller>)EXPORT_CLASS_MA(CAutoPtr_AC<TScrollerScrollBar>)//----------------------------------------------------------------------------------------// TScroller: A view which can "scroll" its contents by translating coordinates.//----------------------------------------------------------------------------------------class EXPORT_MA TScroller : public TView// A TScroller is a view whose specialty is coordinate adjustments.// To create a scrolling area, you install a TScroller object as// a subview of a window and then install the view that actually// draws in the window as a subview of the TScroller object.//    The TScroller object handles messages from TScrollerScrollBar// objects by adjusting coordinate-plane offsets. When, for example,// the user clicks the Down Arrow in the vertical scroll bar, the// window's TScroller object adjusts its coordinate plane so that// all contents are drawn further from the bottom of the window.//    For example, given a scroller whose size is 100 pixels in// both directions and a subview located at (0,0) whose size is// (1000,1000) pixels, a translation value of (300,500) will display// the part of the subview defined by the rectangle (300,500)/(400,600).//    A scroller may refer to a vertical and horizontal scroll bar,// whose values are synchronized with the scroller's translation// values. Furthermore, it is possible to scroll without using scroll// bars by calling the appropriate scroller methods to change the// translation value. An example would be a view that tracks the// mouse to implement automatic scrolling when the mouse strays// beyond the borders of the visible area.{	MA_DECLARE_CLASS;	public:	//------------------------------------------------------------------------------------	// Creation/Destruction Methods	//------------------------------------------------------------------------------------	TScroller(const CViewPoint& itsLocation = gZeroVPt,								  const CViewPoint& itsSize = gZeroVPt,								  SizeDeterminer itsHSizeDet = sizeVariable,								  SizeDeterminer itsVSizeDet = sizeVariable,								  const CViewPoint& itsMax = gZeroVPt,								  bool wantHorzSBar = false,								  bool wantVertSBar = false);		// Constructor			TScroller(const TScroller& inScroller);		// Copy Constructor	virtual void CreateScrollBar(VHSelect itsDirection);		// Called to create a scrollbar in 'itsDirection'.	virtual ~TScroller();		// If there are attached scrollbars frees them before calling Inherited::Free.	//------------------------------------------------------------------------------------	// Standard signature support.	//------------------------------------------------------------------------------------	virtual IDType GetStandardSignature() const;	// override 		// Returns this class's standard signature.	//------------------------------------------------------------------------------------	// Stream I/O protocol support.	//------------------------------------------------------------------------------------	virtual void ReadFields(CStream_AC* aStream);	// override 		virtual void WriteFields(CStream_AC* aStream) const;	// override 	//------------------------------------------------------------------------------------	// SubView Management Methods	//------------------------------------------------------------------------------------	virtual void DoPostCreate(TDocument* itsDocument);		// Called in a post-order traversal of a newly-created view hierarchy			virtual void AddedASubView(TView* theSubView);		// Calls Inherited::AddedASubView and then calls 'theSubView' to BeInScroller passing        // this as the scroller to be in.	virtual void RemovedASubView(TView* theSubView);		// Calls BeInScroller with a parameter of NULL, for 'theSubView', before calling        // Inherited::RemovedASubView.	virtual void ForceRedraw();		// Invalidates the entire view forcing a redraw.	//------------------------------------------------------------------------------------	// Size Methods	//------------------------------------------------------------------------------------	virtual void SetFrame(const CViewRect& newFrame, bool invalidate);		// Resizes the scroller to newFrame, redrawing if 'invalidate' is true.	//------------------------------------------------------------------------------------	// Location Methods	//------------------------------------------------------------------------------------	virtual void AdjustScrollLimits(bool drawScrollBars);		// Adjust the scrolling limits and redraws the scroll bars.	virtual void AdjustScrollBars(bool invalidate);		// Resizes and re-Locates the scrollbars if required.	virtual void SubViewChangedFrame(TView* theSubView,											const CViewRect& oldFrame,											const CViewRect& newFrame,											bool invalidate);		// Adjust the scroll limits based on the size change of the subviews.	virtual void SetScrollLimits(const CViewPoint& scrollLimit,										bool drawScrollBars);		// Sets the scroll of the attached scrollbars, if there are any.	virtual void SetScrollParameters(const CViewPoint& units,											bool horzConstraint,											bool vertConstraint);		// Sets up the fScrollUnit and fConstrain fields of the scroller.	//------------------------------------------------------------------------------------	// Scrolling Methods	//------------------------------------------------------------------------------------	virtual void HaveScrollBar(TScrollerScrollBar* theScrollBar,									  VHSelect direction);		// Sets the fScrollbar field in the given 'direction' to 'theScrollBar'.	virtual void ScrollDraw(const CViewPoint& delta, bool invalidate);		// Method responsible for actually doing the scrolling by calling moving bits.        // Called by DoScroll.Invalidates newly revealed area if invalidate is true else        // updates the redraws the newly revealed area if invalidate is false. !!! NOTE        // this does not match the behaviour of other invalidate parameters because they        // interpret invalidate equals false to neither draw nor invalidate.Well we        // already know that the way that invalidate/ redraw/ don't-render control is        // handled needs _MAJOR_ overhauling anyways Sigh!	virtual void DoScroll(const CViewPoint& delta, bool redraw);		// Method responsible for handling the scrolling, calls ScrollDraw if required.	virtual ViewCoordinate ScrollStep(VHSelect vhs, short partCode);		// Depending on the 'partCode' returns the amount to be scrolled.	virtual ViewCoordinate ScrollRelative(VHSelect vhs, ViewCoordinate sBarValue);		// Returns the relative amount to scroll.	virtual void ScrollBy(const CViewPoint& delta, bool redraw);		// Called to scroll by 'deltaH & 'deltaV'.	virtual void SetLocalOrigin(const CViewPoint& newOrigin, bool redraw);		// Sets the local origin.  Preserves bits by scrolling to the required coordinates.		// Calls ScrollBy	virtual void AutoScroll(const CViewPoint& viewPt, CViewPoint& delta);		// Called during auto scrolling to return the delta to be scrolled.	virtual void RevealRect(const CViewRect& rectToReveal,								   const CViewPoint& minToSee,								   bool redraw);		// Shows 'rectToReveal' with the minimum amount to be seen set to 'minToSee' by        // calling ScrollBy.#if qDrag		virtual CViewPoint GetDragScrollDelta(const CViewPoint& mouseInWindow);#endif#if qMSWindows	virtual bool ContainsMouse(const CViewPoint& theMouse);		// Is the view point 'theMouse' currently in our view.#endif	//------------------------------------------------------------------------------------	// Miscellaneous Methods	//------------------------------------------------------------------------------------	virtual void DoKeyEvent(TToolboxEvent* event);		// Handles when the Home, End, PageUp & PageDown keys are used.	virtual TScroller* GetScroller(bool immediateSuperView);		// Returns this.	virtual void Draw(const CViewRect& area);		// Erases, since scrollers are opaque.	//------------------------------------------------------------------------------------	// data members	//------------------------------------------------------------------------------------	CViewRect fSBarOffsets;							// how much to offset the associated                                                // scroll bars from my bounds	CViewPoint fScrollLimit;						// the 'limit' of the subviews that should                                                // be scrollable	CViewPoint fMaxTranslation;						// the computed maximum amount of                                                // translation taking into account the                                                // visible portion of the view. This keeps                                                // us from scrolling the bottom of a view                                                // to the top of a window	CViewPoint fScrollUnit;							// increment of scroll	CAutoPtr_AC<TScrollerScrollBar> fScrollBars[2];	// associated scroll bars if any	IDType fScrollBarIdentifiers[2];			// associated scroll bars ids if any	bool fConstrain[2];							// should scrolling be constrained to a                                                // multiple of my scrollunit	bool fRespondsToFunctionKeys;				// if it responds to PgUp/ PgDn, etc.};#endif/*	Historical Change Record:		----- R13 -----		07/17/97	Added accessors for opacity		06/05/97	EXPORT_AC, EXPORT_MA, no-load fixes, Memory_AC, Windows_AC, no CW11orLATER, removed segmentation, etc...		04/14/97	Update for real xxx_cast operators.		03/27/97	Since TScrollers are opaque they should erase in the Draw cycle.		03/27/97	Add opacity to TScroller.		03/25/97	AC_ to _AC Name change.		03/21/97	Migrated to CViewRect, CViewPoint, ViewCoordinate.		03/07/97	Massive Changes: Use Exceptions_AC, new to TH_new, use Blah_AC,							Update Copyright, Moved Obsolete APIs into Compatibility.h, etc..		09/30/96	Fix cloning by turning clone methods into copy constructors		08/28/96	Remove superview/document arguments to TView (and derived) constructors.		08/16/96	Use constructors for all TView descendents		08/07/96	Goodbye TObject!		07/18/96	Added #include for UControl		07/15/96	Boolean->bool		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		 3/01/96	Re-arranged data members, natural alignment results in 		 					reduced storage size.		02/28/96	[1328100] Added TScroller::AdjustScrollLimits and used that							in several places.		 1/23/95	[114616] Many minor bugs in MacApp 3.3 final.		----- 3.3 -----		  8/5/95	1266278	TScroller overrides non-virtual AddSubView and RemoveSubView	     5/17/95	Add GetDragScrollDelta.		 3/13/95	Change Free methods to destructors. 		02/22/95	1184041 Change nil to NULL*/
// UScriptableObject.h// Copyright © 1984-2001 by Apple Computer. All rights reserved. /*	Change History		01/02/01	Update copyright.		02/23/00	MScriptableObject’s dtor is now always present and virtual.		12/08/99	Moved qAppleEventAware test below MacAppRequired.h include so					that the switch get set properly.		03/24/99	Update copyright notice.		01/15/99	CAEDesc_AC is now in ACS. 		11/12/98	[2279245] Included MacAppRequired.h for qAppleEventAware.		10/29/98	GetCommandContext can't be const since it might return 'this'.		10/08/98	Merge TCommandHandler into TEventHandler. */#ifndef __USCRIPTABLEOBJECT__#define __USCRIPTABLEOBJECT__// MacApp#ifndef __MACAPPTYPES__#include "MacAppTypes.h"#endif#ifndef __MacAppRequired__#include "MacAppRequired.h"#endif#if qAppleEventAware// Toolbox#ifndef __OSA__#include <OSA.h>#endif#if qAttachable	#ifndef __UOSASCRIPT__	#include "UOSAScript.h"	#endif#endif//----------------------------------------------------------------------------------------// Forward and external class declarations. //----------------------------------------------------------------------------------------class CAEDesc_AC;class TAppleEvent;class TEventHandler;class TScriptableObjectList;class CStream_AC;//----------------------------------------------------------------------------------------// MScriptableObject: Definition of an object that responds to scripting appleevents.//----------------------------------------------------------------------------------------const long kAllProperties = -1; // Constant to get all properties when calling MScriptableObject:GetContentsclass EXPORT_MA MScriptableObject// The MScriptableObject mixin class makes the object it is mixed// with scriptable by describing the object in terms that the Open// Scripting Library (OSL) can understand. Your scriptable object// reponds to script commands and can have custom scripts attached// to it.//    One important use of MScriptableObject is to provide a container// for other instances of MScriptableObject. This class also provides// utility methods to compare objects, work with marking lists,// and get and set object properties.//    The DoScriptCommand method is the central Apple event dispatcher// for MScriptableObject. DoScriptCommand calls the appropriate// "Do" handler for each Apple event it receives. Override the "Do"// methods to provide support for core Apple events such as clone,// close/quit, open, and create element. Add support for your own// Apple events by writing the handler and overriding DoScriptCommand// to associate a command number with your new handler. (The command// number is read in from the 'aedt' resource during the dispatcher// startup).//    See "Inside Macintosh:Interapplication Communication" for// Current information about scripting, object specifiers, and the// core Apple events.{//----------------------------------------------------------------------------------------// Construction//----------------------------------------------------------------------------------------public:	MScriptableObject();	MScriptableObject(DescType myClassID);	virtual ~MScriptableObject();//----------------------------------------------------------------------------------------// Object Description//----------------------------------------------------------------------------------------	virtual DescType GetOMClass();	// Override this to return something other than fOMClassID for the AppleEvent Object	// Model class for this object.		virtual DescType GetSpecifierForm();	// Override this to return something other than formIndex.	virtual bool MakeObjectSpecifier(CAEDesc_AC& theObjectSpecifier,									 DescType preferredForm);	// Creates an object specifier describing this object. This calls GetObjectProperty and	// GetContainer to support formName, formAbsolutePosition, and formUniqueID.	// You'll only need to override this method if you want to make other forms of	// object specifiers.//----------------------------------------------------------------------------------------// Accessing Contained Objects//----------------------------------------------------------------------------------------	virtual MScriptableObject* GetIndContainedObject(DescType desiredType,													 long index);	// Returns an object contained within this one based on its absolute	// position within the container. If you override this method to return	// contained objects TObject::GetContainedObject will use this to get	// one or more objects required by the different reference forms.	virtual MScriptableObject* GetObjectsWithinRange(DescType desiredType,													 MScriptableObject* startBounds,													 MScriptableObject* endBounds);	// Returns an object or list of objects contained within this one	// that lie within the startBounds object and the endBounds object.	// The default method assumes that the startBounds and endBounds objects	// are of the desiredType.	// You can override this to provide custom range access.	virtual MScriptableObject* GetAdjacentObject(DescType desiredType,												 DescType position);	// When the OSL resolves relative reference forms it first specifies an object	// then asks that object for the one before or after it. This method returns	// an object of the desiredType before or after it. The default implementation	// does this by calling GetIndContainedObject for its container until it	// finds itself, the gets the adjacent object by index. This only works if the	// desiredType is the same as this object.	// You can override this method by to provide custom access.	virtual MScriptableObject* GetContainedObject(DescType desiredType,												  DescType selectionForm,												  const CAEDesc_AC& selectionData);	// Returns an object contained within this one. If the desiredType is a property	// of this object, we create a TPropertyAccessor and return it.	// Later on, the TPropertyAccessor can act on the properties of this object.	virtual MScriptableObject* GetObjectsContainer();	// Returns the object that contains this one.	virtual long CountContainedObjects(DescType desiredType);	// Override this to return a count of the number of objects contained within this one.	virtual TScriptableObjectList* MakeNewMarkingList();	// Returns a TScriptableObjectList to use for marking objects contained within this one. //----------------------------------------------------------------------------------------// Comparison//----------------------------------------------------------------------------------------	virtual bool CompareScriptableObjects(DescType operation,										  const CAEDesc_AC& thingToCompare);	// Compares this object to something else. The default method does a simple	// direct comparison.	virtual bool CompareProperties(DescType whichProperty,								   DescType operation,								   const CAEDesc_AC& thingToCompare);	// Compares a property of this object to something else.//----------------------------------------------------------------------------------------// Handling AppleEvents//----------------------------------------------------------------------------------------		virtual void DoAEOnContainedObjects(TScriptableObjectList* theObjectList,										CommandNumber aCommandNumber,										TAppleEvent* message,										TAppleEvent* reply);	// If a complex object specifier resolves into a group of objects, the object	// containing them gets a chance to handle the AppleEvent for the group.	// The default method handles the SetData event by creating a command to 	// change the properties of all the objects.		virtual TEventHandler* GetCommandContext(const CommandNumber aCommandNumber);	// Returns a command handler that can be used as the context for commands	// on this object. This is used mainly for the set property command, which starts	// with a scriptable object and needs to find a command handler it can use as	// its context for undo/redo purposes. 	void HandleScriptCommand(CommandNumber	aCommandNumber,							 TAppleEvent* 	message,							 TAppleEvent* 	reply);	// Behaviors attached to the target object are given a chance to handle the event.	// If the event is not handled by a behavior, DoScriptCommand is called.																					virtual void DoScriptCommand(CommandNumber	aCommandNumber,								 TAppleEvent* 	message,								 TAppleEvent* 	reply);	// Aftering resolving an object specifier into an object this method is called.	// Based on the command number in the aCommandNumber it dispatches the event to	// one of the Core event support routines defined below.	// Override this method if this object supports events beyond the core suite.		// Methods called for the core suite events.	virtual void DoAEClone(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEClose(TAppleEvent* message, TAppleEvent* reply);	virtual void DoAECountElements(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAECreateElement(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEDelete(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEDoObjectsExist(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEGetClassInfo(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEGetEventInfo(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEGetData(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEGetDataSize(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAEMove(TAppleEvent* message, TAppleEvent* reply);	virtual void DoAEOpen(TAppleEvent* message, TAppleEvent* reply);	virtual void DoAEPrint(TAppleEvent* message, TAppleEvent* reply);	virtual void DoAESave(TAppleEvent* message, TAppleEvent* reply);									virtual void DoAESetData(TAppleEvent* message, TAppleEvent* reply);//----------------------------------------------------------------------------------------// Object Properties//----------------------------------------------------------------------------------------									virtual bool GetContents(CAEDesc_AC& theContents, const CAEDesc_AC& desiredType,							 long propertyFlags = kAllProperties);	// Returns the contents of this object.	virtual void SetContents(const CAEDesc_AC& theContents);	// Sets the contents of this object. If theContents is an AERecord,	// SetObjectProperty will be called for each property key.		virtual bool GetObjectProperty(CAEDesc_AC& thePropertyValue,								   DescType whichProperty,								   const CAEDesc_AC& desiredType);	// Gets property values for this object.  Returns true if the property was available,	// false if not.									virtual void SetObjectProperty(const CAEDesc_AC& thePropertyValue,								   DescType whichProperty);	// Override this method to set property values for this object.	virtual void GetSetPropertyInfo(DescType whichProperty,									CommandNumber& aCommandNumber,									bool& canUndo,									bool& causesChange,									TEventHandler* &theContext);	// When an AppleEvent is received that will set a property for this object a TSetPropertyCommand	// will be created to do it. You can override this method to provide the command number and the	// canUndo and causesChange flags for the command.	void SetPropertiesFromEvent(TAppleEvent* theEvent);	// Sets the properties of an object based on the data in the keyAEPropData parameter	// of the event.//----------------------------------------------------------------------------------------// Attached Scripts//----------------------------------------------------------------------------------------#if qAttachable	bool HasOSAScript();		// Return TRUE if this object has a script attached.	void FreeOSAScript();		// Throws out the script attached to this object.	void SetOSAScript(const CAEDesc_AC& scriptDesc);		// Attaches a script described in the scriptDesc	void GetOSAScript(CAEDesc_AC& scriptDesc,							  const CAEDesc_AC& desiredType);		// Returns the script attached to this object as the desiredType.	virtual bool HandleOSAEvent(CommandNumber aCommandNumber,								TAppleEvent* message,								TAppleEvent* reply);		// Gives an attached script a chance to handle an AppleEvent.		// Returns TRUE if the script handled the event.	void IdleOSAScript();		// Gives an attached script idle time so its idle event handler can do some work.		// Respects the script's fNextScriptIdle so you can call it as often as you like.	void ReadOSAScript(CStream_AC* aStream);		// Reads an attached script from aStream.	void WriteOSAScript(CStream_AC* aStream, DescType asType);		// Writes an attached script to aStream.#endif // qAttachable  public:	DescType			fOMClassID;	// The fOMClassID field contains the object model class identification									// number. The default MScriptableObject constructor initializes									// fOMClassID as typeNull. Provide your your object model class's									// identification number to the constructor to set up the fOMClassID									// field.#if qAttachable	COSAScriptCntPtr	fOSAScript;	// The fScript field is used to contain a script attached to this									// object. By default there is no script attached to the object. The									// fScript field is initialized as kOSANullScript by the									// MScriptableObject constructor.#endif};//----------------------------------------------------------------------------------------// MDefaultScriptableObject: Definition of an object that responds to scripting appleevents.//----------------------------------------------------------------------------------------class EXPORT_MA MDefaultScriptableObject : public MScriptableObject// The MDefaultScriptableObject class establishes the default target// for Apple events that do not specify a target object. The TApplication// class mixes with MDefaultScriptableObject to make sure that your// application always has a valid target object for any incoming// Apple event.//    Since an application that is recordable sends Apple events// to itself, the DoScriptCommand method overrides the parent method// to toggle recording on or off (see fRecording) when it gets a// command number containing either cAERecordingOn or cAERecordingOff.//    You can use the IsRecordingOn accessor method (which returns// the state of the protected, bool fRecording field) to determine// whether or not the user has enabled script recording. The constructor// for MDefaultScriptableObject sets the default state of fRecording// to false (recording is off).//    See the TOSADispatcher class for Current information on the// target chain.{public:	MDefaultScriptableObject();	MDefaultScriptableObject(DescType myClassID);														virtual ~MDefaultScriptableObject();		virtual void DoScriptCommand(CommandNumber		aCommandNumber,								TAppleEvent* 		message,								TAppleEvent* 		reply);	// Override		inline bool IsRecordingOn() { return fRecording; }protected:	bool	fRecording;	// The fRecording field keeps track of whether recording						// is on or off. It is initialized to false by the						// constructor. The DoScriptCommand method overrides						// its parent method to toggle fRecording on, if the						// command number passed to it is cAERecordingOn, or						// off, if the command number is cAERecordingOff.};#endif	// #if qAppleEventAware#endif // __USCRIPTABLEOBJECT__/*	Historical Change Record:		----- R11 -----		06/26/96	Install release marker.  Scope many globals.  Change TRUE/FALSE/NULL.		----- R10 -----		03/19/96	Added destructor for MDefaultScriptableObject so the compilers would quit							dropping generated destructors for it everywhere		11/02/95	Add MScriptableObject::GetCommandContext.							Conditionalize IsPendingAction on qOptimizeSendSelfAevt.		10/24/95	Remove GetBestType, GetDefaultType. Add GetSpecifierForm.		10/21/95	Add MScriptableObject::GetBestType and GetDefaultType.							Remove default value from preferredForm param passed to							MakeObjectSpecifier.		----- 3.3b1 -----		  9/5/95	TOSAScriptCntPtr -> COSAScriptCntPtr.		 8/14/95	Modified fOSAScript to use the reference counting mechanism.		 8/14/95	SetPropertiesFromEvent is void.		  8/9/95	Changed and moved error constants into UErrMgr.h so that		  					standard MacApp error specifications could be used.		  8/7/95	Moved error constants over from UScriptableObject.cp.  Removed		  					DoAERevert.		  8/3/95	Add DoAERevert. 		 7/28/95	Add destructor to free attached script. 		 7/27/95	Conditionalize attachability support. 		 7/25/95	Move COSAScript to its own unit, UOSAScript. Move attached							script functionality from MScriptableObject to new class							TOSAScript. Remove methods GetScriptingComponent, 							ExecuteOSAScript and GetOSAParent. 		  7/5/95	Cosmetic. 		 6/28/95	Reorder scripting method params.		 6/27/95	Change COSAScript cast operators to return non-const.		 6/13/95	Make the script class globals non-pointers. 		 					There is no fgNullScript. Use kOSANullScript instead. 		 6/12/95	Add MDefaultScriptableObject							Change OSAHandledEvent to HandleOSAEvent							Change HandleBehaviorScriptCommand to HandleScriptCommand							Reorder DoScriptCommand params							Change MScriptableObject::fEventHandler to fCommandHandler							Add COSAScript::fgGlobalScript, fgEmptyScript, fgSharedScript.							Add accessors for all three		 3/10/95	Add ClassDesc. 		  3/3/95	Changed prototye of COSAScript::SetScript to take		  					a "const CAEDesc_AC& scriptDesc" instead of		  					"const CAEDesc_AC scriptDesc" to fix CW complaints about		  					calls to ambiguous overloaded func when passing		  					a CAETempDesc.		 2/17/95	GetContents now returns a bool. 		 1/26/95	Include OSA.h. Add COSAScript. Add fScript, 							fNextScriptIdle and OSA flags to MScriptableObject. 							Remove ComparePropertyText. Add GetContents and							SetContents. Add desiredType to GetObjectProperty. 							Add attached scripts functions. 		 2/14/95	MScriptableObject::MakeObjectSpecifier now returns a bool.		 2/13/95	Changed return value of MScriptableObject::GetObjectProperty 							from void to bool.		----- 3.5d3 Release -----		 9/30/94	Add MScriptableObject::HandleBehaviorScriptCommand to							support passing apple events to behaviors.		 9/30/94	Add fEventHandler parameter and constructor							support to MScriptableObject.  This will							be used to allow behaviors attached to							eventhandlers to receive and handle apple							events.		 9/14/94	Added parameter to GetSetPropertyInfo to							fix problem with incorrect undo contexts.		 7/26/94	Include MacAppTypes. 		 7/14/94	Merged with Ken Ryall's MAScript changes. */
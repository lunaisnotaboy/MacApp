// UContextMenuBehavior.h// Copyright © 1997-2001 by Apple Computer, Inc. All rights reserved./*	Change History		01/02/01	Update copyright.		12/08/99	Moved qWantsContextualMenus below include of MacAppRequired.h so that					qWantsContextualMenus get defined		11/08/99	Change the API for GetContextualMenu so it has access to the click					event and can fill in all of the parameters for the menu. 		03/24/99	Update copyright notice.		01/15/99	CAEDesc_AC is now in ACS. 		11/12/98	[2279245] Included MacAppRequired.h for qWantsContextualMenus.*/#ifndef __UCONTEXTMENUBEHAVIOR__#define __UCONTEXTMENUBEHAVIOR__// MacApp#ifndef __MacAppRequired__#include "MacAppRequired.h"#endif#if qWantsContextualMenus#ifndef __UBEHAVIOR__#include "UBehavior.h"#endif#ifndef __UView__#include "UView.h"#endif// Externalclass CContextualMenu;//========================================================================================// GLOBAL Functions//========================================================================================EXPORT_MA extern void InitUContextMenuBehavior();//========================================================================================// CLASS TContextMenuBehavior//========================================================================================class EXPORT_MA TContextMenuBehavior : public TBehavior// Attach this behavior to your application to get contextual menu support.{	MA_DECLARE_CLASS;	public:	TContextMenuBehavior(IDType itsIdentifier = kNoIdentifier);		// Constructor	TContextMenuBehavior(const TBehavior& inBehavior);		// Copy Constructor			virtual ~TContextMenuBehavior();	//------------------------------------------------------------------------------------	// ¥ Event Filtering Methods	//------------------------------------------------------------------------------------	// ¥ Special hooks which are used when attached to a TApplication	virtual bool DoToolboxEvent(TToolboxEvent* event);		// When this is called by MacApp, theEvent is the event to be processed.		// If the event is a contextual menu event, handle contextual menu is called to		// get the target view set, and to show the contextual menu.		void DispatchContextualMenuEvent(TToolboxEvent* event);		// Handles the dispatch of contextual menu event.			TView* SetupContextualMenu(TToolboxEvent* event,							   CContextualMenu& contextualMenu);		// Finds the view that should handle the contextual menu click. Gives the view		// a chance to update its selection and set up the contextual menu. 	TView* FindHelpView(TView* thisView,						const CViewPoint& theMouse);		// Recursively finds the frontmost subview that handles help and contains the		// mouse. 	void HandleContextualMenuEvent(UInt32 inUserSelectionType,								   SInt16 inMenuID,								   UInt16 inMenuItem);		// Handles the result of a contextual menu. 	static bool HasContextualMenus();		// Returns whether contextual menus are available (system-wide)};#endif // qWantsContextualMenus#endif // __UCONTEXTMENUBEHAVIOR__
//----------------------------------------------------------------------------------------// CParseTerm.cpp// Copyright © 1999-2000 by Apple Computer, Inc. All rights reserved. //----------------------------------------------------------------------------------------/*	Change History:		10/13/00	CWP6 support: namespacing. 		04/18/00	Remove parse namespace. 		04/17/00	Rename nodes to terms. Remove string versions of Parse. 							Move CParseTerm and CParseToken to their own units. 							Remove pointer to parent term. */#ifndef __CParseTerm__#include "CParseTerm.h"#endif// Network#ifndef __CParseToken__#include "CParseToken.h"#endif#ifndef __TokenTools__#include "TokenTools.h"#endif// ACS#ifndef __CStdReporter_AC__#include "CStdReporter_AC.h"#endif// ANSIusing namespace std;//========================================================================================// CParseTermList//========================================================================================//----------------------------------------------------------------------------------------// Convenience constructors//----------------------------------------------------------------------------------------CParseTermList::CParseTermList(CParseTerm* term1) :	TemplateList_AC<CParseTerm>(1){	Insert(term1);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2) :	TemplateList_AC<CParseTerm>(2){	Insert(term1);	Insert(term2);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3) :	TemplateList_AC<CParseTerm>(3){	Insert(term1);	Insert(term2);	Insert(term3);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4) :	TemplateList_AC<CParseTerm>(4){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5) :	TemplateList_AC<CParseTerm>(5){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);	Insert(term5);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6) :	TemplateList_AC<CParseTerm>(6){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);	Insert(term5);	Insert(term6);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7) :	TemplateList_AC<CParseTerm>(7){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);	Insert(term5);	Insert(term6);	Insert(term7);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7, CParseTerm* term8) :	TemplateList_AC<CParseTerm>(8){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);	Insert(term5);	Insert(term6);	Insert(term7);	Insert(term8);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7, CParseTerm* term8, CParseTerm* term9) :	TemplateList_AC<CParseTerm>(9){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);	Insert(term5);	Insert(term6);	Insert(term7);	Insert(term8);	Insert(term9);}CParseTermList::CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7, CParseTerm* term8, CParseTerm* term9, CParseTerm* term10) :	TemplateList_AC<CParseTerm>(10){	Insert(term1);	Insert(term2);	Insert(term3);	Insert(term4);	Insert(term5);	Insert(term6);	Insert(term7);	Insert(term8);	Insert(term9);	Insert(term10);}//========================================================================================// CParseTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CParseTerm::CParseTerm(IDType identifier)	:	fIdentifier(identifier){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CParseTerm::~CParseTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CParseTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{#pragma unused (text, parentToken)	return false;}//----------------------------------------------------------------------------------------// MakeToken//----------------------------------------------------------------------------------------CParseToken* CParseTerm::MakeToken(const CConstTextToken& text) const{	return TH_new CParseToken(this, text);}//========================================================================================// COptionalTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------COptionalTerm::COptionalTerm(IDType identifier, CParseTerm& subterm)	:	CParseTerm(identifier),		fSubterm(subterm){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------COptionalTerm::~COptionalTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool COptionalTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	// Give the subterm a chance to parse. 	fSubterm.Parse(text, parentToken);	// Optional term always succeeds. 	return true;}//========================================================================================// CMultiTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CMultiTerm::CMultiTerm(IDType identifier, const CParseTermList& subterms,					   const CParseTerm* skipWhiteSpace)	:	CParseTerm(identifier),		fSubterms(subterms),		fSkipWhiteSpace(skipWhiteSpace){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CMultiTerm::~CMultiTerm(){}//========================================================================================// CSequenceTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CSequenceTerm::CSequenceTerm(IDType identifier, const CParseTermList& subterms,							 const CParseTerm* skipWhiteSpace)	:	CMultiTerm(identifier, subterms, skipWhiteSpace){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CSequenceTerm::~CSequenceTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CSequenceTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	CConstTextToken next(text);	bool result = false;	for (CParseTermList::Iterator iter(&fSubterms); iter; ++iter)	{		if (fSkipWhiteSpace)			fSkipWhiteSpace->Parse(next);		result = iter->Parse(next, parentToken);		if (!result)			break;	}		if (result)		text = next;	return result;}//========================================================================================// CSwitchTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CSwitchTerm::CSwitchTerm(IDType identifier, const CParseTermList& subterms,						 const CParseTerm* skipWhiteSpace)	:	CMultiTerm(identifier, subterms, skipWhiteSpace){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CSwitchTerm::~CSwitchTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CSwitchTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	CConstTextToken next(text);	bool result = false;	for (CParseTermList::Iterator iter(&fSubterms); iter; ++iter)	{		if (fSkipWhiteSpace)			fSkipWhiteSpace->Parse(next);		result = iter->Parse(next, parentToken);		if (result)			break;	}		if (result)		text = next;	return result;}//========================================================================================// CAnyOfTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CAnyOfTerm::CAnyOfTerm(IDType identifier, const CParseTermList& subterms,					   const CParseTerm* skipWhiteSpace)	:	CMultiTerm(identifier, subterms, skipWhiteSpace){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CAnyOfTerm::~CAnyOfTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CAnyOfTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	CConstTextToken next(text);	bool result = false;	bool foundAny;	do	{		foundAny = false;		for (CParseTermList::Iterator iter(&fSubterms); iter; ++iter)		{			if (fSkipWhiteSpace)				fSkipWhiteSpace->Parse(next);			bool subResult = iter->Parse(next, parentToken);			if (subResult)			{				foundAny = true;			}		}	}	while (foundAny);		if (result)		text = next;	return result;}//========================================================================================// CSetOfTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CSetOfTerm::CSetOfTerm(IDType identifier, const CParseTermList& subterms,					   const CParseTerm* skipWhiteSpace)	:	CMultiTerm(identifier, subterms, skipWhiteSpace){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CSetOfTerm::~CSetOfTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CSetOfTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	CConstTextToken next(text);	bool result = false;		CParseTermList remaining(fSubterms);	bool foundAny;	do	{		foundAny = false;		for (CParseTermList::Iterator iter(&remaining); iter; ++iter)		{			if (fSkipWhiteSpace)				fSkipWhiteSpace->Parse(next);			bool subResult = iter->Parse(next, parentToken);			if (subResult)			{				foundAny = true;				remaining.AtDelete(iter.CurrentIndex());				break;			}		}	}	while (foundAny);		if (result)		text = next;	return result;}//========================================================================================// CCharSetTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CCharSetTerm::CCharSetTerm(IDType identifier, const char* chars)	:	CParseTerm(identifier),		fChars(chars){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CCharSetTerm::~CCharSetTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CCharSetTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{#pragma unused (parentToken)	CConstTextToken localText(text);	while ((localText.length() > 0) && (fChars.find(*localText) != fChars.npos))	{		++localText;	}	bool result = (localText - text) > 0;	if (result)		text = localText;	return result;}//========================================================================================// CNonBreakingSpaceTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CNonBreakingSpaceTerm::CNonBreakingSpaceTerm(IDType identifier)	:	CCharSetTerm(identifier, " \t"){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CNonBreakingSpaceTerm::~CNonBreakingSpaceTerm(){}//========================================================================================// CWhiteSpaceTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CWhiteSpaceTerm::CWhiteSpaceTerm(IDType identifier)	:	CParseTerm(identifier){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CWhiteSpaceTerm::~CWhiteSpaceTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CWhiteSpaceTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{#pragma unused (parentToken)	CConstTextToken localText(text);	while ((localText.length() > 0) && isspace(*localText))	{		++localText;	}	bool result = (localText - text) > 0;	if (result)		text = localText;	return result;}//========================================================================================// CLiteralParseTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CLiteralParseTerm::CLiteralParseTerm(IDType identifier, const char* theLiteral)	:	CParseTerm(identifier),		fLiteral(theLiteral){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CLiteralParseTerm::~CLiteralParseTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------inline bool CompareUppers(const char c1, const char c2){	return toupper(c1) == toupper(c2);}bool CLiteralParseTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	bool result = false;		if (!text.AtEnd())		// Make sure there's something to compare.	{		CConstTextToken localText(text);#if FIXTHIS		if (fSkipWhiteSpace)			SkipWhiteSpace(localText);#endif		if (localText.length() >= fLiteral.length())		{			if (equal(fLiteral.begin(), fLiteral.end(), localText, CompareUppers))			{				result = true;				if (parentToken)				{					localText.SetLength(fLiteral.length());					parentToken->AddSubToken(MakeToken(localText));				}				text = localText + fLiteral.length();			}		}	}		return result;}//========================================================================================// CIntegerParseTerm//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CIntegerParseTerm::CIntegerParseTerm(IDType identifier)	:	CParseTerm(identifier){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CIntegerParseTerm::~CIntegerParseTerm(){}//----------------------------------------------------------------------------------------// Parse//----------------------------------------------------------------------------------------bool CIntegerParseTerm::Parse(CConstTextToken& text, CParseToken* parentToken) const{	bool result = false;	if (!text.AtEnd())		// Make sure there's something to compare.	{		unsigned long theInteger = 0;		CConstTextToken next = ParseInteger(text, &theInteger);		if (next != text)		{			// We found an integer.  Create a token object. 			result = true;			if (parentToken)			{				CConstTextToken integerText(text, 0, next - text);				parentToken->AddSubToken(TH_new CIntegerParseToken(this, integerText, theInteger));			}			text = next;		}	}	return result;}//========================================================================================// CIntegerParseToken//========================================================================================//----------------------------------------------------------------------------------------// constructor//----------------------------------------------------------------------------------------CIntegerParseToken::CIntegerParseToken(const CIntegerParseTerm* term,									   const CConstTextToken& text, 									   long value)	:	CParseToken(term, text),		fInteger(value){}//----------------------------------------------------------------------------------------// destructor//----------------------------------------------------------------------------------------CIntegerParseToken::~CIntegerParseToken(){}//----------------------------------------------------------------------------------------// End of CParseTerm.cpp
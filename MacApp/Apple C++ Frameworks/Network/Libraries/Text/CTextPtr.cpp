//----------------------------------------------------------------------------------------// CTextPtr.cpp// Copyright © 1996-2000 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		05/18/00	Remove TokenEntry related functions. 		02/07/00	Revised for new threads implementation. 		10/14/99	Added SetCharPtr debugging aid.  Added const to FindToken.		10/13/99	Adopted by MacApp.	Originally written by:	the cyberdogs*/#ifndef __CTextPtr__#include "CTextPtr.h"#endif// Bowser#ifndef __CTextBuffer__#include "CTextBuffer.h"#endif// ACS//========================================================================================// Global Functions//========================================================================================#if qDebug//----------------------------------------------------------------------------------------// TraceThrowIfNullTextPtr//----------------------------------------------------------------------------------------void TraceThrowIfNullTextPtr(const void* inPtr, const char* inFile, short inLine){	if (inPtr == NULL)	{		CException_AC ex(minErr);		ex.SetThrownFrom(inFile, inLine);		ex.DoThrowMessage();		throw ex;	}}#else//----------------------------------------------------------------------------------------// ThrowIfNullTextPtr//----------------------------------------------------------------------------------------void ThrowIfNullTextPtr(const void* inPtr){	if (inPtr == NULL)		ThrowError_AC(minErr);}#endif//========================================================================================// class CConstTextPtr//========================================================================================//----------------------------------------------------------------------------------------// CConstTextPtr constructor//----------------------------------------------------------------------------------------CConstTextPtr::CConstTextPtr()	:		fBuffer(NULL),		fOffset(0)		//fSize(0),		//fLockCount(0){#if qDebug		SetCharPtr();#endif}//----------------------------------------------------------------------------------------// CConstTextPtr constructor//----------------------------------------------------------------------------------------CConstTextPtr::CConstTextPtr(const CTextBuffer* inBuffer)	:		fBuffer(inBuffer),		fOffset(0)		//fSize(fBuffer ? fBuffer->GetSize() : 0),		//fLockCount(0){#if qDebug		SetCharPtr();#endif}//----------------------------------------------------------------------------------------// CConstTextPtr constructor//----------------------------------------------------------------------------------------CConstTextPtr::CConstTextPtr(const CTextBuffer* inBuffer, size_t offset)	:		fBuffer(inBuffer),		fOffset(offset)		//fSize(fBuffer ? fBuffer->GetSize() : 0),		//fLockCount(0){#if qDebug		SetCharPtr();#endif}//----------------------------------------------------------------------------------------// CConstTextPtr copy constructor//----------------------------------------------------------------------------------------CConstTextPtr::CConstTextPtr(const CConstTextPtr& rhs)	:		fBuffer(rhs.fBuffer),		fOffset(rhs.fOffset)		//fSize(rhs.fSize),		//fLockCount(rhs.fLockCount){#if qDebug		SetCharPtr();#endif}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator==(const CConstTextPtr& rhs) const//	Equality override for another CConstTextPtr{	return (fBuffer == rhs.fBuffer) && (fOffset == rhs.fOffset);}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator==(const void* ptr) const//	Equality override for a void*{	return addr() == ptr;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator!=(const CConstTextPtr& rhs) const//	Inequality override for another CConstTextPtr{	return (fBuffer != rhs.fBuffer) || (fOffset != rhs.fOffset);}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator!=(const void* ptr) const// Inequality override for a void*{	return addr() != ptr;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator>(const CConstTextPtr& rhs) const{	return addr() > rhs.addr();}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator>=(const CConstTextPtr& rhs) const{	return addr() >= rhs.addr();}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator<(const CConstTextPtr& rhs) const{	return addr() < rhs.addr();}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::operator<=(const CConstTextPtr& rhs) const{	return addr() <= rhs.addr();}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr& CConstTextPtr::operator++()//	Prefix increment override{	++fOffset;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr CConstTextPtr::operator++(int)//	Postfix increment override{	CConstTextPtr result = *this;	++fOffset;#if qDebug		SetCharPtr();#endif	return result;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr& CConstTextPtr::operator+=(difference_type increment)//	Increment assignment{	fOffset += increment;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr CConstTextPtr::operator+(difference_type increment) const//	Addition override for an integer{	CConstTextPtr result = *this;	result += increment;	return result;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr& CConstTextPtr::operator--()//	Prefix decrement override{	--fOffset;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr CConstTextPtr::operator--(int)//	Postfix decrement override{	CConstTextPtr result = *this;	--fOffset;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr& CConstTextPtr::operator-=(difference_type decrement)//	Decrement assignment{	fOffset -= decrement;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr CConstTextPtr::operator-(difference_type decrement) const//	Subtraction override for an integer{	CConstTextPtr result = *this;	result -= decrement;	return result;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr::difference_type CConstTextPtr::operator-(const CConstTextPtr& rhs) const//	Distance between{	Assertion_AC(fBuffer == rhs.fBuffer, NULL);	return fOffset - rhs.fOffset;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr& CConstTextPtr::operator=(const CConstTextPtr& rhs)//	Assignment override{	fBuffer = rhs.fBuffer;	fOffset = rhs.fOffset;	//fSize = rhs.fSize;	//fLockCount = rhs.fLockCount;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr& CConstTextPtr::operator=(const charT* ptr)//	Assignment override (NULL ptr only){	if (ptr == NULL)	{		fBuffer	= NULL;		fOffset	= 0;	}	else		ThrowError_AC(minErr);#if qDebug		SetCharPtr();#endif	return *this;}//	//----------------------------------------------------------------------------------------//	// CConstTextPtr:://	//----------------------------------------------------------------------------------------////	void CConstTextPtr::operator>>(char in)//	//	Input override//	{//		*get() = in;//	}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr::charT CConstTextPtr::operator*() const//	Indirection override{	const charT* ptr = get(1);	return ptr ? *ptr : '\0';}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr::operator const charT*() const//	Conversion override{	return get(1);}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------const CConstTextPtr::charT& CConstTextPtr::operator[](difference_type index) const//	Subscription override{	const charT* ptr = get(index);	ThrowIfNullTextPtr(ptr);	return ptr[index];}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::AtEnd() const{	return AssureData() == false;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::IsBufferComplete() const{	return fBuffer->IsComplete();}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr::difference_type CConstTextPtr::length() const{	return size() - fOffset;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr::difference_type CConstTextPtr::GetOffset() const{	return fOffset;}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------CConstTextPtr::size_type CConstTextPtr::size() const{	return fBuffer ? fBuffer->GetSize() : 0;}//	//----------------------------------------------------------------------------------------//	// CConstTextPtr:://	//----------------------------------------------------------------------------------------////	void CConstTextPtr::Place(const char in, difference_type index)//	{//		get(index)[index] = in;//	}//----------------------------------------------------------------------------------------// CConstTextPtr:://----------------------------------------------------------------------------------------bool CConstTextPtr::AssureData(difference_type assure) const{	assure += fOffset;	bool hasData = (assure < size());	if (!hasData && fBuffer)		hasData = fBuffer->AssureData(assure);	return hasData;}//----------------------------------------------------------------------------------------// CConstTextPtr::get//----------------------------------------------------------------------------------------const CConstTextPtr::charT* CConstTextPtr::get(difference_type assure) const{	return AssureData(assure) ? get() : NULL;}//----------------------------------------------------------------------------------------// CConstTextPtr::get//----------------------------------------------------------------------------------------const CConstTextPtr::charT* CConstTextPtr::get() const{	OSErr err = noErr;	charT* ptr = 0;	if (!fBuffer)		err = minErr;	else	{		ptr = fBuffer->get();		if (!ptr)			err = minErr;		else		{			if ((fOffset < 0) || (fOffset >= fBuffer->GetSize()))				err = dsChkErr;			else				ptr += fOffset;		}	}	ThrowIfOSErr_AC(err);	return ptr;}//----------------------------------------------------------------------------------------// CConstTextPtr::addr//----------------------------------------------------------------------------------------const CConstTextPtr::charT* CConstTextPtr::addr() const{	const charT* ptr = 0;	if (fBuffer)	{		ptr = fBuffer->get();		if (ptr)			ptr += fOffset;	}	return ptr;}#if qDebug	//----------------------------------------------------------------------------------------// CConstTextPtr::SetCharPtr//----------------------------------------------------------------------------------------char* CConstTextPtr::SetCharPtr(){	fCharPtr = 0;	if (fBuffer)	{		fCharPtr = fBuffer->get();		if (fCharPtr)		{			if ((fOffset < 0) || (fOffset >= fBuffer->GetSize()))				fCharPtr = reinterpret_cast<char*>(0xfbadbadf);			else				fCharPtr += fOffset;		}	}	return fCharPtr;}#endif//========================================================================================// class CTextPtr//========================================================================================//----------------------------------------------------------------------------------------// CTextPtr constructor//----------------------------------------------------------------------------------------CTextPtr::CTextPtr()	:		CConstTextPtr(){}//----------------------------------------------------------------------------------------// CTextPtr constructor//----------------------------------------------------------------------------------------CTextPtr::CTextPtr(CTextBuffer* inBuffer)	:		CConstTextPtr(inBuffer){}//----------------------------------------------------------------------------------------// CTextPtr constructor//----------------------------------------------------------------------------------------CTextPtr::CTextPtr(CTextBuffer* inBuffer, size_t offset)	:		CConstTextPtr(inBuffer, offset){}//----------------------------------------------------------------------------------------// CTextPtr copy constructor//----------------------------------------------------------------------------------------CTextPtr::CTextPtr(const CTextPtr& rhs)	:		CConstTextPtr(rhs){}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr& CTextPtr::operator++()//	Prefix increment override{	++fOffset;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr CTextPtr::operator++(int)//	Postfix increment override{	CTextPtr result = *this;	++fOffset;#if qDebug		SetCharPtr();#endif	return result;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr& CTextPtr::operator+=(difference_type increment)//	Increment assignment{	fOffset += increment;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr CTextPtr::operator+(difference_type increment) const//	Addition override for an integer{	CTextPtr result = *this;	result += increment;	return result;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr& CTextPtr::operator--()//	Prefix decrement override{	--fOffset;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr CTextPtr::operator--(int)//	Postfix decrement override{	CTextPtr result = *this;	--fOffset;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr& CTextPtr::operator-=(difference_type decrement)//	Decrement assignment{	fOffset -= decrement;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr CTextPtr::operator-(difference_type decrement) const//	Subtraction override for an integer{	CTextPtr result = *this;	result -= decrement;	return result;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr::difference_type CTextPtr::operator-(const CConstTextPtr& rhs) const//	Distance between{	return CConstTextPtr::operator-(rhs);}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr& CTextPtr::operator=(const CTextPtr& rhs)//	Assignment override{	fBuffer = rhs.fBuffer;	fOffset = rhs.fOffset;	//fSize = rhs.fSize;	//fLockCount = rhs.fLockCount;#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr& CTextPtr::operator=(const charT* ptr)//	Assignment override (NULL ptr only){	if (ptr == NULL)	{		fBuffer	= NULL;		fOffset	= 0;	}	else		ThrowError_AC(minErr);#if qDebug		SetCharPtr();#endif	return *this;}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr::charT CTextPtr::operator*() const//	Indirection override{	const charT* ptr = get(1);	return ptr ? *ptr : '\0';}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr::operator charT*()//	Conversion override{	return get(1);}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr::operator const charT*() const//	Conversion override{	return get(1);}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------CTextPtr::charT& CTextPtr::operator[](difference_type index)//	Subscription override{	charT* ptr = get(index);	ThrowIfNullTextPtr(ptr);	return ptr[index];}//----------------------------------------------------------------------------------------// CTextPtr:://----------------------------------------------------------------------------------------const CTextPtr::charT& CTextPtr::operator[](difference_type index) const//	Subscription override{	const charT* ptr = get(index);	ThrowIfNullTextPtr(ptr);	return ptr[index];}//----------------------------------------------------------------------------------------// CTextPtr::get//----------------------------------------------------------------------------------------CTextPtr::charT* CTextPtr::get(difference_type assure){	return AssureData(assure) ? get() : NULL;}//----------------------------------------------------------------------------------------// CTextPtr::get//----------------------------------------------------------------------------------------const CTextPtr::charT* CTextPtr::get(difference_type assure) const{	return AssureData(assure) ? get() : NULL;}//----------------------------------------------------------------------------------------// CTextPtr::get//----------------------------------------------------------------------------------------CTextPtr::charT* CTextPtr::get(){	OSErr err = noErr;	charT* ptr = 0;	if (!fBuffer)		err = minErr;	else	{		ptr = fBuffer->get();		if (!ptr)			err = minErr;		else		{			if ((fOffset < 0) || (fOffset >= fBuffer->GetSize()))				err = dsChkErr;			else				ptr += fOffset;		}	}	ThrowIfOSErr_AC(err);	return ptr;}//----------------------------------------------------------------------------------------// CTextPtr::get//----------------------------------------------------------------------------------------const CTextPtr::charT* CTextPtr::get() const{	OSErr err = noErr;	charT* ptr = 0;	if (!fBuffer)		err = minErr;	else	{		ptr = fBuffer->get();		if (!ptr)			err = minErr;		else		{			if ((fOffset < 0) || (fOffset >= fBuffer->GetSize()))				err = dsChkErr;			else				ptr += fOffset;		}	}	ThrowIfOSErr_AC(err);	return ptr;}//========================================================================================// Global Functions//========================================================================================//******************************************************************************////	Following are utility routines for parsing that use a CTextPtr instead of//	a char*////******************************************************************************//----------------------------------------------------------------------------------------// strlen//----------------------------------------------------------------------------------------/*int strlen(const CTextPtr& THIS){	return THIS.length();}//----------------------------------------------------------------------------------------// strchr//----------------------------------------------------------------------------------------CTextPtr& strchr(const CTextPtr& THIS, int what){	char*	p1 = THIS->GetDataPtr(THIS->length());}*///----------------------------------------------------------------------------------------// strncmp//----------------------------------------------------------------------------------------int strncmp(const char* str1, const CTextPtr& str2, size_t len){	const char* p1 = str1;	CTextPtr p2 = str2;	char c1, c2;		len++;		while (--len)	{		if ((c1 = *p1) != (c2 = *p2))			return c1 - c2;		else if (!c1)			break;		++p1;		++p2;	}	return 0;}//----------------------------------------------------------------------------------------// strncmp//----------------------------------------------------------------------------------------int strncmp(const CTextPtr& str1, const char* str2, size_t len){	CTextPtr p1 = str1;	const char* p2 = str2;	char c1, c2;		++len;		while (--len)	{		if ((c1 = *p1) != (c2 = *p2))			return c1 - c2;		else if (!c1)			break;		++p1;		++p2;	}	return 0;}//----------------------------------------------------------------------------------------// strstr//----------------------------------------------------------------------------------------CTextPtr strstr(const CTextPtr& str, const char* pat){	CTextPtr s1 = str;	const char* p1 = pat;	char firstc, c1, c2;		if (!(firstc = *p1++))		return s1;	while ((c1 = *s1) != 0)	{		s1++;		if (c1 == firstc)		{			CTextPtr s2 = s1;			const char* p2 = p1;						while ((c1 = *s2) == (c2 = *p2) && c1)			{				++s2;				++p2;			}						if (!c2)			{				--s1;				return s1;			}		}	}		s1 = NULL;	return s1;}//----------------------------------------------------------------------------------------// FindToken//----------------------------------------------------------------------------------------CTextPtr FindToken(const CTextPtr& string, const char* tokens){	CTextPtr	stringPosition = string;	char*		tokenPosition = const_cast<char*>(tokens);		while (*stringPosition != *tokenPosition)	{		++tokenPosition;		if (*tokenPosition == '\0')		{			tokenPosition = const_cast<char*>(tokens);			++stringPosition;			if (stringPosition.AtEnd())				break;		}	}		return stringPosition;}//----------------------------------------------------------------------------------------// FindNoSpacePrintASCIIToken//----------------------------------------------------------------------------------------CTextPtr FindNoSpacePrintASCIIToken(const CTextPtr& str){	CTextPtr	string = str;	while ((*string < 0x21) && (!string.AtEnd()))		++string;		return string;}//----------------------------------------------------------------------------------------// MakeUpperCase//----------------------------------------------------------------------------------------void MakeUpperCase(const CTextPtr& str, long length){	CTextPtr string = str;	if (string == NULL)		return;			while (length > 0)	{		char c = *string;		if ((c >= 'a') && (c <= 'z'))		{			string[0] = (c - 32);		}		--length;		++string;	}}//----------------------------------------------------------------------------------------// ParseSymbol//----------------------------------------------------------------------------------------CTextPtr ParseSymbol(const CTextPtr& parse, CTextPtr& symbol, long& length){	CTextPtr parser = parse;	symbol = parse;	char curChar = *parser;	while (((curChar >= 'A') && (curChar <= 'Z')) ||			((curChar >= 'a') && (curChar <= 'z')) ||			((curChar >= '0') && (curChar <= '9')) ||			(curChar == '.') ||			(curChar == '-') ||			(curChar == '_'))	{		++parser;		curChar = *parser;	}	length = parser.GetOffset() - symbol.GetOffset();	return parser;}//----------------------------------------------------------------------------------------// ParseValue//----------------------------------------------------------------------------------------CTextPtr ParseValue(const CTextPtr& parse, CTextPtr& value, long* length, const char* except){	CTextPtr	parser = parse;	char		quoteChar = '\0';	// check if we're a quoted value	if ((*parser == '\"') || (*parser == '\''))	{		quoteChar = *parser;	// save quote char		++parser;				// value starts at next character	}	// now we've got the value start	value = parser;	// if we're quoted, the value ends at the closing quote,	// else it ends at the first character that's less than or equal to a space or in except.	if (quoteChar != '\0')	{		while ((*parser != quoteChar) && !parser.AtEnd())		{			++parser;		}	}	else	{		while ((*parser > ' ') && ((except == NULL) || (strchr(except, *parser) == NULL)))		{			++parser;		}	}	*length = parser.GetOffset() - value.GetOffset();	// Skip the closing quote, if there was one.	if ((quoteChar != '\0') && (*parser == quoteChar))		++parser;	return parser;}//----------------------------------------------------------------------------------------// End of CTextPtr.cpp
//----------------------------------------------------------------------------------------// CMemBlock.cpp// Copyright © 1999-2000 by Apple Computer, Inc.  All rights reserved.//----------------------------------------------------------------------------------------/*	Change History		03/31/00	Need to include MemoryUtils_AC. 		08/18/99	Initial version. */#ifndef __CMemBlock__#include "CMemBlock.h"#endif// ACS#ifndef _MEMMGR_#include "MemMgr.h"#endif#ifndef __CException_AC__#include "CException_AC.h"#endif#ifndef __MemoryUtils_AC__#include "MemoryUtils_AC.h"#endif//========================================================================================// class CMemBlock//========================================================================================//----------------------------------------------------------------------------------------// CMemBlock::duplicate//----------------------------------------------------------------------------------------CMemBlock CMemBlock::duplicate(const CMemBlock& from){	size_t size = from.BlockSize();	char* block = static_cast<char*>(MMAllocate(size));	ThrowIfNULL_AC(block);	BlockMove_AC(from, block, size);	return block;}//----------------------------------------------------------------------------------------// CMemBlock::reallocate//----------------------------------------------------------------------------------------CMemBlock& CMemBlock::reallocate(size_t newSize){	if (fBlock)		fBlock = static_cast<char*>(MMReallocate(fBlock, newSize));	else		fBlock = static_cast<char*>(MMAllocateClear(newSize));	return *this;}//----------------------------------------------------------------------------------------// End of CMemBlock.cpp
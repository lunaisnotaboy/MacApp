// CParseTerm.h// Copyright © 1999-2000 by Apple Computer, Inc. All rights reserved. /*	Change History:		10/13/00	CWP6 support: namespacing. 		04/18/00	Remove parse namespace. 		04/17/00	Rename nodes to terms. Remove string versions of Parse. 							Move CParseTerm and CParseToken to their own units. 							Remove pointer to parent term. */#ifndef __CParseTerm__#define __CParseTerm__// Network#ifndef __CParseToken__#include "CParseToken.h"#endif// ACS#ifndef __CException_AC__#include "CException_AC.h"#endif#ifndef __CTextToken__#include "CTextToken.h"#endif#ifndef __MTrackable_AC__#include "MTrackable_AC.h"#endif#ifndef __TemplateList_AC__#include "TemplateList_AC.h"#endif#ifndef __Types_AC__#include "Types_AC.h"#endif// ANSI// Externalclass CParseTerm;class CParseToken;//========================================================================================// CParseTermList//========================================================================================class CParseTermList : public TemplateList_AC<CParseTerm>{public:	CParseTermList(ArrayIndex_AC initialSize = 0) : TemplateList_AC<CParseTerm>(initialSize) {}		// Default constructor. 	CParseTermList(const CParseTermList& from) : TemplateList_AC<CParseTerm>(from) {}		// Copy constructor. 	CParseTermList(CParseTerm* term1);	CParseTermList(CParseTerm* term1, CParseTerm* term2);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7, CParseTerm* term8);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7, CParseTerm* term8, CParseTerm* term9);	CParseTermList(CParseTerm* term1, CParseTerm* term2, CParseTerm* term3, CParseTerm* term4, CParseTerm* term5, CParseTerm* term6, CParseTerm* term7, CParseTerm* term8, CParseTerm* term9, CParseTerm* term10);		// Convenience constructors. 	virtual ~CParseTermList() {}		// Destructor. };//========================================================================================// CParseTerm//========================================================================================class CParseTerm : public MTrackable_AC// CParseTerm is the basic parse grammar term class. {public:	// Types	typedef CConstTextToken::size_type			size_type;	typedef CConstTextToken::difference_type	difference_type;	// Construct and destroy	CParseTerm(IDType identifier);	virtual ~CParseTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the passed in text. If it succeeds, it updates the text pointer to 		// point to the next text to parse, and returns true. 		// Otherwise leaves the text pointer unchanged, and returns false. 		// The parser may add tokens containing parsed results to the parent token. 		// Tokens	virtual CParseToken* MakeToken(const CConstTextToken& text) const;		// Make a new token. 		// A class that overrides the MakeToken method should declare it as returning the 		// actual subclass of CParseToken that it creates. 	// Notification		IDType GetIdentifier() const	{ return fIdentifier; }		// Retrieve the identifier of this term.	// Dataprotected:	IDType				fIdentifier;	// A unique identifier.};//========================================================================================// COptionalTerm//========================================================================================class COptionalTerm : public CParseTerm// COptionalTerm is a term that controls another term but always succeeds. {public:	COptionalTerm(IDType identifier, CParseTerm& subterm);	virtual ~COptionalTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. 	// Dataprotected:	CParseTerm&			fSubterm;		// The term that is allowed to be optional.};//========================================================================================// CMultiTerm//========================================================================================class CMultiTerm : public CParseTerm// CMultiTerm is a term that has multiple subterms. {public:	CMultiTerm(IDType identifier, const CParseTermList& subterms,			   const CParseTerm* skipWhiteSpace = NULL);	virtual ~CMultiTerm();	// Dataprotected:	CParseTermList		fSubterms;			// A list of our subterms.	const CParseTerm*	fSkipWhiteSpace;	// White space skipper. };//========================================================================================// CSequenceTerm//========================================================================================class CSequenceTerm : public CMultiTerm// CSequenceTerm succeeds if all of its subterms succeed in the order they are defined. {public:	CSequenceTerm(IDType identifier, const CParseTermList& subterms,				  const CParseTerm* skipWhiteSpace = NULL);	virtual ~CSequenceTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. };//========================================================================================// CSwitchTerm//========================================================================================class CSwitchTerm : public CMultiTerm// CSwitchTerm succeeds if one of its subterms succeeds. {public:	CSwitchTerm(IDType identifier, const CParseTermList& subterms,				const CParseTerm* skipWhiteSpace = NULL);	virtual ~CSwitchTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. };//========================================================================================// CAnyOfTerm//========================================================================================class CAnyOfTerm : public CMultiTerm// CAnyOfTerm allows its subterms to appear in any order, and any number of times. {public:	CAnyOfTerm(IDType identifier, const CParseTermList& subterms,			   const CParseTerm* skipWhiteSpace = NULL);	virtual ~CAnyOfTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. };//========================================================================================// CSetOfTerm//========================================================================================class CSetOfTerm : public CMultiTerm// CSetOfTerm allows the subterms to appear in any order but each subterm can appear// no more than once. {public:	CSetOfTerm(IDType identifier, const CParseTermList& subterms,			   const CParseTerm* skipWhiteSpace = NULL);	virtual ~CSetOfTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. };//========================================================================================// CCharSetTerm//========================================================================================class CCharSetTerm : public CParseTerm// CCharSetTerm is a term that parses characters in a defined set. {public:	CCharSetTerm(IDType identifier, const char* chars);	virtual ~CCharSetTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. 	// Dataprotected:	const std::string		fChars;};//========================================================================================// CNonBreakingSpaceTerm//========================================================================================class CNonBreakingSpaceTerm : public CCharSetTerm// CNonBreakingSpaceTerm is a term that parses non-breaking white space. {public:	CNonBreakingSpaceTerm(IDType identifier);	virtual ~CNonBreakingSpaceTerm();};//========================================================================================// CWhiteSpaceTerm//========================================================================================class CWhiteSpaceTerm : public CParseTerm// CWhiteSpaceTerm is a term that parses white space. It uses isspace so the white space// includes: space, tab, return, new line, vertical tab, or form feed. {public:	CWhiteSpaceTerm(IDType identifier);	virtual ~CWhiteSpaceTerm();	// Parsing	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Parses the text in the input token. };//========================================================================================// CLiteralParseTerm//========================================================================================class CLiteralParseTerm : public CParseTerm{// CLiteralParseTerm is a parse term which tests for a specific literal.public:	CLiteralParseTerm(IDType identifier, const char* theLiteral);	virtual ~CLiteralParseTerm();	const std::string& GetLiteral() const { return fLiteral; }	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Look for an exact match of the given literal string.private:	const std::string	fLiteral;};//========================================================================================// CIntegerParseTerm//========================================================================================class CIntegerParseTerm : public CParseTerm// CIntegerParseTerm is a parse term which scans the input string for one or more// numeric digits. If it finds at least one integer then it plugs the value into the// parent term's value list. {public:	CIntegerParseTerm(IDType identifier);	virtual ~CIntegerParseTerm();	virtual bool Parse(CConstTextToken& text, CParseToken* parentToken = NULL) const;		// Look for a number consisting of digits.  It must have at least		// one digit.};//========================================================================================// CIntegerParseToken//========================================================================================class CIntegerParseToken : public CParseToken// CIntegerParseToken is a token representing an integer detected during parsing.{public:	CIntegerParseToken(const CIntegerParseTerm* term, const CConstTextToken& text, 					   long value);	virtual ~CIntegerParseToken();	long GetInteger() const { return fInteger; }private:	long		fInteger;};#endif // __CParseTerm__
// CTerminalProtocol.h// Copyright © 2000 by Apple Computer, Inc. All rights reserved./*	Change History:		05/24/00	Finish separating char I/O from endpoints. 							Isolate clients from look errors. 		05/17/00	Disconnect is really Close. 		05/17/00	Go to the endpoint for the state. 		05/16/00	Poll for received data. Add state. 		03/28/00	Initial version. */#ifndef __CTerminalProtocol__#define __CTerminalProtocol__// Network#ifndef __CTCPEndpoint__#include "CTCPEndpoint.h"#endif#ifndef __CTextBuffer__#include "CTextBuffer.h"#endif// ACS// Open Transport#ifndef __OPENTRANSPORT__#include <OpenTransport.h>#endif#ifndef __OPENTPTINTERNET__#include <OpenTptInternet.h>#endif// ANSI#include <string>// Externalclass CCharOutputSink_AC;class CYielder_AC;//========================================================================================// CTerminalProtocol//========================================================================================class EXPORT_AC CTerminalProtocol{public:	enum	{		kImmediate			= 0L,		kForever			= 0x7FFFFFFF	};	//------------------------------------------------------------------------------------	// Construction and Destruction	//------------------------------------------------------------------------------------	CTerminalProtocol(CYielder_AC* yielder, CCharOutputSink_AC& log,					  MConnectionTrace* inProgress = NULL);		// Constructor. 	CTerminalProtocol(const CTerminalProtocol& from);		// CTerminalProtocol copy constructor. 	virtual ~CTerminalProtocol();		// Destructor. 	//------------------------------------------------------------------------------------	// State	//------------------------------------------------------------------------------------	bool IsConnected() const;	bool IsDone() const;	//------------------------------------------------------------------------------------	// Notifier Callbacks	//------------------------------------------------------------------------------------	void Cancel(OSStatus error = kOTCanceledErr);		// Cancel any pending I/O. 	//------------------------------------------------------------------------------------	// Connect and Disconnect	//------------------------------------------------------------------------------------	void Connect(const InetAddress& inAddress);	OSStatus Close();	//------------------------------------------------------------------------------------	// Terminal	//------------------------------------------------------------------------------------	void Send(const char* text, size_t count);		// Send characters. They will be buffered. 	void SendDo(char option);	void SendDont(char option);	void SendOption(bool doOption, char option);	void SendInterruptProcess();	void SendAbortOutput();	void SendAreYouThere();	void SendEraseCharacter();	void SendEraseLine();	void SendBreak();	void SendTelnetCommand(char command);	void Flush();		// Send whatever's been buffered. 	void Transmit(const char* text, size_t count);		// Send the text immediately. Not buffered. 	long Available();		// Determines the number of characters that are available for reading. That is a		// minimum, not an exact count, because more data may arrive at any time. 		// Returns -1 if the connection is ended. 	long Receive(char* buffer, size_t maxlen, OTTimeout timeout = kForever);		// Receive at most maxlen characters into buffer. 	//------------------------------------------------------------------------------------	// Data	//------------------------------------------------------------------------------------protected:	CTCPEndpoint			fTCPEndpoint;	CCharOutputSink_AC&		fLog;	bool					fWasConnected;	bool					fTelnetMode;	bool					fLocalEcho;	CTextBuffer				fWriteBuffer;};#endif // __CTerminalProtocol__
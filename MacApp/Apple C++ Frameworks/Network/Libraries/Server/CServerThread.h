// CServerThread.h// Copyright © 2000 by Apple Computer, Inc. All rights reserved./*	Change History:		04/07/00	Initial version. */#ifndef __CServerThread__#define __CServerThread__// Network// ACS#ifndef __CAutoPtr_AC__#include "CAutoPtr_AC.h"#endif#ifndef __CCooperativeThread_AC__#include "CCooperativeThread_AC.h"#endif// Toolbox#ifndef __OPENTRANSPORT__#include <OpenTransport.h>#endif// ANSI// Externalclass MConnectionTrace;class CTCPEndpoint;class CYielder_AC;//========================================================================================// CServerThread//========================================================================================class EXPORT_AC CServerThread : public CCooperativeThread_AC{public:	//------------------------------------------------------------------------------------	// Construction and Destruction	//------------------------------------------------------------------------------------	CServerThread();		// Constructor. 	CServerThread(const CServerThread& from);		// Copy constructor. 	virtual ~CServerThread();		// Destructor. 	virtual EndpointRef DoMakeEndpoint(CYielder_AC* yielder, MConnectionTrace* inProgress = NULL);		// Create our endpoint.  	//------------------------------------------------------------------------------------	// Connection	//------------------------------------------------------------------------------------	virtual void Run();		// Receive the request, send a response, and disconnect. 	virtual bool ReceiveRequest();		// Receive the request. 	virtual bool SendResponse();		// Send a response to the request. 	void Disconnect();		// Orderly disconnect. 	//------------------------------------------------------------------------------------	// Data	//------------------------------------------------------------------------------------protected:	CAutoPtr_AC<CTCPEndpoint>	fEndpoint;};#endif // __CServerThread__
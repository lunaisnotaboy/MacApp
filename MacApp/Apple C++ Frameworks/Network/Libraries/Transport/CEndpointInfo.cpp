//----------------------------------------------------------------------------------------// CEndpointInfo.cpp// Copyright © 1999-2000 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History:		06/22/99	Split off from CEndpoint. */#ifndef __CEndpointInfo__#include "CEndpointInfo.h"#endif// Network// ACS//	#ifndef __CAssertionFailure_AC__//	#include "CAssertionFailure_AC.h"//	#endif// Toolbox// ANSI//========================================================================================// CEndpointInfo//========================================================================================//----------------------------------------------------------------------------------------// CEndpointInfo construct from an existing endpoint//----------------------------------------------------------------------------------------CEndpointInfo::CEndpointInfo(EndpointRef endpoint){	Clear();	if (endpoint != kOTInvalidEndpointRef)		ThrowIfOSStatus( endpoint->GetEndpointInfo(this) );}//----------------------------------------------------------------------------------------// CEndpointInfo copy constructor//----------------------------------------------------------------------------------------CEndpointInfo::CEndpointInfo(const TEndpointInfo& from){	addr = from.addr;	options = from.options;	tsdu = from.tsdu;	etsdu = from.etsdu;	connect = from.connect;	discon = from.discon;	servtype = from.servtype;	flags = from.flags;}//----------------------------------------------------------------------------------------// End of CEndpointInfo.cpp
// CEndpointInfo.h// Copyright © 1999-2000 by Apple Computer, Inc. All rights reserved./*	Change History:		06/22/99	Split off from CEndpoint. */#ifndef __CEndpointInfo__#define __CEndpointInfo__// Network#ifndef __Transport__#include "Transport.h"#endif// ACS// Open Transport#ifndef __OPENTRANSPORT__#include <OpenTransport.h>#endif//========================================================================================// CEndpointInfo//========================================================================================class EXPORT_AC CEndpointInfo : public TEndpointInfo// CEndpointInfo wraps an Open Transport TEndpointInfo structure. {public:	CEndpointInfo()	{		Clear();	}		// Default constructor. 	CEndpointInfo(EndpointRef endpoint);		// Construct from an existing endpoint. 	CEndpointInfo(const TEndpointInfo& from);		// Copy constructor. 	~CEndpointInfo() {}		// Destructor. 	void Clear()	{		addr = options = tsdu = etsdu = connect = discon = servtype = flags = 0;	}		// Zero everything. };#endif // __CEndpointInfo__
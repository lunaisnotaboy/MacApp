//----------------------------------------------------------------------------------------// COption.cpp// Copyright © 2000 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History:		04/07/00	Initial version. */#ifndef __COption__#include "COption.h"#endif// Network#ifndef __Transport__#include "Transport.h"#endif// ACS// Toolbox// ANSI//========================================================================================// COption//========================================================================================//----------------------------------------------------------------------------------------// Default constructor. //----------------------------------------------------------------------------------------COption::COption(){	len = 0; // subclass must fill in real length	level = 0;	name = 0;	status = 0;}//----------------------------------------------------------------------------------------// Constructor. //----------------------------------------------------------------------------------------COption::COption(OTXTILevel inLevel, OTXTIName inName){	len = 0; // subclass must fill in real length	level = inLevel;	name = inName;	status = 0;}//----------------------------------------------------------------------------------------// Copy constructor. //----------------------------------------------------------------------------------------COption::COption(const COption& from){	len = from.len;	level = from.level;	name = from.name;	status = from.status;}//----------------------------------------------------------------------------------------// Request//----------------------------------------------------------------------------------------void COption::Request(EndpointRef ep, OTFlags inFlags){	// Set up the request for OTOptionManagement to point	// to the option buffer we just filled out, and tell	// it the type of request we want.		TOptMgmt request;	request.opt.buf	= (UInt8*) this;	request.opt.len	= len;	request.flags	= inFlags;	// Set up the reply for OTOptionManagement.  This is where	// OTOptionManagement puts the result of the request.		TOptMgmt result;	result.opt.buf = (UInt8*) this;	result.opt.maxlen = len;	// Call OTOptionManagement. The result of the request will	// be in the option status. 		ThrowIfOSStatus(OTOptionManagement(ep, &request, &result));}//========================================================================================// Global Functions//========================================================================================//----------------------------------------------------------------------------------------// Test//----------------------------------------------------------------------------------------static void Test(EndpointRef ep){	UInt32 value = 0;	if ((GetInternetOption(ep, IP_REUSEADDR, value) == noErr) && !value)		SetInternetOption(ep, IP_REUSEADDR, true);}//----------------------------------------------------------------------------------------// End of COption.cpp
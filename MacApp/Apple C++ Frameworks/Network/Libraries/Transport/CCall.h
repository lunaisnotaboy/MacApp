// CCall.h// Copyright © 1999-2000 by Apple Computer, Inc. All rights reserved./*	Change History:		06/08/99	Initial version. */#ifndef __CCall__#define __CCall__// Network#ifndef __Transport__#include "Transport.h"#endif// ACS// Open Transport#ifndef __OPENTRANSPORT__#include <OpenTransport.h>#endif#ifndef __OPENTPTINTERNET__#include <OpenTptInternet.h>#endif//========================================================================================// CCall//========================================================================================class EXPORT_AC CCall : public TCall// CCall wraps an Open Transport connection call structure. {public:	CCall();		// Default constructor. 	CCall(InetAddress& address, OTSequence seq = 0);		// Construct for an internet address. 	CCall(DNSAddress& address, OTSequence seq = 0);		// Construct for a DNS address. 	CCall(const TCall& from);		// Copy constructor. 	~CCall() {}		// Destructor. 	void Clear();		// Zero everything. };#endif // __CCall__
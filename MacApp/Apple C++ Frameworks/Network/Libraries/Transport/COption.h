// COption.h// Copyright © 2000 by Apple Computer, Inc. All rights reserved./*	Change History:		04/07/00	Initial version. */#ifndef __COption__#define __COption__// Network#ifndef __Transport__#include "Transport.h"#endif// ACS// Open Transport#ifndef __OPENTRANSPORT__#include <OpenTransport.h>#endif#ifndef __OPENTPTINTERNET__#include <OpenTptInternet.h>#endif//========================================================================================// COption//========================================================================================class COption : public TOptionHeader// COption wraps an Open Transport option structure. {public:	COption();		// Default constructor. 	COption(OTXTILevel inLevel, OTXTIName inName);		// Constructor. 	COption(const COption& from);		// Copy constructor. 	~COption() {}		// Destructor. 	void Request(EndpointRef ep, OTFlags inFlags);		// Make an option management request using this option. };//========================================================================================// TemplateOption//========================================================================================template <class T>class TemplateOption : public COption// TemplateOption is a template corresponding to the Open Transport option structure,// where the value is typed. {public:	T	value;					// data goes herepublic:	TemplateOption() :		COption(),		value()	{		len = sizeof(*this);	}		// Default constructor. 	TemplateOption(OTXTILevel inLevel, OTXTIName inName, T inValue) :		COption(inLevel, inName),		value(inValue)	{		len = sizeof(*this);	}		// Construct with a value. 	TemplateOption(const TemplateOption<T>& from) :		COption(from),		value(from.value)	{		// (len is already correct because from must be the same type.) 	}		// Copy constructor. 	~TemplateOption() {}		// Destructor. };//========================================================================================// TemplateOption<UInt32>//========================================================================================EXPORT_CLASS_AC(TemplateOption<UInt32>)	// The most common option value and used for internet IP options. 	// Corresponds to kOTFourByteOptionSize. //========================================================================================// Global Functions//========================================================================================//----------------------------------------------------------------------------------------// SetOption//----------------------------------------------------------------------------------------template <class T>UInt32 SetOption(EndpointRef ep, OTXTILevel inLevel, OTXTIName inName, T inValue)// Function for setting an Open Transport option of any type. // Returns 0 if it succeeds. // Otherwise, returns flag bits: T_FAILURE, T_PARTSUCCESS, T_READONLY or T_NOTSUPPORT. {	TemplateOption<T> option(inLevel, inName, inValue);	option.Request(ep, T_NEGOTIATE);	return (option.status != T_SUCCESS) ? option.status : noErr;}//----------------------------------------------------------------------------------------// GetOption//----------------------------------------------------------------------------------------template <class T>UInt32 GetOption(EndpointRef ep, OTXTILevel inLevel, OTXTIName inName, T& outValue)// Function for getting an Open Transport option of any type. // Returns 0 if it succeeds. // Otherwise, returns flag bits: T_FAILURE, T_PARTSUCCESS or T_NOTSUPPORT. {	TemplateOption<T> option(inLevel, inName, 0);	option.Request(ep, T_CURRENT);	outValue = option.value;	return ((option.status != T_SUCCESS) && (option.status != T_READONLY)) ? option.status : noErr;}//----------------------------------------------------------------------------------------// SetInternetOption//----------------------------------------------------------------------------------------inline UInt32 SetInternetOption(EndpointRef ep, OTXTIName inName, UInt32 inValue)// Function for setting an Open Transport internet IP option. // Returns 0 if it succeeds. // Otherwise, returns flag bits: T_FAILURE, T_PARTSUCCESS, T_READONLY or T_NOTSUPPORT. {	return SetOption(ep, INET_IP, inName, inValue);}//----------------------------------------------------------------------------------------// GetInternetOption//----------------------------------------------------------------------------------------inline UInt32 GetInternetOption(EndpointRef ep, OTXTIName inName, UInt32& outValue)// Function for getting an Open Transport internet IP option. // Returns 0 if it succeeds. // Otherwise, returns flag bits: T_FAILURE, T_PARTSUCCESS or T_NOTSUPPORT. {	return GetOption(ep, INET_IP, inName, outValue);}#endif // __COption__
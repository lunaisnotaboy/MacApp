// CBlocking.h// Copyright © 2000 by Apple Computer, Inc. All rights reserved./*	Change History:		02/17/00	Initial version. */#ifndef __CBlocking__#define __CBlocking__// Network#ifndef __Transport__#include "Transport.h"#endif// ACS// Open Transport#ifndef __OPENTRANSPORT__#include <OpenTransport.h>#endif// ANSI// External//========================================================================================// CBlocking//========================================================================================class EXPORT_AC CBlocking{public:	//------------------------------------------------------------------------------------	// Construction and Destruction	//------------------------------------------------------------------------------------	CBlocking(EndpointRef endpoint);		// Save the old blocking state. 	CBlocking(EndpointRef endpoint, bool blocking);		// Save the old blocking state and set a new blocking state. 	~CBlocking();		// Restore the old locking state. protected:	EndpointRef		fEndpoint;	bool			fWasBlocking;};#endif // __CBlocking__
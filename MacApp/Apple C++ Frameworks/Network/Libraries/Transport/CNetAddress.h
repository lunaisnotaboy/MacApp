// CNetAddress.h// Copyright © 1999-2001 by Apple Computer, Inc. All rights reserved./*	Change History:		02/05/01	Carbon needs to open internet services in context. 		10/06/00	Made ~CInternetServices virtual.		05/15/00	CW Pro 6: namespacing. 		05/02/00	Fix overly clever constructor for ConstStringPtr. 		01/24/00	Pascal string support. 		01/09/00	Simplify implementation of InetAddress output stream operator. 							Add a Terminate method for CInternetServices. 		12/08/99	Add AsString and assignment operators for CDNSAddress. 		09/16/99	Added some constness.		09/08/99	Added string conversion.		08/16/99	Add standard stream operators. 		06/14/99	Add a constructor for CInetAddress that does a name lookup. 							Rename ToString to ToDottedString. 							Add stream operators. 							Add CInternetServices. 		06/08/99	Initial version. */#ifndef __CNetAddress__#define __CNetAddress__// Network#ifndef __COpenTransport__#include "COpenTransport.h"#endif#ifndef __Transport__#include "Transport.h"#endif// ACS#ifndef __CAutoPtr_AC__#include "CAutoPtr_AC.h"#endif// Open Transport#ifndef __OPENTPTINTERNET__#include <OpenTptInternet.h>#endif// ANSI#include <string>#include <ostream>// Externalclass CStream_AC;class ostream;//========================================================================================// CInetAddress//========================================================================================class EXPORT_AC CInetAddress : public InetAddress// CInetAddress wraps an Open Transport internet address. {public:	CInetAddress()	{		fAddressType = AF_INET;		fPort = 0;		fHost = 0;	}		// Default constructor. 	CInetAddress(const InetHost host, const InetPort port = 0)	{		fAddressType = AF_INET;		fPort = port;		fHost = host;	}		// Construct for an existing buffer. 	CInetAddress(const char* name, const InetPort port = 0);	CInetAddress(ConstStringPtr name, const InetPort port = 0);		// Construct from a name. May throw an exception if the name lookup fails. 	CInetAddress(const InetAddress& from)	{		fAddressType = AF_INET;		fPort = from.fPort;		fHost = from.fHost;	}		// Copy constructor. 	~CInetAddress() {}		// Destructor. 	void ToDottedString(char* str) const;		std::string AsString() const;	void Set(const char* name, const InetPort port = 0);	void Set(ConstStringPtr name, const InetPort port = 0);		// Set from a name. May throw an exception if the name lookup fails. };CStream_AC& operator << (CStream_AC& inStream, const CInetAddress& address);std::ostream& operator<<(std::ostream& s, const InetAddress& address);//========================================================================================// CDNSAddress//========================================================================================class EXPORT_AC CDNSAddress : public DNSAddress// CDNSAddress wraps an Open Transport internet address. {public:	CDNSAddress()	{		fAddressType = AF_DNS;		fName[0] = 0;	}		// Default constructor. 	CDNSAddress(const char* name);	CDNSAddress(ConstStringPtr name);		// Construct from a name. 	CDNSAddress(const DNSAddress& from);		// Copy constructor. 	~CDNSAddress() {}		// Destructor. 	std::string AsString() const;	CDNSAddress& operator=(const char* name);	CDNSAddress& operator=(ConstStringPtr name);	CDNSAddress& operator=(const DNSAddress& from);};CStream_AC& operator << (CStream_AC& inStream, const DNSAddress& address);std::ostream& operator<<(std::ostream& s, const DNSAddress& address);//========================================================================================// CInternetServices//========================================================================================class EXPORT_AC CInternetServices : public MUsingOpenTransport// CInternetServices wraps the Open Transport internet service API. {public:	CInternetServices();		// Constructor. #if qCarbon	CInternetServices(OTClientContextPtr clientContext);		// Constructor with a client context. #endif	virtual ~CInternetServices();		// Destructor. 	static OSStatus StringToAddress(const char* name, InetHostInfo* hinfo);	static OSStatus AddressToName(const InetHost addr, InetDomainName name);	static OSStatus SysInfo(const char* name, InetSysInfo* sysinfo);	static OSStatus MailExchange(const char* name, UInt16* num, InetMailExchange* mx);	static OSStatus Query(const char* name, UInt16 qClass, UInt16 qType, char* buf, size_t buflen, 						  void** argv, size_t argvlen, OTFlags flags);	static void Terminate();private:	InetSvcRef		fServices;	static CAutoPtr_AC<CInternetServices>	fgInstance;	static InetSvcRef GetService();};#endif // __CNetAddress__
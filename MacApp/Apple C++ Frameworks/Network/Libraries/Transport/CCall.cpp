//----------------------------------------------------------------------------------------// CCall.cpp// Copyright © 1999-2000 by Apple Computer, Inc. All rights reserved.//----------------------------------------------------------------------------------------/*	Change History:		05/22/00	Change TemplateNetbuf from class to function and specialize							for DNSAddress. 		05/15/00	CW Pro 6: namespacing. 		03/31/00	DNS address needs to be the actual length. 		06/08/99	Initial version. */#ifndef __CCall__#include "CCall.h"#endif// Network#ifndef __CNetAddress__#include "CNetAddress.h"#endif#ifndef __CNetbuf__#include "CNetbuf.h"#endif// ACS// Toolbox// ANSI#include <cstring>using std::strlen;//========================================================================================// CCall//========================================================================================//----------------------------------------------------------------------------------------// CCall Default constructor. //----------------------------------------------------------------------------------------CCall::CCall(){	Clear();}//----------------------------------------------------------------------------------------// CCall Construct for an internet address. //----------------------------------------------------------------------------------------CCall::CCall(InetAddress& address, OTSequence seq){	Clear();	addr = TemplateNetbuf(address);	sequence = seq;}//----------------------------------------------------------------------------------------// CCall Construct for a DNS address. //----------------------------------------------------------------------------------------CCall::CCall(DNSAddress& address, OTSequence seq){	Clear();	addr = TemplateNetbuf(address);	sequence = seq;}//----------------------------------------------------------------------------------------// CCall Copy constructor. //----------------------------------------------------------------------------------------CCall::CCall(const TCall& from){	addr = from.addr;	opt = from.opt;	udata = from.udata;	sequence = from.sequence;}//----------------------------------------------------------------------------------------// CCall::Clear//----------------------------------------------------------------------------------------void CCall::Clear(){	CNetbuf::Clear(addr);	CNetbuf::Clear(opt);	CNetbuf::Clear(udata);	sequence = 0;}//----------------------------------------------------------------------------------------// End of CCall.cpp
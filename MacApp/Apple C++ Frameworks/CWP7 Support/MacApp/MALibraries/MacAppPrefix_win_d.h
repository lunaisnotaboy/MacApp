// MacAppPrefix_win_d.h// Copyright © 2001 by Apple Computer, Inc.  All rights reserved./*	Change History		08/15/01	Initial version. */// Turn on flags// Turn on flags for Carbon#define qCarbon 0// No longer support emulating controls.#define qEmulateStandardControls 0// For Universal Headers#define TARGET_CARBON 1// This switch is for conditioning in stuff that doesn't compile with Mach-0#define qCarbonMach0 0// We're not using TidyHeap for MachO right now.#define qTidyHeap 0// ========================================================// ••• BIG RED SWITCH FOR DEBUGGING •••// 0=nodebug, 1=debug (finer grain control can be found in CWMacAppSwitches.h)// ========================================================#if !defined(wantDebugging)#	define wantDebugging 1#endif#pragma once on#pragma check_header_flags on// Set to 1 if you want to use precompiled headers#ifndef	qUSE_PRECOMPILED_HEADERS #define qUSE_PRECOMPILED_HEADERS    0#endif // Set to 1 if you are building a DLL or shared library.#ifndef	qExportSymbols_AC #define qExportSymbols_AC    1#endif #ifndef __CoreSwitches_AC__#include "CoreSwitches_AC.h"#endif#ifndef __ConditionalMacros_AC__#include "ConditionalMacros_AC.h"#endif#if qAutoSwitches#	ifndef __MacAppAutoSwitches__#	define __MacAppAutoSwitches__#	include "MacAppAutoSwitches.h"#	endif#endif#ifndef __MacAppSwitches__#include "MacAppSwitches.h"#endif//#ifndef __FP__//#include <fp.h>//#endif#ifndef __ansi_prefix__#include <ansi_prefix.win32.h>#endif#ifndef __MacAppRequired__#include "MacAppRequired.h"#endif